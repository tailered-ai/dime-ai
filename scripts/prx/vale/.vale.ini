; PRX v1.1 style layer — STE-inspired subset (NOT ASD-STE100 conformity;
; Vale cannot certify ASD-STE100, and no official issue/rule mapping is
; claimed — SOL-PRX-012). Scoped: invoked only with --config on designated
; PRX surfaces (extracted PR-body prose, the PRX standard document, and the
; vale-controls). It never governs repository Markdown at large.
StylesPath = styles
MinAlertLevel = suggestion

Vocab = PRX

[*.{md,txt}]
BasedOnStyles = PRXSTE
