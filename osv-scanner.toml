# osv-scanner config — dependency security gate, replacing `pnpm audit`.
#
# `pnpm audit` calls npm's legacy /-/npm/v1/security/audits[/quick] endpoints,
# which npm retired 2026-07-15 (410 Gone). The fix landed in pnpm v11.0, but
# this repo is intentionally pinned to pnpm@10.33.0 (see package.json's
# "packageManager" field — pnpm/action-setup@v4 misparses a sha512 hash there,
# GHSA-379q-355j-w6rj) and pnpm v11 silently stops reading the package.json
# "pnpm" config block (overrides/patchedDependencies/onlyBuiltDependencies/
# auditConfig) — a bigger, separate migration. `npm audit` was tried as a
# substitute and abandoned: it can't even resolve this repo's tree (a
# transitive dependency, @upsetjs/venn.js@2.0.0, ships a Yarn-only `patch:`
# devDependency reference that npm's parser has never supported).
#
# osv-scanner reads pnpm-lock.yaml directly — no install, no dependency
# resolution, no package-manager involvement — so it sidesteps both problems
# and audits the *actual* pinned versions instead of a resolver's guess.
# See scripts/check-osv-scan.mjs for the HIGH/CRITICAL severity gate this
# feeds into.
#
# Reviewed quarterly. Each entry below is a real, currently-open finding
# confirmed unfixable (or not applicable to production) as of 2026-07-15 —
# re-verify against a fresh `osv-scanner scan source -L pnpm-lock.yaml`
# before removing or renewing any entry.

[[IgnoredVulns]]
id = "GHSA-4r6h-8v6p-xvw6"
reason = "xlsx@0.18.5 Prototype Pollution — patched:<0.0.0, no fix exists upstream. Server-side only."

[[IgnoredVulns]]
id = "GHSA-5pgg-2g8v-p4x9"
reason = "xlsx@0.18.5 ReDoS — patched:<0.0.0, no fix exists upstream. Server-side only."

[[IgnoredVulns]]
id = "GHSA-jmr9-qjv8-65gv"
reason = "extract-zip@2.0.1 symlink path traversal — OSV last_affected:2.0.1, no fixed release exists (2026-08-26). Transitive via @puppeteer/browsers, which only extracts Chromium archives fetched from Google's CDN server-side; no user-supplied zips reach it. Pre-existing lockfile dep — surfaced on PR #520 which adds zero dependencies."

# RESOLVED 2026-08-03 — GHSA-5xrq-8626-4rwp (vitest UI dev-server file read/execute)
# entry removed: the tracked "future vitest 4.x migration" landed (vitest 2.1.9 ->
# 4.1.10), which is outside the advisory's affected range. Real fix installed beats
# allowlisting; ignore list shrinks by one.

# RESOLVED 2026-08-03 — GHSA-mh99-v99m-4gvg (brace-expansion ReDoS) entry removed.
# The 2026-07-24 rationale ("1.1.16 is the newest 1.x ever published; every working
# version is vulnerable") became obsolete when upstream shipped patched 1.x releases:
# 1.1.17 fixes GHSA-mh99-v99m-4gvg and 1.1.18 fixes its bypass GHSA-rgw5-rvv9-x895.
# The lockfile now pins brace-expansion@1.1.18 (same 1.x line, bare-callable API —
# the minimatch@3 -> glob@7 -> rimraf@3 chain is unaffected, unlike the 5.x jump the
# old entry documented). The package.json override floor moved >=1.1.16 -> >=1.1.18.

# RESOLVED 2026-08-07 — three entries removed, each fixed by a real version upgrade
# rather than an allowlist. Verified against the advisories' authoritative affected
# ranges and the versions pinned in pnpm-lock.yaml:
#
#   GHSA-37ch-88jc-xwx2  path-to-regexp ReDoS      vulnerable <0.1.13, patched 0.1.13
#                        lockfile pins 0.1.13 (+8.4.2) via the package.json override
#                        "express>path-to-regexp": ">=0.1.13 <0.2.0". The old reason
#                        ("patched >=0.1.13 does not exist on npm; requires an express
#                        v5 migration") is obsolete — 0.1.13 shipped and is installed.
#
#   GHSA-r5fr-rjxr-66jc  lodash _.template injection  vulnerable <=4.17.23, patched 4.18.0
#                        lockfile pins lodash@4.18.1. The old reason ("patched >=4.18.0
#                        does not exist on npm") is obsolete.
#
#   GHSA-fx2h-pf6j-xcff  vite server.fs.deny bypass   vulnerable 7.0.0-7.3.4, patched 7.3.5
#                        lockfile pins vite@7.3.6, and the transitive vite@5.x the old
#                        reason named is gone (7.3.6 is the only vite in the tree).
#
# osv-scanner reported all three as "unused ignores" before removal. Removing them
# restores fail-closed behaviour: if any regresses, the gate blocks instead of
# silently allowing it.
