# PRX v1.1 — focused blocking-path mutation dispositions (R8)

Configuration: `scripts/prx/stryker.blocking.prx.json` — StrykerJS 9
(vitest runner), mutating exactly the modules whose logic is reachable by
an `APPROVED_BLOCKING` verdict: `commit-check.mjs`, `body-check.mjs`,
`modes.mjs`, `policy-source.mjs`, `resolve-range.mjs`, `rules.mjs`, and
the CLI wrappers `check-commit.mjs` / `check-body.mjs` (added on
internal-review challenge: listCommits and the exit-code wiring are
decision-reachable).
Raw report: `mutation-blocking-results.json` (this directory). The broader
all-file run (`stryker.prx.json`, including `rule-metadata.mjs` display
strings) is EXPLORATORY AND NON-GATING; its report is
`mutation-results.json`.

Scope exclusions, implemented structurally per R8's allowances:

- Rule-title/surface display metadata lives in `rule-metadata.mjs`,
  excluded from the focused `mutate` list (nothing in the decision path
  consumes it; `rules.test.ts` pins key-set equality and an independent
  class table so registry drift cannot hide there).
- Diagnostic finding-message text is excluded via 65 explicit
  `// Stryker disable next-line StringLiteral: diagnostic message text
  (R8 exclusion)` annotations at the exact literal sites (29 in
  commit-check, 36 in body-check). Rule-id literals, key names, node-type
  strings, mode names, regex literals, and every Boolean / conditional /
  comparison / parser / grammar / verdict mutant remain fully enabled.
  On five shared lines (commit-check 139/150/163/169/229-equivalents,
  body-check 96/185/200 pre-format) the rule-id literal shares the
  annotated line, so its mutant is suppressed too; those rule-id mutants
  were KILLED before annotation (an emptied rule id throws
  `unknown PRX rule id` in `ruleClass`), so no live signal was lost.

Method: iterative runs with two empirical instruments. Round 1 (before hardening) scored 72.17% with 300
survivors + 66 no-coverage. Every one of those 366 mutants was classified
by empirical re-application (mutants patched into a sandbox copy and run
against proposed tests; equivalence claims additionally checked by a
10,008-message differential fuzz for commit-check and a machine-evaluated
predicate matrix for body-check's looksNarrative family). The
classification produced the annotation sites, 100+ new killing assertions
across `commit-check.test.ts`, `body-check.test.ts`, `modes.test.ts`,
`trusted-boundary.test.ts`, and `resolve-range.test.ts`, and the
individual equivalence proofs below. Later rounds verified the result after each hardening batch; the final
report is the last run over the final sources. Because StrykerJS cannot
activate mutants across a process boundary (subprocess CLI tests) and
does not reliably re-execute module scope (static mutants), every
remaining survivor was ALSO hand-applied to the real source with the
real suite re-run (`replay-mutants` log in the bundle): a replayed
failure is recorded as KILLED-IN-REALITY with the artifact class named,
and only replay-passing mutants remain as true survivors requiring an
equivalence proof.

## Acceptance criteria (R8) — met

- Zero unreviewed survivors: every surviving mutant appears in the proof
  table below.
- Zero unexplained no-coverage mutants: every no-coverage mutant appears
  in the no-coverage table with the exact reason its path cannot execute
  in-process (dead defensive arm, or subprocess-only wrapper code that
  Stryker cannot activate across a process boundary — those paths are
  pinned by the subprocess CLI tests, which run the REAL artifact).
- Every equivalent mutant has an individual written proof tied to file,
  line, operator, and logical implication (below).
- Every deterministic blocking condition has a direct negative test that
  fails when the condition is removed (the adversarial fixture suite pins
  exact finding multisets per rule; the R8 hardening added boundary and
  anchor tests for every surviving comparison/regex/guard mutant that was
  killable).

Mutation evidence CANNOT authorize enforcement. It removes one technical
blocker from a future owner decision; the mode stays audit and the
required-check set is untouched.
## Final focused result

These figures are the r1 HISTORICAL record, taken over the sources at
head d98793c21545bd4685151640140dd60c09a6190a. The r2 correction pass
changed the decision-path sources; r2 mutation figures come from the
label-gated GitHub Actions rerun over the changed modules and are
reported separately in the r2 ledger — they do not overwrite this block.

- Mutants generated: 1605 (of which 74 ignored by the explicit diagnostic-text annotations)
- Killed: 1269  |  Timeout (counts as detected): 153
- Survived (Stryker view): 85  |  No coverage (Stryker view): 24
- Of those 109: 35 are replay-KILLED-IN-REALITY (Stryker activation artifacts, each hand-verified), and the rest carry individual equivalence proofs or exact no-coverage explanations below
- Mutation score (detected / valid, Stryker view): 92.88%
- Effective detection including replay-verified kills: 1457/1531 = 95.17%
- Unreviewed survivors: 0  |  Unexplained no-coverage: 0

## r2 corrections (MUT-01..04) — disposition-integrity findings from the second independent review

All four accepted; the corrected rows below are edited in place and
tagged "CORRECTED r2".

- **MUT-01** — body-check.mjs:436 claimed the extractProse catch could
  "never be returned in-process". FALSE: `extractProse(">".repeat(20000))`
  reaches the catch in-process (reproduced before any r2 edit). The
  mutant was a WEAK-ORACLE survivor (the r1 test asserted only
  `typeof`); r2 strengthens the assertion to the exact value, which
  kills it. Catch reachability additionally became DETERMINISTIC in r2:
  the original reproduction depended on where the parser's recursion
  overflowed (it varied with the runtime stack budget — the same input
  parsed cleanly under Stryker's workers), so a structural pre-cap
  (blockquote depth 512 per line) now throws before parsing,
  identically everywhere. Row corrected.
- **MUT-02** — three replay-artifact rows carried misattributed
  mechanism labels: body-check 113 and 435 blamed the subprocess
  boundary and body-check 116 blamed static module scope (and called
  its NoCoverage status a "survivor"). All three sites are catch arms
  reachable IN-PROCESS by the deep-nesting tests; the real Stryker
  artifact is per-test coverage attribution of catch arms under
  in-worker activation. Rows corrected.
- **MUT-03** — resolve-range.mjs:59 (`slice(0, 80)` removal) was
  labeled EQUIVALENT although the mutant changes the emitted
  diagnostic's content and length (unbounded quotation of malformed
  merge-base output). Reclassified NOT EQUIVALENT; a bounded-quote
  assertion was added and negative-tested against the applied mutant.
  Row corrected.
- **MUT-04** — the replay log disagreed with the final report in three
  ways, reconciled on 2026-08-14: (1) entries 598 and 599
  (body-check.mjs:581/582) recorded `status: "Survived",
  replayKilled: false` — TRUE-SURVIVOR claims with no equivalence
  proofs — although the FINAL `mutation-blocking-results.json` records
  both as Killed; they came from a superseded pre-final round and are
  removed. (2) Mutant 213 (body-check.mjs:246, Survived in the final
  report) had never been replayed, so the Method section's
  "every remaining survivor was ALSO hand-applied" claim was false by
  one; it was replayed on 2026-08-14 against the archived r1 tree at
  d98793c21545bd4685151640140dd60c09a6190a and is a TRUE-SURVIVOR
  (suite passes), consistent with its written equivalence proof; its
  record is added. (3) The record count now matches the final
  survivor+no-coverage population exactly: 109 entries, body-check 32
  (previously 110 and 33 — the drift the review's "8-vs-9" pointer
  flagged; the per-file counts, not a line count, were the mismatched
  pair).

### scripts/prx/body-check.mjs

| Line | Mutator | Replacement | Stryker status | Verdict | Proof / evidence |
| --- | --- | --- | --- | --- | --- |
| 97 | ArrayDeclaration | `["Stryker was here"]` | NoCoverage | NO-COVERAGE (explained) | `tree.children ?? []` fallback: fromMarkdown always returns a root with a children ARRAY (probed: "" -> children: []), so the ?? arm is dead code — which is also why Stryker records no coverage. |
| 113 | BlockStatement | `{}` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay fails the real suite (the deep-blockquote test reaches checkBody's parse-failure catch IN-PROCESS; the emptied catch lets execution fall through to a TypeError). CORRECTED r2 (MUT-02): the prior mechanism label blamed the subprocess boundary — wrong; the true artifact is Stryker/vitest failing to attribute per-test coverage to the catch arm under in-worker activation, so the mutant was scheduled against no tests. |
| 116 | StringLiteral | `""` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay fails the real suite (blanking the rule id makes makeFinding throw `unknown PRX rule id` on the in-process-reachable catch path). CORRECTED r2 (MUT-02): the prior text claimed "Stryker recorded a survivor ... module scope" — wrong on both counts; the recorded status is NoCoverage and the line sits inside checkBody's catch, not module scope. Same catch-arm coverage-attribution artifact as line 113. |
| 152 | OptionalChaining | `c.position.start` | Survived | EQUIVALENT | c ranges only over parsed comment nodes from mdast-util-from-markdown, which stamps position on every compiled node (probed; no hand-built nodes on this path) — the optional access can never short-circuit. |
| 180 | OptionalChaining | `capsule.position.start` | Survived | EQUIVALENT | capsule = capsules[0] is drawn from visible = filtered tree.children — parser-produced nodes always carrying position; identical evaluation everywhere. |
| 246 | ConditionalExpression | `true` | Survived | EQUIVALENT | `n.type === "heading"` conjunct -> true in the heading filter: the predicate becomes n.depth === 2, and in mdast `depth` exists only on heading nodes — every non-heading evaluates undefined === 2 === false and still fails the filter, while headings evaluate exactly the original right operand. Same equivalence class as the section-scan twin, executed against a paragraph-only body: findings identical. |
| 250 | OptionalChaining | `n.position.start` | Survived | EQUIVALENT | n is a depth-2 heading filtered from visible — parser-produced, position always present. |
| 314 | MethodExpression | `EXTENSION_ALLOWLIST_PREFIXES.every(p => ` | Survived | EQUIVALENT | some -> every: the receiver is a frozen module constant of length exactly 1 ("Evidence record") that nothing mutates; for a one-element array some(f) === every(f); divergence needs length 0 or >= 2, which cannot occur. |
| 341 | OptionalChaining | `node.position.start` | Survived | EQUIVALENT | walk/loop visits only nodes from the parsed tree (visible and their descendants); every parser-produced node carries position, so the optional chain never short-circuits (applies identically at the Unicode-bullet, blockquoted-list, raw-HTML-list, and FENCE emit sites). |
| 348 | ArrayDeclaration | `["Stryker was here"]` | NoCoverage | NO-COVERAGE (explained) | `node.children ?? []` fallback in the blockquoted-list check: the right operand evaluates only for blockquote nodes, and every parser-produced blockquote carries a children array (probed: bare `>` -> children: []). The fallback arm is unreachable. |
| 355 | OptionalChaining | `node.position.start` | Survived | EQUIVALENT | walk/loop visits only nodes from the parsed tree (visible and their descendants); every parser-produced node carries position, so the optional chain never short-circuits (applies identically at the Unicode-bullet, blockquoted-list, raw-HTML-list, and FENCE emit sites). |
| 369 | OptionalChaining | `node.position.start` | Survived | EQUIVALENT | walk/loop visits only nodes from the parsed tree (visible and their descendants); every parser-produced node carries position, so the optional chain never short-circuits (applies identically at the Unicode-bullet, blockquoted-list, raw-HTML-list, and FENCE emit sites). |
| 399 | OptionalChaining | `node.position.start` | Survived | EQUIVALENT | walk/loop visits only nodes from the parsed tree (visible and their descendants); every parser-produced node carries position, so the optional chain never short-circuits (applies identically at the Unicode-bullet, blockquoted-list, raw-HTML-list, and FENCE emit sites). |
| 435 | BlockStatement | `{}` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay fails the real suite (the deep-nesting test reaches extractProse's catch IN-PROCESS; the emptied catch falls through to a TypeError on the undefined parse result). CORRECTED r2 (MUT-02): the prior subprocess-boundary label was wrong — the artifact is catch-arm coverage attribution, as at line 113. |
| 436 | StringLiteral | `"Stryker was here!"` | NoCoverage | WEAK-ORACLE, killed by r2 test | CORRECTED r2 (MUT-01): the prior claim — "the literal can never be returned in-process (catch-arm reachability proof)" — was FALSE. `extractProse(">".repeat(20000))` throws inside the parser and returns through this catch IN-PROCESS, so the sentinel IS returned; the mutant survived the replay because the r1 oracle asserted only `typeof === "string"`. Reclassified from NO-COVERAGE (explained) to WEAK-ORACLE; body-check.test.ts now asserts the exact value (`toBe("")`), which kills the sentinel. To be confirmed as a plain kill in the r2 label-gated rerun. |
| 485 | ConditionalExpression | `default:` | NoCoverage | NO-COVERAGE (explained) | capsuleValueError default: clause: called solely from the loop over the frozen six-key CAPSULE_KEYS tuple, each with an explicit case label, so default: is unreachable. Double protection: falling off the switch returns undefined, and the caller tests plain truthiness where undefined and the original null are indistinguishable. |
| 499 | ConditionalExpression | `true` | Survived | EQUIVALENT | `n.type === "heading"` -> true: condition becomes n.depth <= 2; depth exists only on heading nodes, so non-headings evaluate undefined <= 2 === false — still fail — while headings evaluate exactly the original right operand. Executed against both subheading bodies: findings byte-identical. |
| 508 | StringLiteral | `""` | Survived | EQUIVALENT | case label blanked: a thematicBreak lands on default:, where visibleInlineText returns "" (no value, no children, not image/html) and the default returns false — exactly the original shared `return false`. |
| 509 | StringLiteral | `""` | Survived | EQUIVALENT | case label blanked: definition nodes carry no value and no children, so the rerouted default computes "".trim() !== "" -> false, identical to the original case result. |
| 512 | ConditionalExpression | `case "code":` | Survived | EQUIVALENT | code-case consequent removed: falls through to the html case, whose body is `return visibleInlineText(node).trim() !== ""`; for a code node visibleInlineText hits `node.value !== undefined` and returns node.value — the fallthrough computes node.value.trim() !== "", the removed body's exact semantics. |
| 512 | StringLiteral | `""` | Survived | EQUIVALENT | case label blanked: code nodes take default:, which equals node.value.trim() !== "" for every code node (visibleInlineText returns node.value for value-bearing non-html nodes). |
| 516 | StringLiteral | `""` | Survived | EQUIVALENT | case label blanked: html nodes fall to default:, whose body is the very same expression as the html case; visibleInlineText dispatches on node.type === "html" internally, so the switch label was never load-bearing. |
| 520 | StringLiteral | `""` | Survived | EQUIVALENT | case label blanked: tables reroute to default: (concatenated text of all rows/cells non-blank) vs original children.some(row -> children.some(cell)). GFM table cells contain inline content only, so on both paths every leaf contributes exactly its visibleInlineText, and the concatenation trims non-empty iff at least one cell's text does. |
| 521 | StringLiteral | `""` | Survived | EQUIVALENT | case label blanked: tableRow nodes are reachable here only via the table case's children.some; the rerouted default (row text non-blank) is equivalent by the same inline-only-cells argument. |
| 523 | ArrayDeclaration | `["Stryker was here"]` | NoCoverage | NO-COVERAGE (explained) | `node.children ?? []` fallback in nodeHasVisibleContent: executes only for the container case labels, and the parser always materializes children arrays on those node types (probed for degenerate cases). The fallback never evaluates. |
| 533 | StringLiteral | `"Stryker was here!"` | NoCoverage | NO-COVERAGE (explained) | `node.alt ?? ""` fallback: mdast-util-from-markdown always sets alt to a STRING for image and imageReference (probed: ![](x.png) -> alt: ""; ![][r] -> alt: ""), never null/undefined — the nullish arm is dead for every parser-produced node. |
| 544 | MethodExpression | `value.split("\n")` | Survived | EQUIVALENT | blank-line filter removal in looksNarrative: whitespace-only lines influence neither count (the listish regex demands a bullet/digit after ^\s*; the sentenceish predicate tests the trimmed line — both fail); the early length-0 return coincides with the fallthrough result. Matrix-executed: outputs identical. |
| 544 | ConditionalExpression | `true` | Survived | EQUIVALENT | filter predicate -> true retains all lines — behaviorally identical to removing the filter; the adjacent proof applies verbatim (matrix-identical). |
| 544 | MethodExpression | `l` | Survived | EQUIVALENT | l.trim() !== "" -> l !== "": only whitespace-only non-empty lines are additionally retained; they contribute to neither count (adjacent proof), and the empty-vs-fallthrough coincidence covers the all-blank case. Matrix-identical. |
| 544 | StringLiteral | `"Stryker was here!"` | Survived | EQUIVALENT | filter sentinel "" -> "Stryker was here!": trim can only equal that string if the line contained it; such a line is 3 words with no terminal punctuation and no bullet — contributing 0 to both counts under the original too, so even the pathological input yields the same return. |
| 545 | ConditionalExpression | `false` | Survived | EQUIVALENT | guard bypassed: with empty lines both filters produce empty arrays, listish=0, sentenceish=0, and `0 > 0 \|\| 0 >= 1` is false — precisely what the guard returned. Pure fast-path. |
| 548 | MethodExpression | `l` | Survived | EQUIVALENT | first l.trim() removal in the punctuation test: /[.!?]\s*$/ is unanchored at the start (leading whitespace irrelevant) and its \s*$ tail absorbs exactly the trailing whitespace trim removes — re.test(l) === re.test(l.trim()) for every string. Matrix-identical. |

### scripts/prx/check-body.mjs

| Line | Mutator | Replacement | Stryker status | Verdict | Proof / evidence |
| --- | --- | --- | --- | --- | --- |
| 19 | StringLiteral | `""` | Survived | EQUIVALENT | readFileSync encoding "utf8" -> "": JSON.parse coerces the returned Buffer to the identical UTF-8 string (same as the check-commit twin). The sibling filename literal is killed by the default-mode test (readFileSync of the directory throws). |
| 23 | MethodExpression | `process.argv` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 50 | ConditionalExpression | `false` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 50 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 51 | StringLiteral | `""` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 83 | ConditionalExpression | `false` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 85 | BlockStatement | `{}` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |

### scripts/prx/check-commit.mjs

| Line | Mutator | Replacement | Stryker status | Verdict | Proof / evidence |
| --- | --- | --- | --- | --- | --- |
| 23 | StringLiteral | `""` | Survived | EQUIVALENT | readFileSync encoding "utf8" -> "": without an encoding readFileSync returns a Buffer; JSON.parse coerces the Buffer to the identical UTF-8 string before parsing, so parseModeState receives the same document. (The sibling filename literal on this line is killed: join(dir, "") targets the directory and readFileSync throws.) Verified by hand-applied replay: full suite passes only because behavior is identical. |
| 43 | ConditionalExpression | `false` | Survived | NO-COVERAGE (explained) | malformed-record guard -> false: the guard's trigger — a git-log record with fewer than 5 newline-separated fields — cannot be produced by any commit object: %H/%P/%an/%ae contribute four newline-terminated fields and %B contributes at least one more (an empty message yields exactly 5, pinned by the empty-message test). Disabling a branch that no git-produced input reaches is unobservable; the guard is defense against a corrupted stream. |
| 43 | BlockStatement | `{}` | NoCoverage | NO-COVERAGE (explained) | malformed-record throw block: unreachable for every git-produced record (see the guard proof — records always have >= 5 fields); the block exists to fail closed on a corrupted git stream and has no reachable trigger to cover. |
| 45 | StringLiteral | `''` | NoCoverage | NO-COVERAGE (explained) | message literal of the unreachable malformed-record throw (an Error message, not a makeFinding diagnostic): dead for the same reachability reason. |
| 46 | StringLiteral | `""` | NoCoverage | NO-COVERAGE (explained) | second literal of that same unreachable throw message: dead for the same reachability reason. |
| 53 | MethodExpression | `parents.trim().split(/\s+/)` | Survived | EQUIVALENT | .filter(Boolean) removal: %P is a single-space-separated list of full parent hashes with no leading/trailing whitespace. For merges split yields the parent array unchanged; for single-parent commits [p1]; for root commits "" splits to [""] with length 1 — on every shape `length > 1` answers identically with or without the Boolean filter. |
| 53 | MethodExpression | `parents` | Survived | EQUIVALENT | .trim() removal: %P output never carries leading or trailing whitespace (git prints the bare hash list), so trim is a no-op on every git-produced record; root/single/merge shapes all split identically untrimmed. |
| 53 | Regex | `/\s/` | Survived | EQUIVALENT | /\s+/ -> /\s/: %P separates parent hashes by exactly ONE space, so the + quantifier never matches more than a single character; both regexes produce identical splits on every git-produced parents field. |
| 77 | MethodExpression | `process.argv` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 79 | ObjectLiteral | `{}` | Survived | EQUIVALENT | flags initializer {} : every consumer treats absent and false identically — checkCommit tests `opts.governed === true` (undefined fails exactly like false), the exemptions test `=== true` the same way, and `if (flags.json)` treats undefined as falsy like false. Assignments from the flag branches still work on the empty object. |
| 82 | StringLiteral | `""` | Survived | EQUIVALENT | default repo "." -> "": the only consumer is resolve(repo), and path.resolve("") === path.resolve(".") === process.cwd() — identical for every invocation. |
| 103 | StringLiteral | `"Stryker was here!"` | NoCoverage | NO-COVERAGE (explained) | `range ?? ""` right operand: the grammar check runs only inside `if (range !== undefined)`, so range is never nullish at this line and the ?? arm is dead — hence no coverage. |
| 125 | ConditionalExpression | `false` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 125 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 126 | StringLiteral | `""` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 176 | ConditionalExpression | `false` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 178 | BlockStatement | `{}` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |

### scripts/prx/commit-check.mjs

| Line | Mutator | Replacement | Stryker status | Verdict | Proof / evidence |
| --- | --- | --- | --- | --- | --- |
| 36 | StringLiteral | `''` | Survived | EQUIVALENT | PREFIX_STRIP_RE template -> ``: new RegExp("") strips nothing, so description becomes subject.trim(). Sole consumer is COPULA_RE.test; by copula-invariance (the strippable prefix `type(scope)?!?: ` contains no whitespace-delimited copula token, and description is a trimmed suffix whose token boundaries survive in both strings) the boolean is identical for every subject. |
| 36 | StringLiteral | `""` | Survived | EQUIVALENT | join("\|") -> join(""): the mutated prefix alternation still contains no copula token and merely stops matching real prefixes; by the same copula-invariance argument the only consumer, COPULA_RE.test, returns the same boolean for every subject. |
| 42 | Regex | `/^([A-Za-z][A-Za-z0-9-]*):[ \t]*(.*)/` | Survived | EQUIVALENT | TRAILER_LINE_RE loses trailing $: applied only to single split("\n") lines with no newline; the greedy (.*) always consumes to end-of-string where $ is vacuously satisfied — match success and both capture groups are byte-identical. |
| 54 | StringLiteral | `"Stryker was here!"` | NoCoverage | NO-COVERAGE (explained) | `lines[0] ?? ""` right operand: String.prototype.split always returns >= 1 element (even "" -> [""]), so lines[0] is never nullish and the ?? arm never evaluates — dead code, hence no coverage. Non-string raw cannot reach this line (checkCommit returns PRX-C-SIZE first; direct parseCommitMessage(nonString) throws earlier at raw.replace). Fuzz-confirmed unobservable. |
| 68 | StringLiteral | `"Stryker was here!"` | Survived | EQUIVALENT | fenceMarker initializer -> sentinel: only read in `m[1][0] === fenceMarker` behind `inFence === true`, and inFence only becomes true alongside the overwrite `fenceMarker = m[1][0]`; the initial value is dead on every path. |
| 92 | ConditionalExpression | `true` | Survived | EQUIVALENT | Guard (or its right conjunct) forced true: with empty bodyLines, end=-1 makes `end >= start` fail; with an unclosed fence the final run either starts in-fence (rejected by !block[0].inFence) or contains the fence-opener line, which matches neither trailer-line nor continuation shape, failing block.every — trailers stays null in every newly-entered case, identical to skipping the block. |
| 92 | LogicalOperator | `!unclosedFence \|\| bodyLines.length > 0` | Survived | EQUIVALENT | && -> \|\|: differs only when one conjunct is false — exactly the empty-body / unclosed-fence cases, both still yielding trailers === null by the same argument. |
| 92 | ConditionalExpression | `true` | Survived | EQUIVALENT | Guard (or its right conjunct) forced true: with empty bodyLines, end=-1 makes `end >= start` fail; with an unclosed fence the final run either starts in-fence (rejected by !block[0].inFence) or contains the fence-opener line, which matches neither trailer-line nor continuation shape, failing block.every — trailers stays null in every newly-entered case, identical to skipping the block. |
| 92 | EqualityOperator | `bodyLines.length >= 0` | Survived | EQUIVALENT | > 0 -> >= 0: differs only at length === 0, where end=-1 makes `end >= start` fail and trailers stays null, exactly as when the block is skipped. |
| 94 | ConditionalExpression | `true` | Survived | EQUIVALENT | `end >= 0` conjunct -> true: the separator scan guarantees bodyLines[0] is non-blank when bodyLines is non-empty, so the blank-skip walk always terminates at index >= 0; the guard is never the deciding conjunct and bodyLines[-1] is never read. |
| 94 | EqualityOperator | `end > 0` | Survived | EQUIVALENT | >= 0 -> > 0: differs only when end === 0 is evaluated, where the original tests bodyLines[0] — non-blank by the separator-scan invariant — and terminates with end === 0, exactly what the mutant does by short-circuiting. |
| 98 | ConditionalExpression | `true` | Survived | EQUIVALENT | At this point start >= 0 always holds (the start-walk ends at a blank index >= -1 then +=1) and end >= start always holds (bodyLines[end] is non-blank, so the start-walk decrements at least once before +=1). Forcing the whole condition or either conjunct to true replaces an always-true expression with true — unobservable. |
| 98 | LogicalOperator | `end >= start \|\| start >= 0` | Survived | EQUIVALENT | && -> \|\|: both operands are always true (see the adjacent proof); true\|\|true === true&&true. |
| 98 | ConditionalExpression | `true` | Survived | EQUIVALENT | At this point start >= 0 always holds (the start-walk ends at a blank index >= -1 then +=1) and end >= start always holds (bodyLines[end] is non-blank, so the start-walk decrements at least once before +=1). Forcing the whole condition or either conjunct to true replaces an always-true expression with true — unobservable. |
| 98 | ConditionalExpression | `true` | Survived | EQUIVALENT | At this point start >= 0 always holds (the start-walk ends at a blank index >= -1 then +=1) and end >= start always holds (bodyLines[end] is non-blank, so the start-walk decrements at least once before +=1). Forcing the whole condition or either conjunct to true replaces an always-true expression with true — unobservable. |
| 113 | ConditionalExpression | `true` | Survived | EQUIVALENT | `parsed.length > 0` -> true: the else-branch is reachable only for a non-trailer-shaped block line, but allShaped requires block[0] trailer-shaped, so the first iteration always pushes an entry; every later else sees length >= 1 where the original is already true. |
| 113 | EqualityOperator | `parsed.length >= 0` | Survived | EQUIVALENT | > 0 -> >= 0: tautology; the branch is only reached with length >= 1 (adjacent proof), so parsed[-1] is unreachable. |
| 132 | ArrayDeclaration | `["Stryker was here"]` | Survived | EQUIVALENT | ?? [] -> ?? [sentinel]: feeds new Set(...) whose only consumer is has(l.line) with numeric line; a set holding only a string answers has(number) false (SameValueZero, no coercion), exactly like the empty set. |
| 147 | ConditionalExpression | `true` | Survived | EQUIVALENT | some-predicate -> true makes hasBody = length > 0: by the separator-scan invariant a non-empty bodyLines always contains a non-blank first line, so the original some is true iff non-empty — identical. |
| 147 | MethodExpression | `l.text` | Survived | EQUIVALENT | trim removal in the hasBody predicate: if bodyLines is non-empty, bodyLines[0].text is non-blank (invariant) hence !== "", so both predicates make some true; if empty, both false. |
| 322 | ArrayDeclaration | `["Stryker was here"]` | Survived | EQUIVALENT | ?? [] -> ?? [sentinel]: fallback applies only when trailers is null; the array is immediately filtered by e.key === <governed key>, and a string element's .key is undefined — byKey returns [] exactly as with the empty array. |
| 384 | LogicalOperator | `subject.replace(PREFIX_STRIP_RE, "").tri` | Survived | EQUIVALENT | \|\| subject -> && subject: when the stripped trimmed description is truthy the mutant yields subject — same COPULA_RE.test by copula-invariance; when falsy (""), the case only arises for prefix-plus-whitespace or whitespace-only subjects, which contain no copula either way. |
| 384 | MethodExpression | `subject.replace(PREFIX_STRIP_RE, "")` | Survived | EQUIVALENT | trim removal on description: leading/trailing whitespace cannot create or destroy a COPULA_RE match ((^\|\s) accepts a retained leading space as readily as ^; (\s\|$) likewise at the end); the \|\| fallback fires for "" vs whitespace-only, both copula-free. |

### scripts/prx/modes.mjs

| Line | Mutator | Replacement | Stryker status | Verdict | Proof / evidence |
| --- | --- | --- | --- | --- | --- |
| 16 | MethodExpression | `["PRX-C-SIZE", "PRX-C-SUBJECT", "PRX-C-P` | Survived | EQUIVALENT | APPROVED_BLOCKING .filter(...) removal: the predicate returns true for every element (all listed rules are deterministic — pinned independently by the rules.test.ts class table), so the output array is identical; the filter's only side effect is the defensive load-time throw on registry drift, unreachable while the class table holds. |
| 17 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 18 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 19 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 20 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 21 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 22 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 23 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 24 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 25 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 26 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 27 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 28 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 29 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 30 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 32 | ConditionalExpression | `true` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 32 | ConditionalExpression | `false` | Survived | EQUIVALENT | load-guard condition -> false: ruleClass(id) === "deterministic" for every listed id (independent class-table test), so the original condition is false on every element and the throw never fires — forcing false changes nothing on any reachable path. |
| 32 | EqualityOperator | `ruleClass(id) === "deterministic"` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 32 | StringLiteral | `""` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded a survivor because the mutant sits in module scope (a 'static' mutant) and the vitest runner does not reliably re-execute module scope under in-worker activation. |
| 32 | BlockStatement | `{}` | NoCoverage | NO-COVERAGE (explained) | load-guard throw block: executes only for a non-deterministic id inside APPROVED_BLOCKING, which cannot occur while rules.test.ts pins every listed id deterministic; intentionally dead defensive code. |
| 34 | StringLiteral | `''` | NoCoverage | NO-COVERAGE (explained) | message literal of that same unreachable defensive throw (an Error message, not a makeFinding diagnostic): dead for the load-guard reason. |
| 49 | OptionalChaining | `state.mode` | Survived | EQUIVALENT | state?.mode -> state.mode: the second \|\| operand is evaluated only when `state?.version !== 1` is false, i.e. state?.version === 1, which implies state is non-nullish — where state.mode and state?.mode are identical. (The state?.version chain has its own killing test via JSON null.) |

### scripts/prx/policy-source.mjs

No surviving or uncovered mutants.

### scripts/prx/resolve-range.mjs

| Line | Mutator | Replacement | Stryker status | Verdict | Proof / evidence |
| --- | --- | --- | --- | --- | --- |
| 16 | ArrayDeclaration | `[]` | Survived | EQUIVALENT | stdio array -> []: Node fills missing stdio entries with 'pipe'; none of the invoked git commands (cat-file -e, merge-base, merge-base --is-ancestor) reads stdin, so ignore-vs-pipe stdin is indistinguishable; stdout capture and nonzero-exit throw — the function's whole contract, pinned by every topology/failure test — are preserved. |
| 16 | StringLiteral | `""` | Survived | EQUIVALENT | individual stdio entry blanked: empirically exercised by every resolver test (each calls runGit); no pinned behavior differs — the git children read no stdin, and execFileSync's stdout-capture and throw-on-nonzero contract is unchanged for the mutated wiring. |
| 16 | StringLiteral | `""` | Survived | EQUIVALENT | individual stdio entry blanked: empirically exercised by every resolver test (each calls runGit); no pinned behavior differs — the git children read no stdin, and execFileSync's stdout-capture and throw-on-nonzero contract is unchanged for the mutated wiring. |
| 16 | StringLiteral | `""` | Survived | EQUIVALENT | individual stdio entry blanked: empirically exercised by every resolver test (each calls runGit); no pinned behavior differs — the git children read no stdin, and execFileSync's stdout-capture and throw-on-nonzero contract is unchanged for the mutated wiring. |
| 59 | MethodExpression | `mergeBase` | Survived | NOT EQUIVALENT, killed by r2 test | CORRECTED r2 (MUT-03): the prior EQUIVALENT label was wrong — removing slice(0, 80) observably changes the diagnostic (unbounded attacker-controlled merge-base output quoted into the error instead of an 80-character prefix). A mutant that changes emitted output is a survivor with a weak oracle, not an equivalent. resolve-range.test.ts now asserts the exact 80-character bound (negative-tested: the assertion fails with the mutant applied). |
| 74 | MethodExpression | `process.argv` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 106 | ConditionalExpression | `false` | Survived | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |
| 108 | BlockStatement | `{}` | NoCoverage | ARTIFACT (replay-killed) | KILLED-IN-REALITY — hand-applied replay of this mutant fails the real suite. Stryker recorded no kill because the covering tests execute the mutated file in a SPAWNED subprocess, and mutant activation does not cross the process boundary. |

### scripts/prx/rules.mjs

No surviving or uncovered mutants.

## r2 focused rerun (label-gated CI matrix, Stryker 9.6.1)

Environment and scope, stated exactly: local full mutation runs are
BLOCKED on the r2 machine (memory exhaustion at Stryker concurrency
8-12 on 8 GB physical), so the r2 contract moved the focused runs to
the label-gated GitHub Actions matrix `15-prx-mutation` (one job per
r2-changed decision-path module, `--concurrency 2`, ubuntu-latest,
Stryker 9.6.1 exact-pinned, frozen lockfile, lifecycle scripts
disabled). First rerun: workflow run 31859979909 on head
a436f28abe85f6874b599fe1ba747c3a4edff5c5, all six jobs `success`
(native GitHub conclusions); raw per-module reports and logs ship in
the review bundle's `test-output/`. These figures are SEPARATE from
the r1 historical block above and are scoped to that head and that
environment.

| Module | Valid mutants | Killed | Timeout | Survived | No coverage | Score |
| --- | --- | --- | --- | --- | --- | --- |
| scripts/prx/lib/canonical.mjs | 589 | 483 | 13 | 85 | 8 | 84.21% |
| scripts/prx/commit-check.mjs | 445 | 395 | 1 | 46 | 3 | 88.99% |
| scripts/prx/body-check.mjs | 629 | 537 | 19 | 64 | 9 | 88.39% |
| scripts/prx/check-commit.mjs | 185 | 163 | 1 | 13 | 8 | 88.65% |
| scripts/prx/rules.mjs | 124 | 99 | 0 | 25 | 0 | 79.84% |
| scripts/prx/modes.mjs | 66 | 43 | 0 | 21 | 2 | 65.15% |
| Aggregate (six per-module runs) | 2038 | 1720 | 34 | 254 | 30 | 86.06% |

Survivor triage of the 284 surviving/no-coverage mutants, by class:

1. **Literal-class (128: StringLiteral 119, ObjectLiteral 3,
   ArrayDeclaration 6).** Dominated by diagnostic message fragments and
   configuration data in NEW r2 code (canonical reason strings, the
   sanitizer/container element lists, the rerun's own workflow-visible
   strings). The r1 pass suppressed this class with explicit
   `// Stryker disable next-line StringLiteral` annotations at the
   literal sites; much of the new r2 code does not carry those
   annotations yet, so the class appears as survivors instead of
   Ignored. Post-run hardening killed the highest-value members
   structurally instead of annotating: the FULL sanitizer removed-set
   and container-set element lists are now each pinned by per-element
   tests, the APPROVED_BLOCKING list is pinned by an independent
   exact-set copy plus a blocks-in-enforcing test per rule, and the
   shared grammar anchors (RUN_ID_RE / SHA40_RE / SCOPE_RE) are pinned
   at both ends. Annotating the remaining diagnostic fragments is
   recorded follow-up work, not silently waved off.
2. **r1-carried equivalence classes (recurring spans, line numbers
   shifted).** The r1 proof table above covers these mechanisms
   verbatim where the code is unchanged: parser-position
   OptionalChaining (8), the looksNarrative filter family, the
   final-trailer-block guard tautologies, the CAPSULE/TRAILER
   line-regex `$`-anchor vacuity, the wrapper subprocess/module-scope
   activation artifacts in check-commit.mjs/check-body.mjs, and the
   malformed-git-record defensive guard. The carried proofs are
   identified by original-span text, not line numbers.
3. **Post-run targeted kills (new tests in this pass).** Added after
   run 1 and verified locally: out-of-block governed record integrity
   (governed flag, value trimming, empty values), ungoverned duplicate
   keys draw no governed count, control-finding code-point labels and
   context wording, context-advisory claim wording, revert-shape
   advisory absence without the marker, `--verified-revert` CLI flag
   behavior, sanitized-scanner cross-chunk comment/element state,
   self-closing removed elements, exact drive-letter reason, and the
   evidence-ref adapter root config (run first-segment grammar,
   per-root minimum depth).
4. **Residual register.** Every remaining survivor is enumerated with
   file, line, mutator, and replacement in the bundle's raw per-module
   JSON reports. The residual is dominated by reason-string granularity
   in `evidenceRef` (verdict-equivalent mutants that only reroute WHICH
   rejection reason fires first), scanner index arithmetic whose
   divergence requires inputs the tag grammar excludes, and the
   defensive-arm classes above. HONEST SCOPING: the r2 rerun does NOT
   yet reproduce the r1 bar of zero unreviewed survivors with
   individual written proofs; the class triage above plus the raw
   registers are the r2 disposition, and the label-gated lane re-runs
   on demand (re-add the `prx-mutation` label) so the next pass can
   extend the per-mutant table over the residual.

SUPERSEDED BY r3: the "next pass" named above is the r3 evidence-closure
section below, which carries the per-mutant table over all 226 undetected
mutants. The four paragraphs above remain as the r2 record of what was
known at that head; where the r3 register disagrees with them, the r3
register governs. In particular the "reason-string granularity in
`evidenceRef`" characterization was incomplete — one `evidenceRef`
mutant in that residual was a live blocking-path survivor, not a
reason-string reroute.

## r3 evidence closure — every undetected mutant dispositioned (Gap A)

The r2 record reported 206 survived and 20 no-coverage of 2038 valid
(88.91%) and triaged them BY CLASS. It carried no equivalent of the r1
load-bearing claim "zero unreviewed blocking-path survivors". This
section restores that claim, truthfully, at the level of the individual
mutant.

### Method

Every one of the 226 undetected mutants from the authoritative run 2
was carried through three independent instruments, and all three agree
per mutant in the register below.

1. **Empirical replay.** Each mutant was applied to the real source in
   a frozen worktree at the r2 head and the real mutation-scope suite
   was run against it (the r1 `replay-mutants` instrument, re-run over
   the r2 population; 226 of 226, no sampling). The source was restored
   byte-exactly and its SHA-256 re-verified after every case. A mutant
   the real suite already fails is a Stryker ACTIVATION ARTIFACT —
   module-scope re-execution or a subprocess boundary — not a live
   survivor. 25 of the 226 are artifacts; 201 genuinely pass the r2
   suite.
2. **Classification.** Each mutant was assigned exactly one of
   `BLOCKING_PATH`, `ADVISORY_PATH`, `OFF_PATH`, `EQUIVALENT` against
   the definitions in the r2 contract, with the divergence located by
   differential probe against a sandboxed copy — never by reading alone.
   Uncertainty resolves to the LIVE class, never to `EQUIVALENT`.
3. **Killing tests, negative-tested in both directions.** Every test
   added in this pass was run against the applied mutant (it must fail)
   and against the pristine source (it must pass). Sources were restored
   byte-exactly and SHA-256-verified after every application.

`EQUIVALENT` here means what the r1 contract meant: no reachable input
can change any observable of the exported API — finding multisets
including messages and lines, return values, thrown errors, exit codes.
A mutant that changes only WHICH diagnostic text is emitted is NOT
equivalent; it is recorded `OFF_PATH` with the divergence named.

### Result

| Class | Count | Meaning |
| --- | --- | --- |
| `BLOCKING_PATH` | 71 | can change the presence, rule id, level, or line of an APPROVED_BLOCKING finding, a checker degrade path, or an exit code |
| `ADVISORY_PATH` | 21 | divergence confined to advisory/heuristic findings or the `extractProse` style stream |
| `OFF_PATH` | 56 | diagnostic message text only, or unreachable in-process from any exported entry point (reachability argument given per row) |
| `EQUIVALENT` | 78 | individually proven unobservable |
| **Total** | **226** | 206 survived + 20 no-coverage |

Of the 71 `BLOCKING_PATH` mutants, **25 are Stryker activation
artifacts** that the real suite already kills (18 in `modes.mjs` module
scope, 6 across the `check-commit.mjs` subprocess boundary, 1 in
`canonical.mjs`), and **46 were live**.

### The blocking-path survivor count, stated plainly

**All 46 live blocking-path survivors now have killing tests, so the
count of live unreviewed blocking-path survivors is ZERO — and the
count is not manufactured: it is the result of writing 46 tests, each
negative-tested against its mutant, not of reclassifying anything as
equivalent.** The residual that remains is stated in the same breath:
21 `ADVISORY_PATH` and 56 `OFF_PATH` mutants are deliberately NOT
killed in this pass, and 78 are proven equivalent. Those 77
non-equivalent, non-blocking mutants are the honest remainder — they
cannot change a blocking verdict, and each carries its own row below.

Live blocking-path survivors, by module, all now killed:

| Module | Live | Mutant ids |
| --- | --- | --- |
| `lib/canonical.mjs` | 28 | 41, 42, 43, 44, 45, 113, 135, 137, 144, 170, 171, 187, 190, 273, 295, 322, 340, 427, 487, 538, 540, 541, 542, 545, 546, 550, 569, 571 |
| `body-check.mjs` | 12 | 24, 25, 30, 46, 109, 111, 112, 114, 115, 117, 129, 475 |
| `check-commit.mjs` | 4 | 23, 26, 80, 83 |
| `commit-check.mjs` | 2 | 17, 50 |

`canonical.mjs` was treated as the priority per the r2 contract — it is
the shared layer every predicate routes through, so a live survivor
there has the widest blast radius. It held 28 of the 46.

### Four r1 equivalence proofs refuted

The r1 table's own claims did not survive this pass intact. Four are
wrong, and all four are the MUT-01/MUT-03 class the second review
already flagged — a proof written from a premise that was never
executed:

1. **The "`$`-anchor vacuity" class, wrong at three separate sites.**
   The r1 proof argued that a trailing `$` is vacuous because "the
   greedy `(.*)` always consumes to end-of-string". It does not: `.`
   also excludes CR, U+2028 and U+2029. `parseCommitMessage` normalizes
   only CRLF, and CommonMark does not treat U+2028/U+2029 as line
   endings, so those code points survive inside the very strings these
   regexes match. Refuted at `TRAILER_LINE_RE` (a broken line becomes a
   trailer, flipping the commit into governed scope and inventing
   `PRX-C-GOV` findings), at `CAPSULE_LINE_RE` (a broken capsule line
   validates silently, losing two `PRX-B-CAPSULE` findings), and at
   `FENCE_OPEN_RE` (a broken line opens a fence and reclassifies the
   rest of the body as code).
2. **The malformed-git-record guard, called unreachable.** The r1 proof
   showed no COMMIT OBJECT can produce a short record, which is true and
   was independently re-confirmed here (git refuses NUL in commit
   objects via fsck; forced with `--literally`, `%B` truncates at the
   NUL and NUL-bearing author fields render empty). The proof missed the
   ARGUMENT channel: `listCommits` passes `range` to `git log`
   unvalidated, so a caller-supplied `--format`/`--pretty` rewrites the
   record shape. Without the guard the checker audits bogus records and
   reports `PRX-C-SUBJECT` and `PRX-C-PREFIX` errors for commits it
   never parsed. (The CLI surface is unaffected — `main()`'s range
   grammar blocks the vector — so the divergence is on the exported
   `listCommits` contract.)

One of the four was found only because two independent classifications
of the same mutant disagreed; the dispute was settled by running it.
That is recorded here rather than smoothed over, because it is the
reason the count above can be trusted: disagreement was resolved by
execution, not by preferring the tidier answer.

### One regression introduced by the r2 hardening itself

`canonical.mjs:400` — the drive-letter rejection in `evidenceRef` — was
KILLED in run 1 and SURVIVED in run 2. The r2 post-run hardening
rewrote that assertion to pin the exact reason strings and dropped the
verdict check in the process; the mutant leaves the reason string
untouched, so nothing failed. Under it, a governed commit carrying
`Evidence: C:evil` loses its `PRX-C-GOV` finding entirely. Both cases
now route through the `reject()` helper with anchored regexes, keeping
the exact-reason pins while restoring the verdict assertion. Full
delta in the Gap B section below.

### Verification run (r3, label-gated CI matrix)

The claim above is not left as an argument. Mutation run `31918253169`
re-ran the full focused matrix at head
`a5d632569ca3599483649c72784d5a4d1c286f98`, all six jobs `success`
(native GitHub conclusions), and confirms it mutant-for-mutant.

| Module | Valid | Detected | Survived | No coverage | Score |
| --- | --- | --- | --- | --- | --- |
| `lib/canonical.mjs` | 589 | 542 | 47 | 0 | 92.02% |
| `commit-check.mjs` | 445 | 418 | 26 | 1 | 93.93% |
| `body-check.mjs` | 629 | 568 | 52 | 9 | 90.30% |
| `check-commit.mjs` | 185 | 170 | 11 | 4 | 91.89% |
| `rules.mjs` | 124 | 123 | 1 | 0 | 99.19% |
| `modes.mjs` | 66 | 43 | 21 | 2 | 65.15% |
| **Aggregate** | **2038** | **1864** | **158** | **16** | **91.46%** |

Matching that run's undetected set against the register, mutant for
mutant on file, span, mutator, and replacement: **0 of the 46 live
blocking-path survivors remain undetected.** 24 blocking-path mutants
are still recorded undetected, and every one of them is an activation
artifact the real suite kills — 18 in `modes.mjs` module scope and 6
across the `check-commit.mjs` subprocess boundary — which is what the
226-mutant replay established independently.

Progression across the four runs, on the identical 2038-mutant
population: 86.06% (run 1, pre-hardening baseline) → 88.91% (run 2,
authoritative) → 88.96% (r3, drive-letter regression fixed) →
**91.46%** (r3 verification). The gain is entirely tests; no
decision-path source file was edited in r3.

The remaining 158 survived and 16 no-coverage mutants are the 21
advisory-path, 56 off-path, and 78 equivalent classes, minus those the
new tests killed incidentally. They are enumerated individually below —
not summarized by class.

### Per-mutant register

One row per undetected mutant, ordered by class then line. "Run-2" is
the status Stryker recorded in the authoritative run; "Replay" is the
empirical verdict against the real suite (`killed` = activation
artifact, `survivor` = genuinely undetected). Where a row carries an r1
mechanism verbatim, the carried proof is named in the disposition.

#### `scripts/prx/body-check.mjs`

| Line | Mutator | Replacement | Run-2 | Replay | Class | Disposition / proof |
| --- | --- | --- | --- | --- | --- | --- |
| 64 | Regex | `/([A-Za-z][A-Za-z0-9-]*):[ \t]*(.*)$/` | Survived | survivor | BLOCKING_PATH | Drops the `^` anchor from CAPSULE_LINE_RE. Without `m`, `^` matches only at string start; the regex is applied per split("\n") line in isCapsuleCandidate (line 113) and in the capsule value loop (line 246). Un-anchored, an INDENTED line such as " Scope: TOS-1" now matches with m[1]="Scope", so a bare fence that the original does NOT treat as an identifier capsule (lines.some() is false -> isCapsuleCandidate false -> zero PRX-B-CAPSULE work) becomes a capsule and is run through the strict validator. Observed: PRX-B-CAPSULE (deterministic, APPROVED_BLOCKING) count goes 0 -> 5 on that input. This is presence-of-a-blocking-finding divergence, not message text. static:true, so Stryker's module-scope activation is why it reports Survived even once a test exists; the divergence is real in-process (replayed). |
| 64 | Regex | `/^([A-Za-z][A-Za-z0-9-]*):[ \t]*(.*)/` | Survived | survivor | BLOCKING_PATH | Drops the trailing `$`. The r1/r2 carried claim ('CAPSULE/TRAILER line-regex $-anchor vacuity', r1 commit-check row line 42) is FALSE for this site and I refute it empirically. The argument 'lines from split("\n") contain no newline so (.*)$ is vacuous' ignores CR: `.` does not match \r, and micromark PRESERVES internal \r\n inside a fenced code node's value (probed: value = "Scope: TOS-1\r\nRun-Id: ONE-20260812-A"). On a CRLF body the original regex FAILS to match "Scope: TOS-1\r" (greedy (.*) stops before \r, `$` then cannot be satisfied), producing PRX-B-CAPSULE 'capsule contains a non-key line' + 'capsule key "Scope" is missing'; the mutant matches and both blocking findings disappear. Presence/count of an APPROVED_BLOCKING rule changes (6 -> 4). static:true -> module-scope activation artifact explains the Survived status. |
| 64 | Regex | `/^([A-Za-z][A-Za-z0-9-]*):[^ \t]*(.*)$/` | Survived | survivor | BLOCKING_PATH | Inverts the optional separator class: `[ \t]*` -> `[^ \t]*`. For a space-separated line the greedy [^ \t]* matches empty (next char is a space) and (.*) still captures the value, which .trim() normalizes -> no divergence. For a colon-immediate line ("Scope:TOS-1") the mutant's [^ \t]* greedily eats "TOS-1", leaving m[2]="" -> value === "" -> the checker emits PRX-B-CAPSULE 'capsule key "X" has no value' where the original accepted the value. On a fully valid colon-immediate capsule the blocking finding count goes 0 -> 6. Presence of an APPROVED_BLOCKING finding changes. static:true (module-scope activation artifact). |
| 71 | MethodExpression | `node.value` | Survived | survivor | BLOCKING_PATH | isCommentNode drops .trim(), so an html node whose raw value carries LEADING whitespace (up to 3 spaces still parses as an HTML block, probed) no longer startsWith("<!--") and is reclassified from comment to visible content. `visible` is the input to the capsule-ordering check (line 231 `visible[0] !== capsule`), to bodyRendersMeaningfulText (PRX-B-VISIBLE) and to hasVisibleSectionContent. Concretely, prefixing the healthy fixture with " <!-- note -->\n\n" changes checkBody from [] to a PRX-B-CAPSULE error at line 3 ('visible content precedes the identifier capsule'). PRX-B-CAPSULE is APPROVED_BLOCKING, so a clean PR body becomes a blocking failure: presence of a blocking finding changes. The existing oracle at body-check.test.ts:365 ('comment with TRAILING whitespace') does not kill it — under the mutant that node also stops being a comment, but meaningfulVisibleHtml still strips the comment to " " so PRX-B-VISIBLE still fires; that is the weak-oracle shape (same family as canonical.mjs id 427). |
| 134 | ConditionalExpression | `true` | Survived | survivor | BLOCKING_PATH | The whole skip-condition in parseBody's blockquote pre-cap scan is forced true, so the depth walk BREAKS at the first character that is not '>', instead of skipping spaces and tabs between markers. A line of 513 space-separated markers ("> ".repeat(513)) counts depth 513 in the original and throws the pre-cap Error -> checkBody catches it and degrades to the single PRX-B-SIZE finding (APPROVED_BLOCKING) and extractProse returns "". Under the mutant depth is 1, no throw, and checkBody returns the ordinary 14 PRX-B-SECTION-MISSING findings. This changes checkBody's degrade behavior and the presence of PRX-B-SIZE. NOTE: a killing test HAS ALREADY LANDED — body-check.test.ts:320 'the pre-cap counts markers separated by spaces and tabs (r3)' pins exactly this family for both separators; this mutant list predates that test. |
| 134 | LogicalOperator | `ch !== " " \|\| ch !== "\t"` | Survived | survivor | BLOCKING_PATH | && -> \|\| makes the condition a tautology: a single character cannot equal both " " and "\t", so `ch !== " " \|\| ch !== "\t"` is true for EVERY ch, and the depth walk breaks at the first non-'>' character. Behaviourally identical to id 109: a 513-marker line separated by spaces OR tabs no longer trips the pre-cap, so checkBody's PRX-B-SIZE degrade (APPROVED_BLOCKING) disappears and extractProse stops returning "". Killing test already landed at body-check.test.ts:320. |
| 134 | ConditionalExpression | `true` | Survived | survivor | BLOCKING_PATH | Left conjunct forced true: the condition becomes `true && ch !== "\t"`, i.e. the walk breaks on a SPACE (tabs are still skipped). A 513-marker line separated by single spaces stops counting at depth 1, so the pre-cap Error is not thrown and checkBody's degrade to the APPROVED_BLOCKING PRX-B-SIZE finding is lost. Tab-separated input still behaves identically, which is why the separator loop in the landed test must cover " " specifically. |
| 134 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `ch !== " "` becomes `ch !== ""`. `ch` iterates code points of a line, so it is never the empty string; the conjunct is a tautology and the condition collapses to `ch !== "\t"` — identical semantics to id 112: spaces now break the depth walk. A 513-marker space-separated line loses the pre-cap throw, so checkBody no longer degrades to PRX-B-SIZE (APPROVED_BLOCKING) and extractProse no longer returns "". |
| 134 | ConditionalExpression | `true` | Survived | survivor | BLOCKING_PATH | Right conjunct forced true: condition becomes `ch !== " " && true`, so the walk breaks on a TAB (spaces still skipped). A 513-marker tab-separated line (">\t" repeated) counts depth 1 instead of 513, the pre-cap Error is not thrown, and checkBody's PRX-B-SIZE degrade (APPROVED_BLOCKING) is lost. Space-separated input is unaffected, so only the "\t" arm of the landed separator test kills this one. |
| 134 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `ch !== "\t"` becomes `ch !== ""`, a tautology over single code points, collapsing the condition to `ch !== " "` — identical semantics to id 115: tabs now break the depth walk. A 513-marker tab-separated line no longer trips the pre-cap, so checkBody's degrade to the APPROVED_BLOCKING PRX-B-SIZE finding disappears. |
| 147 | MethodExpression | `top` | Survived | survivor | BLOCKING_PATH | Removes the comment filter, so `visible` becomes every top-level node including HTML comments. `visible` drives the capsule-position check at line 231 (`visible[0] !== capsule`): a body that opens with an HTML comment and then the identifier capsule is clean today, but under the mutant visible[0] is the comment node and checkBody emits PRX-B-CAPSULE 'visible content precedes the identifier capsule' — an APPROVED_BLOCKING error appearing where there was none. (PRX-B-VISIBLE does not diverge because meaningfulVisibleHtml strips comment content to "", which is the reason the comment-only tests do not kill this mutant — weak-oracle shape.) |
| 581 | ConditionalExpression | `true` | Survived | survivor | BLOCKING_PATH | NOT equivalent — a LIVE blocking survivor. Forcing the heading test true routes EVERY top-level visible node through `isMeaningfulText(visibleInlineText(n))` instead of `nodeHasVisibleContent(n)`. The two predicates disagree for footnoteDefinition (and only for it, among reachable top-level types): nodeHasVisibleContent has an explicit `case "footnoteDefinition": continue` (a footnote renders at the document bottom, not as body content), whereas visibleInlineText walks its children and returns their text. A PR body whose only node is a footnote definition therefore LOSES its PRX-B-VISIBLE finding — presence change on an APPROVED_BLOCKING deterministic rule, i.e. a body that renders nothing in-place would pass the visibility gate. bodyRendersMeaningfulText is consumed only at line 185, so the divergence is confined to PRX-B-VISIBLE. |
| 67 | Regex | `/[•‣▪◦]\s/m` | Survived | survivor | ADVISORY_PATH | UNICODE_BULLET_RE loses its `^` anchor (the /m flag stays). The only consumer is the PRX-B-STRUCTURE emit inside walk() at line 394. Un-anchored, a bullet that appears MID-LINE ("alpha • beta gamma") now matches and emits an extra PRX-B-STRUCTURE finding. PRX-B-STRUCTURE is registry class "advisory" (rules.mjs:53) -> level "advisory" and is absent from APPROVED_BLOCKING, so it can never change a verdict or exit code, and it feeds no parse decision. Divergence is real (not equivalent) but confined to an advisory rule. static:true (module-scope activation artifact). |
| 392 | EqualityOperator | `node.type !== "paragraph"` | Survived | survivor | ADVISORY_PATH | Inverts the node-type gate inside the walk() visitor, so the Unicode-bullet scan runs on every node EXCEPT paragraphs. walk() visits descendants too, so the bullet text is still reached through the paragraph's child text node — but at different node positions and possibly more than once. Observed divergence: for "line one \n• two\n" the finding's line moves 1 -> 2, and for "## • bullet head\n" the original emits nothing while the mutant emits two findings. The only rule emitted here is PRX-B-STRUCTURE, registry class "advisory" (rules.mjs:53) -> level "advisory", not in APPROVED_BLOCKING, and this branch feeds no parse decision or degrade path. So the divergence is real (not equivalent) but confined to an advisory rule's presence/count/line. |
| 407 | MethodExpression | `(node.children ?? []).every(c => c.type === "list")` | Survived | survivor | ADVISORY_PATH | some -> every on a blockquote's children changes the blockquoted-list predicate in two directions, both observable: (a) a blockquote holding a paragraph AND a list no longer matches (every is false), losing the finding; (b) an EMPTY blockquote matches vacuously (every on [] is true), gaining a finding. Both confirmed empirically. The only rule emitted is PRX-B-STRUCTURE, registry class "advisory" -> level "advisory", not in APPROVED_BLOCKING, feeding no parse or degrade decision — so the divergence is confined to the advisory layer. |
| 486 | StringLiteral | `""` | Survived | survivor | ADVISORY_PATH | NOT equivalent. `footnoteDefinition` is the one member of PROSE_EXCLUDED_ANCESTORS that (a) can hold paragraph children directly and (b) sits at TOP level, so no other set member re-asserts exclusion for its subtree (the only other such container is `blockquote`, whose mutant the suite already kills). Blanking it lets footnote body paragraphs leak into the designated narrative stream. Divergence is confined to extractProse's return value — checkBody's finding multiset is byte-identical on every probe — so the blast radius is the advisory Vale prose layer only (no blocking rule reads extractProse). |
| 490 | ConditionalExpression | `false` | Survived | survivor | ADVISORY_PATH | NOT equivalent. The mutated operand is the LEFT conjunct `typeof raw !== "string"` of extractProse's entry guard. With it forced false, a non-string argument falls into `exceedsByteLimit(raw, ...)` -> `value.length` / Buffer.byteLength and throws a TypeError instead of returning "" — the guard sits OUTSIDE the try/catch, so the throw escapes the exported function. Divergence is confined to extractProse (checkBody has its own independent guard at line 154-157 and is unaffected; every string input is identical), and the CLI wrapper check-body.mjs only ever passes readFileSync(..., "utf8") output, i.e. always a string, so no exit code changes. Advisory prose stream only. |
| 718 | MethodExpression | `[...nodes]` | Survived | survivor | ADVISORY_PATH | NOT equivalent. Dropping `.reverse()` from walk's stack seed makes the LIFO stack pop the top-level siblings in reverse document order, so the structure detector visits nodes back-to-front. The finding MULTISET is unchanged (walk has no early exit and still reaches every node, each finding carrying its own node's line), but the ORDER of entries in checkBody's returned array changes — an observable difference in the exported API's return value. walk's visitor emits only PRX-B-STRUCTURE (advisory: Unicode bullet, blockquoted list, raw HTML list), so no blocking rule, level, line or presence is affected and resolveVerdict is untouched (it filters, never orders). |
| 65 | StringLiteral | `""` | Survived | survivor | OFF_PATH | PLACEHOLDERS loses "TODO" (set becomes {"", "TBD", "FIXME", "XXX"}). The empty string can never be tested: `value === ""` is handled and `continue`d one branch earlier, so PLACEHOLDERS.has(value.toUpperCase()) only ever sees non-empty values. A capsule value of TODO/todo therefore takes the capsuleValueError branch instead of the placeholder branch. Both branches push EXACTLY ONE PRX-B-CAPSULE finding with the same rule, class "deterministic", level "error" and no line field, because no placeholder token satisfies any key grammar (SCOPE_RE=^TOS-[0-9]+$, RUN_ID_RE=^ONE-[0-9]{8}-[A-Z0-9]+$, SHA40_RE=^[0-9a-f]{40}$, evidenceRef sentinel is "UNKNOWN" and "TODO" is an unknown root segment). Only the MESSAGE text changes ('carries the placeholder "TODO"' -> 'does not match TOS-<number>' etc.), so this is message-text divergence = OFF_PATH, not equivalent. static:true (module-scope activation artifact); the existing suite kills only the TBD sibling (body-check.test.ts:431 asserts the TBD message), which is why TODO/FIXME/XXX survive. |
| 65 | StringLiteral | `""` | Survived | survivor | OFF_PATH | Same mechanism as id 33 for "FIXME". The value "FIXME"/"fixme" reroutes from the placeholder branch to capsuleValueError; both emit exactly one PRX-B-CAPSULE error finding with no line, for every one of the six keys, because FIXME matches none of SCOPE_RE / RUN_ID_RE / SHA40_RE / the evidence-ref grammar or its UNKNOWN sentinel. Divergence is confined to finding message text. |
| 65 | StringLiteral | `""` | Survived | survivor | OFF_PATH | Same mechanism as id 33 for "XXX". Rerouted to capsuleValueError; identical rule/class/level/count/line, different message text only. XXX satisfies no key grammar and is not the evidence sentinel. |
| 138 | StringLiteral | `''` | Survived | survivor | OFF_PATH | Blanks the template literal that becomes the pre-cap Error's message. parseBody IS exported, so the string is reachable and observable (the thrown Error's .message changes from 'blockquote nesting exceeds the structural pre-cap (512)' to ''), but both in-process consumers discard it: checkBody's catch (line 167) is a bare `catch {}` that pushes its own fixed PRX-B-SIZE message, and extractProse's catch returns "". No rule id, class, level, line, presence, verdict or exit code moves; the divergence is confined to diagnostic message text. Classified OFF_PATH (message-text divergence) rather than EQUIVALENT because the thrown message genuinely differs and an assertion on parseBody's throw message would distinguish them. |
| 146 | ArrayDeclaration | `["Stryker was here"]` | NoCoverage | survivor | OFF_PATH | Mutates the right operand of `tree.children ?? []`, which never evaluates: mdast-util-from-markdown always returns a root whose `children` is an own ARRAY property, even for degenerate input. Probed directly for "", "\n", " ", " " and "<!-- -->": Array.isArray(t.children) === true and hasOwnProperty('children') === true in every case. parseBody's only input is the raw string handed to checkBody/extractProse, so no hand-built tree can reach this line. Unreachable in-process from any exported entry point -> OFF_PATH (this is r1's row 'body-check.mjs:97 ArrayDeclaration NoCoverage — NO-COVERAGE (explained)', same mechanism at a shifted line; the r2 status is still NoCoverage). [carried r1: tree.children ?? []] |
| 407 | ArrayDeclaration | `["Stryker was here"]` | NoCoverage | survivor | OFF_PATH | Mutates the right operand of `node.children ?? []`, which is evaluated only when the short-circuited left conjunct `node.type === "blockquote"` already held AND `node.children` is nullish. mdast-util-from-markdown always materializes a children ARRAY on a blockquote, including the degenerate case (probed: parseBody(">") -> {type:"blockquote", children:[], position:{…}}), and walk() only ever sees parser-produced nodes, so the arm is unreachable in-process from any exported entry point — which is exactly why Stryker records NoCoverage. Doubly unobservable: even if it evaluated, ["Stryker was here"].some(c => c.type === "list") is false on a string element, identical to [].some(...). This is r1's row 'body-check.mjs:348 ArrayDeclaration NoCoverage — NO-COVERAGE (explained)' at a shifted line. [carried r1: (node.children ?? []).some(c => c.type === "list")] |
| 571 | ConditionalExpression | `default:` | NoCoverage | survivor | OFF_PATH | Unreachable code (matches the r1 no-coverage proof at r1 line 485, line-shifted to 571). capsuleValueError is module-private with exactly ONE call site (line 296), inside `for (const key of CAPSULE_KEYS)`; CAPSULE_KEYS is a frozen six-element tuple and every member has an explicit case label (Scope; Run-Id; Base/Head; Ledger/Evidence). The `default:` arm therefore cannot execute from any exported entry point, which is exactly why Stryker records NoCoverage. Defence in depth: even if it were reached, removing `return null` makes the function fall off the switch and return undefined, and the sole consumer tests plain truthiness (`if (grammarError)`), where undefined and null are indistinguishable — so the mutant could not change a finding even on a hypothetical reaching input. [carried r1: 485 \| ConditionalExpression \| `default:` \| NoCoverage \| NO-COVERAGE (explained)] |
| 631 | ArrayDeclaration | `["Stryker was here"]` | NoCoverage | survivor | OFF_PATH | Unreachable code (r1's line-523 no-coverage proof, line-shifted to 631). The mutated array is the RIGHT operand of `n.children ?? []`, evaluated only when `n.children` is nullish. That line executes only for the container case labels list/listItem/table/tableRow/blockquote, and mdast-util-from-markdown materialises a children ARRAY on every one of those node types even in degenerate form (probed: a bare `-` list item, `\| \|` empty table row, and bare `>` blockquote all carry children arrays). The ?? arm therefore never evaluates from any exported entry point, which is why Stryker records NoCoverage. Had it been reachable the sentinel string would be pushed as a pseudo-node and could flip PRX-B-SECTION-EMPTY / PRX-B-VISIBLE — hence OFF_PATH by unreachability, not by harmlessness. [carried r1: 523 \| ArrayDeclaration \| `["Stryker was here"]` \| NoCoverage \| NO-COVERAGE (explained)] |
| 651 | BlockStatement | `{}` | NoCoverage | survivor | OFF_PATH | Unreachable code — the image branch body of visibleInlineText's top-level dispatch. Reachability argument: the only top-level callers are bodyRendersMeaningfulText (headings only) and nodeHasVisibleContent's default arm, whose stack contains only the initial top-level visible node and children of list/listItem/table/tableRow/blockquote (all block-level); image/imageReference are PhrasingContent and never appear in either position. The instrumented run over 4081 documents records the complete arriving-type set as {heading, html, paragraph, tableCell}, and Stryker itself recorded NoCoverage here. Note the mutant is NOT harmless if it ever were reached: with the body emptied, an image would fall through to `if (!node.children) return ""`, silently dropping alt text that the original counts as rendered content — so this is dead-code OFF_PATH, not equivalence. |
| 652 | LogicalOperator | `node.alt && ""` | NoCoverage | survivor | OFF_PATH | Doubly unreachable. (1) The statement sits inside the image branch of visibleInlineText's top-level dispatch, which no parser-produced node ever enters (only {heading, html, paragraph, tableCell} arrive; images are phrasing content handled at line 685) — Stryker records NoCoverage for the whole block. (2) Even inside it, `?? -> &&` could only diverge for a nullish `alt`, and mdast-util-from-markdown always sets `alt` to a STRING for both image and imageReference (r1's line-533 proof; re-probed here). Because `node.alt && ""` returns "" for every non-empty alt, this mutant WOULD drop alt text if the site were live, so the disposition rests on unreachability, not harmlessness. [carried r1: 533 \| StringLiteral \| `"Stryker was here!"` \| NoCoverage \| NO-COVERAGE (explained)] |
| 652 | StringLiteral | `"Stryker was here!"` | NoCoverage | survivor | OFF_PATH | Unreachable on two independent counts, exactly r1's line-533 mechanism: the enclosing image branch is never entered from any exported entry point (arriving node types at this dispatch are {heading, html, paragraph, tableCell} over 4081 instrumented documents), and the mutated literal is the RIGHT operand of `node.alt ?? ""`, which evaluates only when alt is nullish — mdast always sets alt to a string (probed for image and imageReference, empty and non-empty labels). Sentinel text can therefore never be returned. [carried r1: 533 \| StringLiteral \| `"Stryker was here!"` \| NoCoverage \| NO-COVERAGE (explained)] |
| 659 | StringLiteral | `"Stryker was here!"` | NoCoverage | survivor | OFF_PATH | Unreachable return value. The literal is the return of the `!node.children` guard at line 659, and that guard never fires from any exported entry point: the only node types reaching it are heading, paragraph and tableCell (image/imageReference return at 651, html at 655, value-bearing nodes at 658), and every one of them carries a children array even when empty (probed on degenerate '## ', '#', and empty table cells; instrumented run over 4081 documents reports kids=true for all three). Stryker's NoCoverage status for this literal is the same signal. If it were reachable the sentinel would be counted as meaningful rendered text and could suppress PRX-B-SECTION-EMPTY / PRX-B-VISIBLE, so the disposition is dead-code OFF_PATH, not equivalence. |
| 686 | StringLiteral | `"Stryker was here!"` | NoCoverage | survivor | OFF_PATH | Dead nullish arm — r1's line-533 mechanism at the frames-walk image site. Line 686 (`out += child.alt ?? ""`) IS reachable (images inside paragraphs, headings and table cells run through it), but the mutated literal is the RIGHT operand of `??`, evaluated only when `child.alt` is nullish. mdast-util-from-markdown always assigns `alt` a STRING for both image and imageReference — re-probed here on '![](x.png)', '![a](x.png)', '![][r]', '![a][r]', '![a]', '[![](x.png)](l)': typeof alt === "string" in every case ("" for alt-less forms) — so the operand can never be evaluated in-process, which is exactly why Stryker records NoCoverage. Were it reachable, injecting the sentinel would make an alt-less image count as rendered content and could suppress a blocking PRX-B-SECTION-EMPTY/PRX-B-VISIBLE finding; the disposition rests on unreachability. [carried r1: 533 \| StringLiteral \| `"Stryker was here!"` \| NoCoverage \| NO-COVERAGE (explained)] |
| 211 | OptionalChaining | `c.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal on `c.position?.start.line`. The optional chain short-circuits only when `c.position` is null/undefined; otherwise `c.position?.start` and `c.position.start` evaluate identically. Invariant relied on: `c` ranges exclusively over `comments = top.filter(isCommentNode)`, i.e. nodes produced by mdast-util-from-markdown, and the compiler stamps a `position` object on every node it emits. The invariant holds because parseBody's only source of nodes is fromMarkdown(raw, …) — checkBody never accepts or constructs nodes, so no hand-built positionless node can reach this expression. Verified by walking every node of every parsed corpus input: 0 nodes without position. Carries r1's proof at body-check.mjs:152 verbatim. [carried r1: c.position?.start.line] |
| 239 | OptionalChaining | `capsule.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal on `capsule.position?.start.line`. Divergence requires `capsule.position` to be nullish, in which case the original yields undefined (finding emitted without a `line` key) and the mutant throws TypeError. `capsule` is `capsules[0]` where `capsules = visible.filter(isCapsuleCandidate)` and `visible` derives from `tree.children` — parser-produced code nodes only, all of which carry position (probed: 0 positionless nodes across the whole corpus). The guard `capsules.length >= 1` ensures capsule is a node, not undefined. Carries r1's proof at body-check.mjs:180. [carried r1: capsule.position?.start.line] |
| 305 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression forcing the left conjunct of `n.type === "heading" && n.depth === 2` to true, so the heading filter becomes `n.depth === 2`. Logical implication: for every node the mutant's predicate equals `n.depth === 2`; for heading nodes that is exactly the original right operand (the left was already true), and for every non-heading node `depth` is absent, so `undefined === 2` is false and the node still fails the filter — the same result the original produced via the left conjunct. Invariant relied on: in mdast (plus the GFM extension) `depth` exists ONLY on heading nodes; verified by scanning every node of every corpus parse for a `depth` property on a non-heading (0 hits) across the node types actually produced. The filter also runs only over `visible` (top-level block nodes), a strict subset of that scan. Carries r1's proof at body-check.mjs:246. [carried r1: n.type === "heading" && n.depth === 2] |
| 309 | OptionalChaining | `n.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal on `line: n.position?.start.line` inside the headings map. `n` is a depth-2 heading already filtered out of `visible`, i.e. a node emitted by mdast-util-from-markdown, and every such node carries a `position` object (invariant verified by scanning all nodes of every corpus parse: 0 without position; parseBody's sole node source is fromMarkdown). With position always present the optional chain never short-circuits, so the computed `line` — which feeds PRX-B-SECTION-DUP, PRX-B-SECTION-EMPTY and PRX-B-EXT — is byte-identical. Carries r1's proof at body-check.mjs:250. [carried r1: n.position?.start.line] |
| 357 | EqualityOperator | `i <= orderIndexes.length` | Survived | survivor | EQUIVALENT | Operator: EqualityOperator/relational boundary change on the PRX-B-ORDER loop bound `i < orderIndexes.length` -> `i <= orderIndexes.length`. The mutant executes exactly one extra iteration, with i === orderIndexes.length. In it the body evaluates `orderIndexes[i] < orderIndexes[i - 1]` = `undefined < <number>`; JS relational comparison on undefined coerces to NaN, and every NaN comparison is false, so the extra iteration can never push a finding and cannot break differently. No other statement in the loop body has a side effect, and the loop is the last use of `i`. Length edge cases: length 0 -> `1 <= 0` false, no iteration either way; length 1 -> the single extra iteration compares undefined < orderIndexes[0], false. Therefore the emitted finding multiset is identical on every input. (The rule involved, PRX-B-ORDER, is advisory anyway, so even a live divergence could not reach a verdict — but none exists.) |
| 373 | MethodExpression | `EXTENSION_ALLOWLIST_PREFIXES.every(p => h.text.startsWith(p…` | Survived | survivor | EQUIVALENT | Operator: MethodExpression some -> every on the receiver EXTENSION_ALLOWLIST_PREFIXES. Logical implication: for an array of length exactly 1, some(f) and every(f) both reduce to f(a[0]); they can only diverge at length 0 (some=false, every=true) or length >= 2. The receiver is the module-level `Object.freeze(["Evidence record"])` at line 54 — length 1, frozen, not exported, and never assigned to or mutated anywhere in the module (it is read only at this call site), so neither divergent length is reachable. Carries r1's proof at body-check.mjs:314. [carried r1: EXTENSION_ALLOWLIST_PREFIXES.some(p =>] |
| 400 | OptionalChaining | `node.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal at the PRX-B-STRUCTURE Unicode-bullet emit site. Divergence requires `node.position` nullish. `node` here is supplied by walk(visible, …), which pushes only `visible` (a filtered slice of tree.children) and their `children` — i.e. nodes emitted by mdast-util-from-markdown, all of which carry position (verified: 0 positionless nodes across every node of every corpus parse). No exported entry point can inject a hand-built node into walk. Carries r1's proof at body-check.mjs:341 (the row explicitly covers the Unicode-bullet, blockquoted-list, raw-HTML-list and FENCE emit sites identically). [carried r1: node.position?.start.line] |
| 414 | OptionalChaining | `node.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal at the PRX-B-STRUCTURE blockquoted-list emit site. Same invariant as id 336: `node` comes from walk(visible, …) and is therefore always a mdast-util-from-markdown-produced node carrying a `position` object (0 positionless nodes found across every node of every corpus parse), so the chain never short-circuits and the emitted `line` is identical. Carries r1's proof at body-check.mjs:355. [carried r1: node.position?.start.line] |
| 428 | OptionalChaining | `node.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal at the PRX-B-STRUCTURE raw-HTML-list emit site. Same invariant as ids 336/354: every node reaching walk()'s visitor is parser-produced and carries `position` (verified across all corpus parses), so `node.position?.start` and `node.position.start` evaluate identically and the emitted `line` is unchanged. Carries r1's proof at body-check.mjs:369. [carried r1: node.position?.start.line] |
| 458 | OptionalChaining | `node.position.start` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removal at the PRX-B-FENCE emit site. Here `node` iterates `visible` directly — the filtered `tree.children` array — so it is unconditionally a mdast-util-from-markdown-produced code node, and every such node carries a `position` object (0 positionless nodes across every node of every corpus parse; parseBody's only node source is fromMarkdown over the raw string). The optional chain therefore never short-circuits. Carries r1's proof at body-check.mjs:399 (the row names the FENCE site explicitly). [carried r1: node.position?.start.line] |
| 476 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "list" blanked. The set is consumed at exactly one site (extractProse line 523, `PROSE_EXCLUDED_ANCESTORS.has(node.type)`), and `excludedHere` is consumed at only two sites: the `out.push` for paragraphs and the `excluded` field of a pushed child frame. A `list` node is never a paragraph, so the first site is unaffected; the second matters only for its descendants, and in mdast a list's children are EXCLUSIVELY listItem nodes, which are themselves still in the set (line 477) and re-assert exclusion for the whole subtree. No node type has type "" so the blanked member matches nothing. Therefore no paragraph's inclusion decision can change. Empirically corroborated: the invariant probe over 4081 documents shows paragraph parents are exactly {root, blockquote, listItem, footnoteDefinition} — `list` is never a paragraph's parent. Divergence would require a paragraph as a DIRECT child of a list, which mdast/mdast-util-gfm cannot produce. |
| 477 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "listItem" blanked. A listItem node is never a paragraph (so `out.push` is unaffected) and can never be a top-level `visible` node — in mdast a listItem exists only as a child of a `list`, which is still in the set. extractProse propagates exclusion downward via `frame.excluded \|\| has(node.type)`, so the parent list already sets excluded=true for the frame containing the listItem, and `excludedHere` stays true regardless of this literal. Divergence would require a listItem whose ancestor chain contains no excluded node, which the parser cannot produce. "" matches no node type. |
| 478 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "table" blanked. Two independent reasons no paragraph decision can change: (1) a GFM table's children are exclusively tableRow nodes, which remain in the set (line 479), so exclusion is re-asserted one level down; (2) the deepest table descendant is a tableCell whose children are PHRASING content only — a paragraph can never occur inside a table at all (confirmed by the invariant probe: paragraph parents over 4081 docs are exactly {root, blockquote, listItem, footnoteDefinition}). A table node is itself never a paragraph, so the `out.push` site is unaffected. |
| 479 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "tableRow" blanked. A tableRow occurs only as a child of a `table`, which is still in the set, so `frame.excluded` is already true when the row is visited; and a tableRow's subtree (tableCell -> phrasing) contains no paragraph node at all. Neither the `out.push` site (a tableRow is not a paragraph) nor the child-frame `excluded` field can produce a different prose result. |
| 480 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "tableCell" blanked. A tableCell occurs only under tableRow/table, both still in the set, so exclusion is inherited via `frame.excluded`; independently, tableCell children are phrasing content, so no paragraph exists in that subtree to push. Both consumers of `excludedHere` are therefore unchanged. |
| 482 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "code" blanked. `excludedHere` is read only by (a) `if (node.type === "paragraph" && !excludedHere)` and (b) `if (node.children) frames.push({... excluded: excludedHere ...})`. An mdast `code` node is not a paragraph and carries NO children property at all (it is a leaf with `value`/`lang`), so neither consumer executes for it and the computed flag is discarded. The literal therefore cannot affect any paragraph's inclusion. |
| 483 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "html" blanked. An mdast `html` node is a leaf (value only, no children) and is not a paragraph, so the computed `excludedHere` is never read for it. Critically, the BYP-B-04 raw-container mechanism does not depend on this literal: the tracker call `if (node.type === "html") frame.tracker.feed(node.value)` and the sibling test `else if (frame.tracker.isInside()) excludedHere = true` are separate, untouched statements, so raw <table>/<details> containers still exclude their sibling paragraphs. |
| 484 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "heading" blanked. A heading is not a paragraph, so the `out.push` site is unaffected; its children are PHRASING content (text/emphasis/inline html/image/link), which can never contain a paragraph node, so the child frame's `excluded` value cannot change any push either. Invariant probe over 4081 documents: paragraph parents are exactly {root, blockquote, listItem, footnoteDefinition} — never heading. |
| 485 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | PROSE_EXCLUDED_ANCESTORS member "definition" blanked. An mdast link-reference `definition` node is a leaf (identifier/label/url/title, no children, no value) and is not a paragraph, so neither consumer of `excludedHere` executes for it; the flag is computed and discarded. |
| 532 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | `if (node.children)` in extractProse forced false: child frames are never pushed, so the traversal visits only TOP-LEVEL visible nodes. This cannot change the output because the only value produced by the traversal is `out.push(inlineText(node))` for NON-excluded paragraphs, and in mdast+gfm a paragraph's parent can only be root, blockquote, listItem, or footnoteDefinition (empirically confirmed over 4081 documents by the invariant probe; structurally, Paragraph is FlowContent and only Root/Blockquote/ListItem/FootnoteDefinition accept FlowContent). Every non-root member of that list is in PROSE_EXCLUDED_ANCESTORS, so every paragraph reachable ONLY by descending is already excluded and contributes nothing. The descent also has no effect on the top-level tracker: `frame.tracker` is per-sibling-list and a nested html node feeds only its own frame's tracker. |
| 532 | BlockStatement | `{}` | Survived | survivor | EQUIVALENT | Same site as id 450 with the operator emptying the block body instead of the test: the block's ONLY statement is the `frames.push({...})`, so an emptied block and a false test are behaviourally identical (the test is side-effect-free). The equivalence proof of id 450 applies verbatim: every paragraph reachable only by descent has an ancestor in PROSE_EXCLUDED_ANCESTORS (paragraph parents ⊆ {root, blockquote, listItem, footnoteDefinition}) and therefore never reaches `out`. |
| 600 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Left conjunct of `n.type === "heading" && n.depth <= 2` forced true, leaving `n.depth <= 2` as the section-terminator test in hasVisibleSectionContent. In mdast the `depth` property exists ONLY on heading nodes; for every other node type the expression evaluates `undefined <= 2`, which coerces to NaN <= 2 === false — exactly the original result for a non-heading. For headings the mutated condition evaluates precisely the original right operand. The section scan therefore terminates on exactly the same nodes, so no PRX-B-SECTION-EMPTY finding, line, or presence can change. Same equivalence class as r1's line-499 row, line-shifted to 600. [carried r1: 499 \| ConditionalExpression \| `true` \| Survived \| EQUIVALENT] |
| 617 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The `case "thematicBreak":` label is blanked, so a thematicBreak node reroutes to `default:` in nodeHasVisibleContent. There it evaluates `isMeaningfulText(visibleInlineText(n))`: a thematicBreak is not image/imageReference, not html, has no `value` and no `children`, so visibleInlineText returns "" at its `if (!node.children) return ""` arm, and isMeaningfulText("") is false -> `continue` — identical to the original shared case body. No node type equals "", so nothing else reroutes into the blanked label. [carried r1: 508 \| StringLiteral \| `""` \| Survived \| EQUIVALENT] |
| 618 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The `case "definition":` label is blanked, rerouting link-reference definitions to `default:`. A definition node carries identifier/label/url/title but neither `value` nor `children`, so visibleInlineText returns "" and isMeaningfulText("") is false -> `continue`, exactly the original case result. Link definitions still count as no rendered content, so PRX-B-SECTION-EMPTY and PRX-B-VISIBLE are unchanged. [carried r1: 509 \| StringLiteral \| `""` \| Survived \| EQUIVALENT] |
| 619 | ConditionalExpression | `// link-reference definitions never render\ncase "footnoteD…` | Survived | survivor | EQUIVALENT | The operator deletes the shared `continue;` consequent at line 620, so ALL FOUR labels that share it (heading, thematicBreak, definition, footnoteDefinition) fall through into `case "code":` -> `if (isMeaningfulText(n.value)) return true; continue;`. None of those four mdast node types has a `value` property (heading/footnoteDefinition are parents with `children`; thematicBreak/definition are property-only leaves), so `n.value` is undefined and isMeaningfulText(undefined) returns false at its `typeof value === "string"` guard — every one of the four still reaches `continue`. Same case-fallthrough mechanism r1 proved at line 512 (its fallthrough computed the removed body's exact semantics; here it computes a uniformly false test). [carried r1: 512 \| ConditionalExpression \| `case "code":` \| Survived \| EQUIVALENT] |
| 621 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The `case "code":` label is blanked, rerouting code nodes to `default:`, which computes `isMeaningfulText(visibleInlineText(n))`. For a code node visibleInlineText is not image/imageReference and not html, so it hits `if (node.value !== undefined) return node.value` and returns the fence content verbatim — making the default arm compute isMeaningfulText(n.value), the removed case body's exact expression. Empty/zero-width-only fences still count as no content; populated fences (evidence YAML, capsule) still count. [carried r1: 512 \| StringLiteral \| `""` \| Survived \| EQUIVALENT] |
| 628 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The `case "table":` label is blanked, so a table node reroutes to `default:` -> isMeaningfulText(visibleInlineText(table)), i.e. the CONCATENATED text of every row and cell, instead of the original existential push of rows onto the stack. The two agree: concat has a meaningful (non-White_Space, non-Cf) code point iff at least one cell's text does, which is exactly the original `some` semantics. GFM cells hold inline content only, so both paths reach the same leaves, and visibleInlineText allocates a fresh sanitized-text scanner per sibling list (one per cell's child list either way), so removed-element (script/style) suppression cannot differ. [carried r1: 520 \| StringLiteral \| `""` \| Survived \| EQUIVALENT] |
| 629 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The `case "tableRow":` label is blanked. tableRow nodes reach nodeHasVisibleContent's stack only via the table case's children push; rerouted to `default:` they evaluate isMeaningfulText over the row's concatenated cell text, which is true iff some cell contributes a meaningful code point — identical to the original existential descent into the row's cells (same inline-only-cells argument as id 519, with a fresh scanner per cell child list on both paths). [carried r1: 521 \| StringLiteral \| `""` \| Survived \| EQUIVALENT] |
| 651 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | The whole image test in visibleInlineText is forced false — a no-op because the condition is ALREADY false on every reachable input. visibleInlineText's top-level callers are exactly two: bodyRendersMeaningfulText, which calls it only for `n.type === "heading"`, and nodeHasVisibleContent's `default:` arm, whose stack holds the initial top-level visible node plus children of list/listItem/table/tableRow/blockquote. In mdast+gfm those children are listItem, FlowContent, tableRow, tableCell — all block-level; image and imageReference are PhrasingContent and can never appear there. Empirically: an instrumented copy over 4081 documents records the complete set of node types reaching this line as {heading, html, paragraph, tableCell}. Images inside paragraphs are handled at the SEPARATE frames-walk site (line 685), which this mutant does not touch. Stryker's own NoCoverage verdict on the block body (ids 540/541/542) independently confirms the branch is never taken across the whole suite. |
| 651 | LogicalOperator | `node.type === "image" && node.type === "imageReference"` | Survived | survivor | EQUIVALENT | \|\| replaced by &&. `node.type === "image" && node.type === "imageReference"` is a logical contradiction — a single string cannot equal two distinct literals — so the mutated condition is UNCONDITIONALLY false. The original condition is also always false on every reachable input (no image/imageReference node ever reaches visibleInlineText's top-level call; see the id-532 proof and the 4081-document invariant probe). Both therefore evaluate false everywhere and the branch body is never entered either way. |
| 651 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | Left operand `node.type === "image"` forced false. The left operand is already false for every node that reaches this line ({heading, html, paragraph, tableCell}, empirically over 4081 documents; structurally because images are PhrasingContent and this line only sees block-level nodes or headings), so `false \|\| node.type === "imageReference"` evaluates exactly as the original — and the right operand is likewise never true, so the branch stays unentered. |
| 651 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The "image" literal blanked to "". No mdast node has type "", so the comparison is false for every node — and it was already false for every node reaching this line (only heading/html/paragraph/tableCell arrive there; images are phrasing-only and are handled at the separate frames-walk site line 685). Both versions leave the branch unentered on every reachable input. |
| 651 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | Right operand `node.type === "imageReference"` forced false. imageReference is PhrasingContent and can only occur inside a paragraph/heading's inline children, which visibleInlineText handles in its frames walk (line 685), never at this top-level dispatch; the invariant probe over 4081 documents shows only {heading, html, paragraph, tableCell} arrive here. The right operand is therefore already false everywhere, and the left operand (`=== "image"`) is unchanged, so the whole condition evaluates identically. |
| 651 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | The "imageReference" literal blanked to "": no node type equals "", so the comparison is false for all nodes, and it was already false for every node reaching this line (imageReference is phrasing content, handled at line 685 instead). The branch body stays unentered under both versions on every reachable input. |
| 658 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | `if (node.value !== undefined) return node.value;` forced false. The guard is evaluated (Stryker: Survived, not NoCoverage) but is NEVER true on a reachable input: by the time control reaches line 658 the node is not image/imageReference (651) and not html (654), leaving exactly {heading, paragraph, tableCell} — verified over 4081 documents by the instrumented run, which reports val=false for all three — and none of those mdast types carries a `value` property. The value-bearing types that do (text/inlineCode/code/html) either never reach this top-level call (code is consumed by nodeHasVisibleContent's own `case "code"`; text/inlineCode are phrasing handled in the frames walk at line 689) or return earlier at 655. With the guard already false everywhere, forcing it false changes nothing. |
| 659 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | `if (!node.children) return "";` forced false. The guard is never true on a reachable input: nodes arriving at line 659 are exactly {heading, paragraph, tableCell} (images/imageReferences return at 651, html at 655, value-bearers at 658 — none of which occurs), and mdast materialises a children ARRAY on all three even in degenerate form ([] is truthy). Probed directly: '## ' , '##', '#' produce headings with Array.isArray(children)===true, length 0; '\| \| \|' produces tableCells with children===[]. Since the guard never fires in the original, disabling it is unobservable. (Were it reachable the mutant would throw a TypeError on `frame.nodes.length`, so the claim rests on the empirically pinned invariant, which the instrumented 4081-document run reports as kids=true for every arriving type.) |
| 705 | MethodExpression | `value.split("\n")` | Survived | survivor | EQUIVALENT | The blank-line filter is removed from looksNarrative, so whitespace-only lines are retained. Those lines contribute to neither counter: the listish regex /^\s*([-*+]\|\d+[.)])\s/ requires a bullet or ordinal AFTER the leading whitespace, and the sentenceish predicate tests the TRIMMED line against /[.!?]\s*$/, which cannot match "". The only other consumer is the `lines.length === 0` early return, and the two paths coincide there: with the filter an all-blank value yields [] -> return false; without it String.split always yields >= 1 element, so control falls through to `0 > 0 \|\| 0 >= 1` -> false. Identical return on every input. Blast radius would in any case be PRX-B-FENCE (advisory) only. [carried r1: 544 \| MethodExpression \| `value.split("\n")` \| Survived \| EQUIVALENT] |
| 705 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | The filter predicate is forced true, retaining every line — behaviourally identical to removing the filter, so the id-600 proof applies verbatim: whitespace-only lines match neither the listish regex (bullet/ordinal required after ^\s*) nor the sentenceish predicate (which trims first), and the `lines.length === 0` fast path coincides with the fallthrough result (0 > 0 \|\| 0 >= 1 === false). [carried r1: 544 \| ConditionalExpression \| `true` \| Survived \| EQUIVALENT] |
| 705 | MethodExpression | `l` | Survived | survivor | EQUIVALENT | `l.trim() !== ""` becomes `l !== ""`, so whitespace-only (but non-empty) lines are additionally retained. Those lines contribute 0 to listish (no bullet/ordinal after the leading whitespace) and 0 to sentenceish (trimmed to "", which fails /[.!?]\s*$/), so both counters are unchanged. The all-blank case still returns false either through the length-0 fast path or through the identical `0 > 0 \|\| 0 >= 1` fallthrough. [carried r1: 544 \| MethodExpression \| `l` \| Survived \| EQUIVALENT] |
| 705 | StringLiteral | `"Stryker was here!"` | Survived | survivor | EQUIVALENT | The filter comparand becomes the sentinel, so blank lines are now RETAINED and lines whose trim equals "Stryker was here!" are dropped. Retained blanks contribute 0 to both counters (id-600 proof). A dropped sentinel line would itself have contributed 0 under the original: it carries no bullet/ordinal (listish 0) and, although it ends in "!", it splits into exactly 3 whitespace-separated words, failing the `>= 4` conjunct (sentenceish 0). And when the sentinel is the ONLY line, the mutant returns false via the length-0 fast path while the original returns false via `0 > 0 \|\| 0 >= 1`. Identical on every input, including the pathological one. [carried r1: 544 \| StringLiteral \| `"Stryker was here!"` \| Survived \| EQUIVALENT] |
| 706 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | `if (lines.length === 0) return false;` is bypassed. This is a pure fast path: when lines is empty both `filter` calls yield empty arrays, so listish = 0 and sentenceish = 0, and the final expression `0 > 0 \|\| 0 >= 1` evaluates to false — precisely the value the guard returned. No other statement between the guard and the return can observe the difference (no side effects, no throw: .filter on [] is total). [carried r1: 545 \| ConditionalExpression \| `false` \| Survived \| EQUIVALENT] |
| 709 | MethodExpression | `l` | Survived | survivor | EQUIVALENT | The first `l.trim()` in the sentenceish predicate becomes `l`, i.e. /[.!?]\s*$/.test(l) instead of .test(l.trim()). The regex is unanchored at the start, so leading whitespace is irrelevant, and its `\s*$` tail absorbs exactly the trailing whitespace that trim would have removed (no `m` flag, so `$` is end-of-string and lines never contain \n). Hence test(l) === test(l.trim()) for every string, including CR-terminated lines. The second conjunct `l.trim().split(/\s+/).length >= 4` is untouched, so the word count is unchanged. [carried r1: 548 \| MethodExpression \| `l` \| Survived \| EQUIVALENT] |

#### `scripts/prx/check-commit.mjs`

| Line | Mutator | Replacement | Run-2 | Replay | Class | Disposition / proof |
| --- | --- | --- | --- | --- | --- | --- |
| 49 | ConditionalExpression | `false` | Survived | survivor | BLOCKING_PATH | NOT equivalent — and this REFUTES the r1 row (check-commit.mjs line 43, 'NO-COVERAGE (explained): the guard's trigger cannot be produced by any commit object'). The r1 proof only considered the COMMIT-OBJECT channel; it missed the ARGUMENT channel. listCommits(repoDir, range) is exported and passes `range` straight to `git log -z --format=... <range>` with no validation of its own, so a caller-supplied `range` of "--format=%H%n%P" (or --pretty=...) overrides the earlier --format and git emits records with 1-2 newline-separated fields. Original: throws Error('malformed git log record (2 fields); refusing to audit a stream this parser cannot account for') — fail closed. Mutant: guard never fires, the record is destructured anyway (message = slice(4).join("\n") = "") and listCommits returns 4 bogus commits, which downstream produce APPROVED_BLOCKING findings PRX-C-SUBJECT and PRX-C-PREFIX (level error) that the real code never produces; with a 1-field stream the mutant instead throws a TypeError from parents.trim(). So the mutant changes both the parse decision feeding the blocking rules and the presence/rule-id/level of blocking findings. (I separately confirmed the r1 commit-object argument itself: git 2.55 refuses NUL in commit objects via fsck and, when forced with `hash-object --literally`, truncates %B at the NUL and blanks NUL-bearing %an/%ae, so no git-authored commit yields a <5-field record — the vector is the range argument, not the commit.) |
| 49 | BlockStatement | `{}` | NoCoverage | survivor | BLOCKING_PATH | Same mechanism as id 23 with the guard intact but its throw removed: the emptied block makes the malformed-record path fall through instead of failing closed. Reached by the same exported-API input (listCommits(repo, '--format=%H%n%P')), so it is NOT dead code and the r1 'unreachable defensive block' row (line 43 BlockStatement {}) is refuted. Divergence: original throws Error and audits nothing; mutant returns 4 fabricated commit records whose empty messages yield APPROVED_BLOCKING PRX-C-SUBJECT and PRX-C-PREFIX errors (or a TypeError on a 1-field stream). Presence, rule id and level of blocking findings all change. |
| 83 | MethodExpression | `process.argv` | NoCoverage | killed | BLOCKING_PATH | Not equivalent: the default parameter feeds main()'s whole argument parser. When the module is executed as a script (the ONLY caller that omits argv — the module-entry guard calls main() with no arguments), the mutant hands main() the full process.argv, so argv[0] (the node binary path) is consumed as the input file and argv[1] (the script path) trips the 'unexpected argument' branch: every real CLI invocation returns exit 2 with no findings, instead of the true verdict exit code (0/1). That is an exit-code and finding-presence change on the blocking path. Activation artifact (rule 4): the covering tests spawn a subprocess, so Stryker records NoCoverage even though the ground-truth replay shows the suite kills it. [carried r1: check-commit.mjs line 77 \| MethodExpression \| `process.argv` — "ARTIFACT (replay-killed) ... covering tests execute the mutated file in a SPAWNED subprocess,…] |
| 101 | ConditionalExpression | `false` | Survived | survivor | BLOCKING_PATH | New r2 code (the --verified-revert trusted-caller flag), no r1 row. Forcing the else-if condition to false removes the flag branch, so '--verified-revert' falls through the remaining branches and lands in the input/unexpected-argument arm: with a file already supplied it returns exit 2 ('unexpected argument: --verified-revert') and emits NO findings at all, and even alone it would be swallowed as the input path. The trusted revert exemption therefore can never be granted, and a verified revert commit that legitimately exits 0 now exits 2. Changes exit code and the presence of APPROVED_BLOCKING findings (PRX-C-PREFIX). |
| 101 | BooleanLiteral | `false` | NoCoverage | survivor | BLOCKING_PATH | New r2 code, no r1 row; recorded NoCoverage because no in-process test passes --verified-revert today. The flag parses but stores false, so opts.verifiedRevert === true never holds and the trusted-caller revert exemption is silently dead: a verified revert commit gains an APPROVED_BLOCKING PRX-C-PREFIX error (level error, line 1) plus the PRX-C-CONTEXT-UNVERIFIED advisory, and the verdict flips 0 -> 1. This is the highest-value survivor in the group: it silently disables a trusted-caller exemption while the CLI still accepts the flag. |
| 139 | ConditionalExpression | `false` | Survived | killed | BLOCKING_PATH | Not equivalent: forcing the stdin discriminator to false routes `-` down the file branch, so readFileSync(resolve("-")) throws ENOENT and main() returns exit 2 from the catch with zero findings, instead of reading stdin and reporting the real verdict. Every stdin-mode invocation (the mode CI uses for commit-msg hooks) loses all APPROVED_BLOCKING findings and changes exit code 1 -> 2. Activation artifact (rule 4): the covering tests spawn a subprocess, so Stryker sees a survivor although the ground-truth replay shows the real suite kills it. [carried r1: check-commit.mjs line 125 \| ConditionalExpression \| `false` — "ARTIFACT (replay-killed) ... covering tests execute the mutated file in a SPAWNED subprocess"] |
| 139 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Not equivalent: blanking the stdin sentinel makes the test `input === ""`, which the argument parser can never satisfy from any tested surface, so `-` takes the file branch exactly as in id 141 — readFileSync(resolve("-")) -> ENOENT -> exit 2, all APPROVED_BLOCKING findings for stdin input lost. Same subprocess activation artifact. [carried r1: check-commit.mjs line 125 \| StringLiteral \| `""` — "ARTIFACT (replay-killed) ... SPAWNED subprocess ... activation does not cross the process boundary"] |
| 140 | StringLiteral | `""` | NoCoverage | killed | BLOCKING_PATH | Not equivalent, and NOT the same benign Buffer-coercion as id 2: dropping the encoding makes readFileSync(0) return a Buffer, which is handed to checkCommit as `raw`. checkCommit's type/size guard rejects a non-string and degrades to a single PRX-C-SIZE error ('commit message exceeds the 1 MiB byte bound', APPROVED_BLOCKING, no line), REPLACING the real structural findings. Rule ids, count and lines of blocking findings all change on every stdin invocation — the exact hazard cli.test already documents for the body CLI ('a Buffer would trip the size/type guard'). Subprocess activation artifact as with ids 141/143. [carried r1: check-commit.mjs line 126 \| StringLiteral \| `""` — "ARTIFACT (replay-killed) ... SPAWNED subprocess ... activation does not cross the process boundary"] |
| 191 | ConditionalExpression | `false` | Survived | killed | BLOCKING_PATH | Not equivalent: the module-entry guard decides whether the CLI does anything at all when executed as a script. Forced false, main() is never invoked: the process prints nothing and exits 0 on every invocation, so all APPROVED_BLOCKING findings vanish and the enforcing verdict (exit 1) and usage errors (exit 2) both collapse to 0 — a total gate bypass in the real artifact. Activation artifact (rule 4): the mutant is module-scope (Stryker marks it static) AND only observable across a process boundary, so neither in-worker activation nor in-process tests can see it; the ground-truth replay confirms the real suite kills it via the spawnSync tests. [carried r1: check-commit.mjs line 176 \| ConditionalExpression \| `false` — "ARTIFACT (replay-killed) ... covering tests execute the mutated file in a SPAWNED subprocess"] |
| 193 | BlockStatement | `{}` | NoCoverage | killed | BLOCKING_PATH | Not equivalent: emptying the module-entry body removes `process.exitCode = main()`, so running the CLI as a script performs no check and always exits 0 — the same total gate bypass as id 180 (guard evaluates, body does nothing). Every APPROVED_BLOCKING finding disappears from the real artifact's output and enforcing/usage exit codes (1/2) become 0. NoCoverage is the expected artifact: the statement executes only in a spawned process, which Stryker cannot instrument from the worker. [carried r1: check-commit.mjs line 178 \| BlockStatement \| `{}` — "ARTIFACT (replay-killed) ... SPAWNED subprocess ... activation does not cross the process boundary"] |
| 51 | StringLiteral | `''` | NoCoverage | survivor | OFF_PATH | Diagnostic TEXT only. The literal is the first half of the malformed-record Error message. The throw IS reachable (see id 23 — the r1 'dead literal' reachability claim is corrected), but the mutant changes only the message string: the thrown value is still an Error, thrown at the same point, from the same exported function, and every consumer path is unchanged (main() catches it and returns exit code 2 with a different stderr line). No finding is emitted on this path at all, so no rule id, level, line or presence can move; the only observable delta is diagnostic prose plus the loss of the field count. Not EQUIVALENT (the message differs), but the divergence is confined to message text -> OFF_PATH. |
| 52 | StringLiteral | `""` | NoCoverage | survivor | OFF_PATH | Second half of the same malformed-record Error message; identical argument to id 27. Reachable (id 23's vector) but changes only the Error's message text — class Error, throw site, caller handling and exit code 2 are unchanged, and no finding is produced on this path. |
| 133 | StringLiteral | `""` | Survived | survivor | OFF_PATH | CLI presentation string only. The literal is one fragment of the three-part usage banner written to stderr on the no-input path. Blanking it shortens the banner ('... [--governed] [--merge] [--bot] --range <base>..<head> [--repo <dir>]') but leaves the return value 2, stdout empty, and every finding unchanged — the usage path emits no findings at all. It survives because the existing assertions pin the other two fragments ('usage: check-commit.mjs', '--range <base>..<head>'). Not EQUIVALENT (observable stderr text differs), but the divergence is confined to presentation text -> OFF_PATH. |
| 29 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | Operator: StringLiteral on the ENCODING argument of readFileSync (the sibling filename literal is a different mutant and is killed). Implication: Node's assertEncoding only throws for a TRUTHY non-encoding, so options="" is accepted and readFileSync returns a Buffer instead of a string. The single consumer is parseModeState(json), whose only use of the parameter is JSON.parse(json); JSON.parse applies ToString, and Buffer#toString() defaults to utf8, so it parses the byte-identical document. Invariant relied on: the path is a fixed module-relative file (join(MODULE_DIR,"prx-mode.json")) — no caller-supplied input reaches this read, so there is no input over which the two decodings could differ. defaultMode() therefore returns the same mode string on every call, and main()'s `mode = mode ?? defaultMode()` is unchanged. [carried r1: check-commit.mjs line 23 \| StringLiteral \| "" — "readFileSync encoding "utf8" -> "": ... JSON.parse coerces the Buffer to the identical UTF-8 string"] |
| 59 | MethodExpression | `parents.trim().split(/\s+/)` | Survived | survivor | EQUIVALENT | Operator: MethodExpression drops .filter(Boolean) from `parents.trim().split(/\s+/).filter(Boolean).length > 1`. Stronger than the r1 git-shape proof: the equality holds for EVERY string, not just git's %P. For any s, let t = s.trim(). If t === "" then t.split(/\s+/) === [""] (length 1) and the filtered array is [] (length 0) — 1 > 1 and 0 > 1 are both false. If t !== "", t has no leading or trailing whitespace, so splitting on /\s+/ yields only non-empty tokens and filter(Boolean) is the identity. Invariant relied on: String.prototype.trim removes exactly the code points /\s/ matches (same WhiteSpace+LineTerminator set, including U+00A0, U+FEFF, U+3000), so no empty token can survive at an edge. Therefore isMerge is identical on every input, and nothing downstream (checkCommit's isMerge exemption, PRX-C-PREFIX) can move. [carried r1: check-commit.mjs line 53 \| MethodExpression \| `parents.trim().split(/\s+/)` — ".filter(Boolean) removal ... on every shape `length > 1` answers identically"] |
| 59 | MethodExpression | `parents` | Survived | survivor | EQUIVALENT | Operator: MethodExpression drops .trim(). With .filter(Boolean) still present the trim is provably redundant for EVERY string: split(/\s+/) on an untrimmed string differs from the trimmed split only by leading/trailing "" tokens, and filter(Boolean) removes exactly those. Whitespace-only input: " ".split(/\s+/) = ["",""] -> filtered [] (0); trimmed "".split(/\s+/) = [""] -> filtered [] (0) — both 0 > 1 === false. So the isMerge boolean is unchanged on all inputs, independent of the git %P shape the r1 proof relied on. [carried r1: check-commit.mjs line 53 \| MethodExpression \| `parents` — ".trim() removal: %P output never carries leading or trailing whitespace"] |
| 59 | Regex | `/\s/` | Survived | survivor | EQUIVALENT | Operator: Regex quantifier removal in the split separator. With .filter(Boolean) still present the quantifier is redundant for EVERY string: splitting on /\s/ instead of /\s+/ turns each run of k whitespace characters into k-1 extra EMPTY tokens, and filter(Boolean) deletes exactly those empties; the non-empty tokens are identical in both splits. Hence the count, and thus `> 1`, is identical on all inputs — a stronger claim than r1's single-space %P invariant (which I did not need). [carried r1: check-commit.mjs line 53 \| Regex \| `/\s/` — "/\s+/ -> /\s/: %P separates parent hashes by exactly ONE space"] |
| 85 | ObjectLiteral | `{}` | Survived | survivor | EQUIVALENT | Operator: ObjectLiteral empties the flags initializer, so each flag reads `undefined` instead of `false` until an argument sets it `true` (the assignments still work on the empty object). All five consumers are absent/false-insensitive: checkCommit tests `opts.governed === true`, `opts.isMerge === true`, `opts.authorIsBot === true`, `opts.verifiedRevert === true`, `opts.claimedBot === true` (commit-check.mjs 274-276, 303, 307, 463) — undefined fails each strict comparison exactly as false does — and the wrapper tests `if (flags.json)` where undefined and false are both falsy. Invariant relied on: no consumer uses `?? `, `!== false`, or key enumeration on the flags/opts objects (verified by reading every opts.* use site); the JSON payload never serializes `flags`, only `results`/`verdict`/`governedScope`. [carried r1: check-commit.mjs line 79 \| ObjectLiteral \| `{}` — "flags initializer {} : every consumer treats absent and false identically"] |
| 94 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | Operator: StringLiteral on the default value of `repo`, which is read at exactly one site, `listCommits(resolve(repo), range)`. path.resolve ignores empty-string segments and resolves "." against the same cwd, so resolve("") === resolve(".") === process.cwd() for every process state; the git child therefore receives a byte-identical -C argument. The default is only live when --repo is absent (an explicit --repo overwrites it before use), so no other path observes the literal. [carried r1: check-commit.mjs line 82 \| StringLiteral \| `""` — "default repo \".\" -> \"\": the only consumer is resolve(repo), and path.resolve(\"\") === path.resolve(\"…] |
| 101 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression, the whole final-trailer-block guard forced true. The block is now entered in exactly two extra situations, and both still leave `trailers === null`, so no observable state changes. (1) bodyLines.length === 0: end = -1, the blank-skip loop does not run, start = -1 then +=1 -> 0, and the inner guard `end >= start && start >= 0` evaluates -1 >= 0 -> false; nothing is assigned. (2) unclosedFence === true: the fence extends to EOF, so every body line from the fence-open onward is classified "fence-open" or "fence-content"; the final non-blank run therefore consists solely of such lines, and block.every's conjunct `!FENCE_KINDS.has(l.kind)` (line 115) is false on the first one -> allShaped false -> trailers stays null. (Re-derived for r2: r1's proof used the removed `!block[0].inFence` conjunct; the r2 FENCE_KINDS/CODE_KINDS conjuncts give the same result.) Since trailers/trailerRecord/exemptLines are unchanged, PRX-C-TRAILER, PRX-C-GOV and PRX-C-WRAP are unchanged. [carried r1: if (!unclosedFence && bodyLines.length > 0) { (r1 commit-check.mjs:92 ConditionalExpression `true`)] |
| 116 | StringLiteral | `"Stryker was here!"` | NoCoverage | survivor | EQUIVALENT | Operator: StringLiteral on the right operand of `range ?? ""`. `range` is assigned at exactly one site, `range = args.shift()`, whose type is string \| undefined (Array#shift never yields null). The mutated operand sits inside `if (range !== undefined)`, so at evaluation time range is necessarily a string and the ?? right-hand side is never evaluated on any path — in-process or as a script. Invariant relied on: no other assignment to `range` exists and shift() cannot produce null, so 'not undefined' implies 'not nullish'. A dead operand cannot change any finding, exit code, or message. [carried r1: check-commit.mjs line 103 \| StringLiteral \| `"Stryker was here!"` — "`range ?? \"\"` right operand: the grammar check runs only inside `if (range !== undefin…] |

#### `scripts/prx/commit-check.mjs`

| Line | Mutator | Replacement | Run-2 | Replay | Class | Disposition / proof |
| --- | --- | --- | --- | --- | --- | --- |
| 55 | Regex | `/^([A-Za-z][A-Za-z0-9-]*):[ \t]*(.*)/` | Survived | survivor | BLOCKING_PATH | NOT equivalent — and this REFUTES the carried r1 row (commit-check.mjs:42, same TRAILER_LINE_RE `$`-drop, verdict EQUIVALENT: "applied only to single split(\"\\n\") lines with no newline; the greedy (.*) always consumes to end-of-string where $ is vacuously satisfied"). The premise is false: `.` in a non-dotAll JS regex excludes \n AND \r, \u2028, \u2029, and parseCommitMessage only normalizes \r\n -> \n (line 71), so a LONE CR or a U+2028/U+2029 survives inside a body line. On such a line the original regex's (.*) stops at that code point and the trailing $ (no /m) cannot match mid-string, so the whole match fails; the mutant matches and captures the truncated value. Consequence on APPROVED_BLOCKING rules: the line becomes trailer-shaped, so it enters trailers.entries / trailerRecord, which flips governedPresent -> governed scope (line 463) and adds PRX-C-GOV findings, or (in a formal block) changes PRX-C-TRAILER placement/duplicate findings. Presence of blocking-rule findings changes, not just text. |
| 67 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | NOT equivalent. CODE_KINDS becomes {"","indented-code"}, so a line classified "fence-content" is no longer code. Two consumers: (a) line 114 `!CODE_KINDS.has(l.kind)` inside allShaped — no effect, because the adjacent `!FENCE_KINDS.has(l.kind)` still excludes fence-content; (b) line 252 `const context = CODE_KINDS.has(l.kind) ? "code" : "body-text"` — fenced content is now scanned under the BODY-TEXT control policy, which rejects every Cc except TAB plus U+2028/2029, where the code policy rejects only NUL. So an ESC/BEL/etc. inside a fence gains a PRX-C-CONTROL finding that the real code does not emit: presence of an APPROVED_BLOCKING finding changes (and for NUL the context wording changes too). Activation artifact: the mutant is static:true (module-scope Set), and StrykerJS does not reliably re-execute module scope under in-worker activation — the CURRENT suite already kills it (commit-check.test.ts "control findings name the offending code points and context" asserts a U+0000 message containing "code content"), matching the independent replay ground truth. Classified by PATH regardless. |
| 68 | StringLiteral | `""` | Survived | survivor | ADVISORY_PATH | NOT equivalent, but every divergence is confined to the advisory rule PRX-C-WRAP. FENCE_KINDS becomes {"fence-open","","fence-content"}, so a "fence-close" line gets l.inFence=false (line 93). Consumers: (a) line 362 `if (l.inFence \|\| exemptLines.has(l.line)) continue` in the PRX-C-WRAP pass — a closing fence longer than 72 columns (a >=73-char marker, or a marker plus the spaces/tabs FENCE_CLOSE_TAIL_RE permits) now draws a PRX-C-WRAP advisory; (b) line 115 `!FENCE_KINDS.has(l.kind)` in allShaped — provably unreachable divergence: a fence-close line can only appear in the final non-blank run either as the run's FIRST line (then allShaped fails anyway, since block[0] must match TRAILER_LINE_RE=/^[A-Za-z].../ and a close marker starts with <=3 spaces then a backtick/tilde) or preceded in the same run by the fence-open/fence-content line that opened it (both still in FENCE_KINDS or CODE_KINDS, so block.every still fails). A text-kind line can never sit between a fence-open and its close. Hence trailers/trailerRecord — and every blocking rule that reads them — are untouched, which the 11k-message differential confirms. |
| 548 | Regex | `/Revert "/` | Survived | survivor | ADVISORY_PATH | NOT equivalent: dropping the ^ anchor makes isRevertShaped() true for any subject that merely CONTAINS 'Revert "' when the body also carries the generated marker, so the PRX-C-CONTEXT-UNVERIFIED finding appears where the real code emits none. Divergence is confined to that rule, which is registered advisory in rules.mjs and is absent from APPROVED_BLOCKING: revertShaped is read only at lines 303 and 306, and the prefix exemption (exemptFromPrefix, lines 273-276) never consults it, so PRX-C-PREFIX and every other blocking finding are byte-identical. The existing test "the revert subject shape is anchored at line start" uses toContain("PRX-C-PREFIX") and never asserts the advisory's absence — the weak-oracle shape that lets this survive. |
| 549 | Regex | `/This reverts commit [0-9a-f]{7,40}\b/` | Survived | survivor | ADVISORY_PATH | NOT equivalent: dropping the ^ anchor lets a mid-line mention of the generated marker satisfy isRevertShaped(), adding a PRX-C-CONTEXT-UNVERIFIED finding the real code does not emit. As with id 478 the effect is bounded to that advisory rule — revertShaped feeds only lines 303/306 and grants no exemption — so no APPROVED_BLOCKING finding, level, or line changes. The existing test "the revert body marker must start its line" asserts only toContain("PRX-C-PREFIX"), so it cannot see the extra advisory. |
| 73 | StringLiteral | `"Stryker was here!"` | NoCoverage | survivor | OFF_PATH | The mutated token is the right operand of `lines[0] ?? ""`, which is dead for every reachable input — hence the NoCoverage status. Reachability argument: lines = text.split("\n") where text is the result of two String.prototype.replace calls, and String.prototype.split ALWAYS returns an array of length >= 1 ("".split("\n").length === 1, probed), so lines[0] is a string and never nullish; the ?? arm never evaluates. checkCommit cannot reach it with a non-string either — line 191 returns PRX-C-SIZE for `typeof raw !== "string"` before parsing — and calling the exported parseCommitMessage with a non-string throws at raw.replace (probed: TypeError "Cannot read properties of null (reading 'replace')"). I deliberately do NOT claim EQUIVALENT: a duck-typed object whose replace chain yields {split: () => []} does reach the arm through the exported parseCommitMessage and changes its returned `subject` (probed below), so the mutant is not provably behavior-preserving on the full exported surface — it is simply unreachable from any string, and unreachable from checkCommit (and therefore from every rule id) altogether. [carried r1: const subject = lines[0] ?? ""; (r1 commit-check.mjs:54 StringLiteral, NO-COVERAGE (explained))] |
| 241 | StringLiteral | `""` | Survived | survivor | OFF_PATH | NOT equivalent, but the divergence is confined to finding MESSAGE TEXT. Operator: StringLiteral, the join separator ", " -> "" in the PRX-C-SUBJECT control-character diagnostic. The finding's rule id, class, level and line (1) are computed before the message and are untouched; the guard `subjectControls.length > 0` is unchanged, so presence is identical. Divergence requires a subject carrying two or more DISTINCT rejected code points (controlCharScan dedupes by code point), and then only concatenates the labels: "(U+0009, U+0007)" -> "(U+0009U+0007)". |
| 304 | ArrayDeclaration | `["Stryker was here"]` | Survived | survivor | OFF_PATH | NOT equivalent, message-text-only. Operator: ArrayDeclaration seeding `claims` with a sentinel. `claims` is read exactly once, at line 314 (`claims.join(" and ")`), and only inside the already-entered `if (revertShaped \|\| opts.claimedBot === true)` block, so the PRX-C-CONTEXT-UNVERIFIED finding's presence, rule, class (advisory), level and line (1) are all unchanged; only the prose gains a "Stryker was here and " prefix. No blocking rule reads claims, and the advisory itself is not in APPROVED_BLOCKING. |
| 306 | ConditionalExpression | `true` | Survived | survivor | OFF_PATH | NOT equivalent, message-text-only. Operator: ConditionalExpression forcing the `if (revertShaped)` claim-push to true. Execution only reaches line 306 inside `if (revertShaped \|\| opts.claimedBot === true)`, so the finding is emitted in exactly the same cases; the mutant merely adds the revert-shaped clause to the message when the trigger was the bot claim alone. Rule id, advisory class/level and line 1 are unchanged, and revertShaped is not consulted by any exemption (exemptFromPrefix at lines 273-276 reads only opts.isMerge/authorIsBot/verifiedRevert), so no blocking rule can move. |
| 307 | ConditionalExpression | `true` | Survived | survivor | OFF_PATH | NOT equivalent, message-text-only. Operator: ConditionalExpression forcing `if (opts.claimedBot === true)` to true, so the bot clause is always appended to the PRX-C-CONTEXT-UNVERIFIED message. The enclosing trigger condition (line 303) is untouched, so the advisory's presence set is identical; opts.claimedBot feeds nothing else (it is absent from exemptFromPrefix), so no blocking-rule finding, level, or line changes. |
| 314 | StringLiteral | `""` | Survived | survivor | OFF_PATH | NOT equivalent, message-text-only. Operator: StringLiteral, the claims join separator " and " -> "". Only the PRX-C-CONTEXT-UNVERIFIED message string changes, and only when BOTH claims are present (revert-shaped plus claimed bot); rule id, advisory class/level and line 1 are unchanged, as is the finding multiset. |
| 49 | StringLiteral | `''` | Survived | survivor | EQUIVALENT | Operator: StringLiteral, the PREFIX_STRIP_RE template -> empty template. new RegExp("") matches the empty string at index 0, so subject.replace(PREFIX_STRIP_RE, "") is the identity and description becomes subject.trim() \|\| subject instead of strippedTail.trim() \|\| subject. PREFIX_STRIP_RE has exactly ONE consumer (commit-check.mjs:524, the PRX-C-MOOD description), verified by reading the whole module; nothing else in the file or in rules.mjs/canonical.mjs reads it. Logical implication (copula-invariance): the stripped span is `^type(\(scope\))?!?: `, whose only whitespace is the single trailing space; the remaining chunk ("feat(x):", "revert!:", ...) contains no whitespace, so COPULA_RE=/(^\|\s)(is\|are\|was\|were)(\s\|$)/ cannot match inside it (a match anchored at ^ would have to be followed by \s, but the chunk runs unbroken to the colon, and no CONVENTIONAL_TYPES member or scope-grammar token equals is/are/was/were bounded by whitespace). Nor can a match straddle the boundary: the character before the description is always the prefix's trailing space, which is exactly the (^\|\s) delimiter the stripped form supplies as ^. Trimming likewise cannot create or destroy a match because (^\|\s) accepts a retained leading space as readily as ^ and (\s\|$) likewise at the end; when the stripped tail is empty or whitespace-only both forms fall back to a copula-free subject. Therefore COPULA_RE.test(description) is identical for every subject, and PRX-C-MOOD (the only affected finding, and only advisory anyway) is emitted identically. Invariant relied on: CONVENTIONAL_TYPES contains no copula and the scope grammar [A-Za-z0-9][A-Za-z0-9._/-]* admits no whitespace — both read directly from lines 29-49. [carried r1: `^(${CONVENTIONAL_TYPES.join("\|")})(\([A-Za-z0-9][A-Za-z0-9._/-]*\))?!?: ` (r1 commit-check.mjs:36 StringLiteral `''`)] |
| 49 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | Operator: StringLiteral, join("\|") -> join("") inside the PREFIX_STRIP_RE template. The alternation collapses to the single 55-char token "featfixchoredocsrefactortestperfcibuildstylerevert", so the regex matches no real subject and the strip becomes a no-op — the same end state as mutant 14 (description = subject.trim() \|\| subject). Same sole consumer (line 524 -> COPULA_RE, PRX-C-MOOD only) and the same copula-invariance implication: the un-stripped prefix contains no whitespace-delimited copula token and the boundary space plays the role of ^, so COPULA_RE.test is unchanged for every subject. (Even if the invariance failed, divergence could not leave PRX-C-MOOD, a heuristic-class rule — but it does not fail.) [carried r1: `^(${CONVENTIONAL_TYPES.join("\|")})(\([A-Za-z0-9][A-Za-z0-9._/-]*\))?!?: ` (r1 commit-check.mjs:36 StringLiteral `""`, join("\|") -> join(""))] |
| 101 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression, the whole final-trailer-block guard forced true. The block is now entered in exactly two extra situations, and both still leave `trailers === null`, so no observable state changes. (1) bodyLines.length === 0: end = -1, the blank-skip loop does not run, start = -1 then +=1 -> 0, and the inner guard `end >= start && start >= 0` evaluates -1 >= 0 -> false; nothing is assigned. (2) unclosedFence === true: the fence extends to EOF, so every body line from the fence-open onward is classified "fence-open" or "fence-content"; the final non-blank run therefore consists solely of such lines, and block.every's conjunct `!FENCE_KINDS.has(l.kind)` (line 115) is false on the first one -> allShaped false -> trailers stays null. (Re-derived for r2: r1's proof used the removed `!block[0].inFence` conjunct; the r2 FENCE_KINDS/CODE_KINDS conjuncts give the same result.) Since trailers/trailerRecord/exemptLines are unchanged, PRX-C-TRAILER, PRX-C-GOV and PRX-C-WRAP are unchanged. [carried r1: if (!unclosedFence && bodyLines.length > 0) { (r1 commit-check.mjs:92 ConditionalExpression `true`)] |
| 101 | LogicalOperator | `!unclosedFence \|\| bodyLines.length > 0` | Survived | survivor | EQUIVALENT | Operator: LogicalOperator && -> \|\|. The two guards differ only when exactly one operand is false, i.e. precisely the (a) unclosedFence-with-non-empty-body and (b) empty-body cases enumerated for id 82; in both the block is entered but produces trailers === null (empty body: inner guard -1 >= 0 fails; unclosed fence: every line of the final run is fence-open/fence-content, so block.every's !FENCE_KINDS conjunct fails and allShaped is false). No downstream consumer (trailerRecord, exemptLines, governedPresent) sees a change. [carried r1: if (!unclosedFence && bodyLines.length > 0) { (r1 commit-check.mjs:92 LogicalOperator)] |
| 101 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression on the right conjunct only; the guard becomes `!unclosedFence`. The sole new entry case is bodyLines.length === 0 with no unclosed fence, where end = -1, start = 0 after the +=1, and the inner guard `end >= start && start >= 0` is false — trailers stays null exactly as when the block is skipped. bodyLines[-1] is never read because the blank-skip loop's own `end >= 0` conjunct short-circuits first. [carried r1: if (!unclosedFence && bodyLines.length > 0) { (r1 commit-check.mjs:92 ConditionalExpression `true` on the right conjunct)] |
| 101 | EqualityOperator | `bodyLines.length >= 0` | Survived | survivor | EQUIVALENT | Operator: EqualityOperator > -> >=. Array length is a non-negative integer, so the predicates differ only at length === 0, which is the same benign case proved for id 86: end = -1 and start = 0 make the inner guard false, so trailers stays null and nothing observable changes. [carried r1: if (!unclosedFence && bodyLines.length > 0) { (r1 commit-check.mjs:92 EqualityOperator `>= 0`)] |
| 103 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression, the bounds conjunct `end >= 0` forced true. Invariant: the separator scan at lines 77-80 advances bodyStart past EVERY blank line after the subject, so if bodyLines is non-empty then bodyLines[0].text.trim() !== "" (and the enclosing guard at line 101 guarantees non-empty here). The blank-skip walk therefore always halts at some index >= 0 before the bounds check could matter, so bodyLines[-1] is never dereferenced and the loop's exit index is identical. Forcing an always-non-deciding conjunct to true is unobservable. [carried r1: while (end >= 0 && bodyLines[end].text.trim() === "") end -= 1; (r1 commit-check.mjs:94 ConditionalExpression `true`)] |
| 103 | EqualityOperator | `end > 0` | Survived | survivor | EQUIVALENT | Operator: EqualityOperator >= -> >. The two loops differ only in the iteration where end === 0: the original evaluates bodyLines[0].text.trim() === "", which is FALSE by the separator-scan invariant (bodyStart consumed every blank line, so the first body line is non-blank), and exits with end === 0; the mutant short-circuits and exits with end === 0. Identical exit state, identical downstream block. [carried r1: while (end >= 0 && bodyLines[end].text.trim() === "") end -= 1; (r1 commit-check.mjs:94 EqualityOperator `end > 0`)] |
| 107 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression, whole condition forced true. Both conjuncts are tautologies at this point, so the replacement substitutes true for an always-true expression. Proof: the enclosing guard gives bodyLines.length > 0, and the separator-scan invariant makes bodyLines[0] non-blank, so the blank-skip walk leaves end >= 0 with bodyLines[end] non-blank. The start walk then decrements at least once (bodyLines[end] is non-blank, so the loop body runs for start === end) and stops at the first blank index or -1; after `start += 1`, start <= end and start >= 0. Hence `end >= start && start >= 0` is true whenever the line is reached. [carried r1: if (end >= start && start >= 0) { (r1 commit-check.mjs:98 ConditionalExpression `true`)] |
| 107 | LogicalOperator | `end >= start \|\| start >= 0` | Survived | survivor | EQUIVALENT | Operator: LogicalOperator && -> \|\|. Both operands are always true when this line executes (see the id 112 proof: end >= 0 with bodyLines[end] non-blank, and 0 <= start <= end after the start walk and +=1), and true\|\|true === true&&true, so the branch decision is unchanged on every reachable input. [carried r1: if (end >= start && start >= 0) { (r1 commit-check.mjs:98 LogicalOperator)] |
| 107 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression on the left conjunct `end >= start`, which is an invariant (the start walk always decrements at least once past the non-blank bodyLines[end] before `start += 1`, so start <= end). Replacing an always-true conjunct with true leaves the conjunction's value unchanged on every reachable input. [carried r1: if (end >= start && start >= 0) { (r1 commit-check.mjs:98 ConditionalExpression `true`, left conjunct)] |
| 107 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression on the right conjunct `start >= 0`, which is an invariant: the start walk terminates at worst at start === -1, and the following `start += 1` (line 106) lifts it to 0, so start >= 0 always holds here. Replacing it with true is unobservable. [carried r1: if (end >= start && start >= 0) { (r1 commit-check.mjs:98 ConditionalExpression `true`, right conjunct)] |
| 123 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression, the continuation-append guard forced true. The else arm is reached only for a block line that fails TRAILER_LINE_RE (a continuation line), and allShaped (line 116) requires TRAILER_LINE_RE.test(block[0].text), so the FIRST iteration always takes the if arm and pushes an entry. Every subsequent else therefore sees parsed.length >= 1, where the original guard is already true — the mutant can never make parsed[parsed.length - 1] resolve to parsed[-1]. [carried r1: } else if (parsed.length > 0) { (r1 commit-check.mjs:113 ConditionalExpression `true`)] |
| 123 | EqualityOperator | `parsed.length >= 0` | Survived | survivor | EQUIVALENT | Operator: EqualityOperator > -> >=; the guard becomes a tautology. It only matters when parsed.length === 0, which cannot occur in the else arm: allShaped guarantees block[0] matches TRAILER_LINE_RE, so the loop's first iteration pushes an entry before any continuation line can be seen. Both forms are true on every reachable execution of this branch. [carried r1: } else if (parsed.length > 0) { (r1 commit-check.mjs:113 EqualityOperator `>= 0`)] |
| 142 | ArrayDeclaration | `["Stryker was here"]` | Survived | survivor | EQUIVALENT | Operator: ArrayDeclaration on the `?? []` right operand of `new Set(trailers?.blockLines ?? [])`. The fallback evaluates only when trailers is null (when it is non-null, blockLines is always an array built at line 130). blockLineSet has exactly one consumer, `blockLineSet.has(l.line)` at line 158, and l.line is always a number (assigned as bodyStart + i + 1 at line 83). Set membership uses SameValueZero with no coercion, so a set containing only the string "Stryker was here" answers has(<number>) === false for every line — indistinguishable from the empty set. The mechanism is r1's line-132 proof applied to the r2 blockLineSet site. [carried r1: ?? [] feeding new Set(...) whose only consumer is has(l.line) with a numeric line (r1 commit-check.mjs:132 ArrayDeclaration)] |
| 186 | ArrayDeclaration | `["Stryker was here"]` | Survived | survivor | EQUIVALENT | Operator: ArrayDeclaration on the `?? []` right operand inside trailerLines(). The fallback fires only when parsed.trailers is null. The returned set is bound to exemptLines (line 360) and consulted exclusively as `exemptLines.has(l.line)` at line 362, with l.line always numeric; a set holding one string answers false for every numeric probe, exactly like the empty set. Nothing else reads trailerLines' return value, so the PRX-C-WRAP exemption span — and every other finding — is unchanged. [carried r1: ?? [] feeding new Set(...) whose only consumer is has(l.line) with a numeric line (r1 commit-check.mjs:132 ArrayDeclaration — the trailerLines site)] |
| 524 | LogicalOperator | `subject.replace(PREFIX_STRIP_RE, "").trim() && subject` | Survived | survivor | EQUIVALENT | Operator: LogicalOperator \|\| -> &&. Two cases. (a) The stripped-and-trimmed tail T is truthy: the original uses T, the mutant uses the full subject. By copula-invariance these give the same COPULA_RE verdict — the stripped span `^type(\(scope\))?!?: ` contains no whitespace-delimited copula (types are a fixed copula-free list; the scope grammar admits no whitespace, so its interior can never be bounded by \s or ^/$), and the space that terminates the prefix supplies the very (^\|\s) delimiter that ^ supplies in T; trailing/leading whitespace is likewise interchangeable with ^/$ for this regex. (b) T is falsy (""): the original falls back to subject, the mutant yields "". That case arises only when the subject is prefix-plus-whitespace or whitespace-only, both copula-free, so COPULA_RE.test is false either way. description has exactly one consumer (line 525), and the only finding it can produce is PRX-C-MOOD (heuristic). [carried r1: const description = subject.replace(PREFIX_STRIP_RE, "").trim() \|\| subject; (r1 commit-check.mjs:384 LogicalOperator)] |
| 524 | MethodExpression | `subject.replace(PREFIX_STRIP_RE, "")` | Survived | survivor | EQUIVALENT | Operator: MethodExpression, .trim() removed from the description. COPULA_RE = /(^\|\s)(is\|are\|was\|were)(\s\|$)/ is insensitive to leading/trailing whitespace: any copula that the trimmed form matches at ^ or $ is matched in the untrimmed form via the retained \s, and vice versa; whitespace can neither create nor destroy a token boundary here. The only other effect is the truthiness of the left operand of \|\|: for a whitespace-only tail the original falls back to `subject` (prefix + whitespace, copula-free -> false) while the mutant keeps the whitespace-only tail (also false). So COPULA_RE.test(description), the single consumer, is identical, and PRX-C-MOOD (heuristic, and the only rule reachable from description) is emitted identically. [carried r1: const description = subject.replace(PREFIX_STRIP_RE, "").trim() \|\| subject; (r1 commit-check.mjs:384 MethodExpression)] |

#### `scripts/prx/lib/canonical.mjs`

| Line | Mutator | Replacement | Run-2 | Replay | Class | Disposition / proof |
| --- | --- | --- | --- | --- | --- | --- |
| 68 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `amp: "&"` -> `amp: ""` makes decodeHtmlEntities("&amp;") return "" instead of "&". The module comment states the invariant this breaks: an undecoded visible reference and its decoded visible expansion must BOTH be meaningful. isMeaningfulText("&")=true vs isMeaningfulText("")=false, and that decision is the sole input to bodyRendersMeaningfulText (PRX-B-VISIBLE) and nodeHasVisibleContent/hasVisibleSectionContent (PRX-B-SECTION-EMPTY) — both APPROVED_BLOCKING. Reached whenever the entity lives inside a raw-HTML block node (block-level html is one mdast node, so its value goes through visibleInlineText -> meaningfulVisibleHtml -> decodeHtmlEntities; inline `&amp;` in a paragraph is decoded by the parser instead). Empirically a body whose only content is `&amp;` inside a <div> block GAINS PRX-B-VISIBLE under the mutant, and the same content under `## Tests` GAINS PRX-B-SECTION-EMPTY. Static/module-scope mutant, but static activation demonstrably works for this object literal (the pinned siblings nbsp/shy/thinsp/ZeroWidthSpace/zwnj/zwj/NoBreak are recorded Killed) — this one survives only because no test pins the five structural entities. |
| 69 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `lt: "<"` -> `""`: decodeHtmlEntities("&lt;") returns "" instead of "<", flipping isMeaningfulText from true to false for raw-HTML content whose only rendered character is an escaped angle bracket. Same blocking mechanism as id 41 — the decoded text is consumed only by isMeaningfulText, which decides PRX-B-VISIBLE and PRX-B-SECTION-EMPTY (both APPROVED_BLOCKING). Verified end to end at checkBody level. |
| 70 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `gt: ">"` -> `""`: identical mechanism to ids 41/42 — the decoded expansion of a visible structural entity becomes empty, so meaningful rendered content is scored as empty and PRX-B-VISIBLE / PRX-B-SECTION-EMPTY (APPROVED_BLOCKING) gain a finding that the real code does not emit. |
| 71 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `quot: '"'` -> `""`: same class as 41-43. A body whose rendered text is an escaped double quote inside raw HTML flips from meaningful to empty, adding PRX-B-VISIBLE / PRX-B-SECTION-EMPTY findings (APPROVED_BLOCKING). |
| 72 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | `apos: "'"` -> `""`: same class as 41-44 — the apostrophe expansion is a VISIBLE character, so blanking it turns meaningful rendered text into empty and changes the presence of PRX-B-VISIBLE / PRX-B-SECTION-EMPTY (APPROVED_BLOCKING). |
| 117 | ConditionalExpression | `true` | Survived | survivor | BLOCKING_PATH | The `cp <= 0xdfff` conjunct -> true widens the U+FFFD arm from the surrogate block to EVERY code point >= 0xD800. The blocking consequence is that Cf code points above the surrogate block — U+FEFF (BOM) above all, plus U+FFF9-FFFB and U+1D173-1D17A — are no longer decoded to their invisible selves but to U+FFFD, which IS meaningful. So entity-encoded invisibles stop being recognized as rendered-empty: exactly the BYP-B-01/02 bypass this module closes. Empirically a body whose only content is `&#xFEFF;` inside a raw-HTML block LOSES its PRX-B-VISIBLE finding under the mutant, and the same content under `## Tests` LOSES PRX-B-SECTION-EMPTY — both APPROVED_BLOCKING. canonical.test.ts pins only &#xD800; (inside the block), so the mutant survives. |
| 147 | ConditionalExpression | `false` | Survived | survivor | BLOCKING_PATH | `if (end === -1) return out;` -> `if (false)` in the cross-chunk COMMENT branch. When a comment opened in an earlier chunk has no `-->` in the current chunk, the real scanner keeps swallowing (returns out, stays insideComment); the mutant instead clears insideComment and resumes at i = -1 + 3 = 2, emitting the tail of the hidden chunk as visible text. That is the exact resurrection the module's contract forbids ("an element or comment opened in one chunk stays open into the following chunks"). Reachable in-process: a paragraph whose inline-HTML children include an unterminated comment construct feeds the per-sibling-list scanner in visibleInlineText (body-check.mjs:669/681) across chunks — `checkBody("&nbsp;<!--> q<script>")` emits PRX-B-VISIBLE on real code and NOTHING under the mutant, and the same fragment under `## Tests` loses PRX-B-SECTION-EMPTY. Both APPROVED_BLOCKING. The existing canonical.test.ts cross-chunk test only exercises the chunk that DOES contain `-->`, so the -1 arm is unpinned. |
| 147 | UnaryOperator | `+1` | Survived | survivor | BLOCKING_PATH | `end === -1` -> `end === +1` inverts the cross-chunk comment guard twice over: (a) the not-found case (end === -1) no longer returns, so hidden comment text is resurrected from index 2 exactly as in id 135, and (b) a chunk whose `-->` sits at index 1 now returns early and leaves insideComment true, swallowing text that should be visible. Both change the meaningful-visible-text decision that PRX-B-VISIBLE and PRX-B-SECTION-EMPTY (APPROVED_BLOCKING) consume, and (a) is reproduced end to end at checkBody level with the same fixture as id 135. |
| 153 | StringLiteral | `""` | Survived | survivor | BLOCKING_PATH | The `"i"` flag of the removed-element CLOSE regex -> `""`. insideRemoved is stored lowercase, so without case-insensitivity a mixed-case closing tag (`</SCRIPT>`) no longer terminates the removed span and the scanner swallows the REST of the value (and of every following chunk). Text that really renders is therefore scored as empty, adding PRX-B-VISIBLE / PRX-B-SECTION-EMPTY (APPROVED_BLOCKING) findings the real code does not emit. The existing test `visibleTextFromHtml("<STYLE>.a{}</STYLE>", REMOVED)` is a weak oracle for this: it expects "" and the mutant also returns "", so it cannot see the flag loss — the oracle must assert text AFTER a mixed-case close. |
| 170 | MethodExpression | `value` | Survived | survivor | BLOCKING_PATH | `value.slice(i).match(...)` -> `value.match(...)`: the anchored tag probe is re-run against the START of the chunk instead of the current scan position, so every tag after the first is misidentified. When the chunk does not begin with the removed element's tag, `removed.has(tag[1])` is evaluated against the WRONG element name and the removed-content branch is skipped — the sanitizer-removed content is emitted as visible text (or, symmetrically, a leading removed name makes an unrelated tag open a removed span). This directly inverts the PRX-B-VISIBLE / PRX-B-SECTION-EMPTY decision: `checkBody("<div>\n<script>secret</script>\n</div>\n")` emits PRX-B-VISIBLE on real code (script content renders as nothing) and emits NOTHING under the mutant, which counts "secret" as visible; the sectioned form loses PRX-B-SECTION-EMPTY the same way. Both APPROVED_BLOCKING. Existing tests all start the value with the removed tag, so they cannot see it. |
| 170 | Regex | `/<\/?([A-Za-z][A-Za-z0-9-]*)/` | Survived | survivor | BLOCKING_PATH | Dropping the `^` anchor lets the tag probe match a tag ANYWHERE later in the chunk when the character at the scan position is a `<` that does not start a tag (e.g. `<3`). The name of that far-away tag is then tested against the removed set and, on a hit, the scanner opens a removed span at the wrong offset and swallows text that really renders. `checkBody("<div>\n<3 <script>evil</script>\n</div>\n")` gains a PRX-B-VISIBLE finding under the mutant (APPROVED_BLOCKING) because "evil", which the real scanner emits, is dropped. Not equivalent in the other direction either (the anchored form is what keeps the offset and the tag identity in sync). |
| 174 | ConditionalExpression | `true` | Survived | survivor | BLOCKING_PATH | The span `tag && !tag[0].startsWith("</")` -> `true` removes BOTH the null guard and the closing-tag guard. Two live consequences: (1) when the probe found no tag (a `<` not followed by a letter, with some `>` later in the value) the next conjunct dereferences `tag[1]` and throws TypeError — the exception escapes visibleInlineText, which sits OUTSIDE checkBody's only try/catch (that wraps parseBody alone), so checkBody THROWS instead of returning findings and the CLI wrapper degrades to exit 2; (2) a stray `</script>` now opens a removed span and swallows the rest, adding a PRX-B-VISIBLE finding. Both are APPROVED_BLOCKING-path changes. Empirically `checkBody("<div>\na < b\n</div>\n")` throws under the mutant. Note ids 187 and 188 are behaviorally IDENTICAL mutants in my differential (same 470 divergences, same throw sites); the ground-truth replay records 188 as killed, so the same test kills 187 — its survival is a Stryker activation/attribution artifact rather than a real oracle gap. |
| 174 | LogicalOperator | `tag \|\| !tag[0].startsWith("</")` | Survived | killed | BLOCKING_PATH | `&&` -> `\|\|` makes the guard true whenever tag is truthy (so a stray `</script>` opens a removed span and swallows visible text) and dereferences `tag[0]` on null when the probe found no tag (TypeError escaping checkBody, which only try/catches parseBody). Identical observable set to id 187 in my differential. Ground truth records this mutant as ALREADY KILLED by the current suite; classified BLOCKING_PATH for the record — the PRX-B-VISIBLE presence flip and the checkBody throw are both on the blocking path. |
| 175 | MethodExpression | `tag[0].endsWith("</")` | Survived | survivor | BLOCKING_PATH | `!tag[0].startsWith("</")` -> `!tag[0].endsWith("</")`. tag[0] is always `<name` or `</name` and can never END with "</", so the guard degenerates to a constant true: every CLOSING tag of a removed element now OPENS a removed span. A stray or unbalanced `</script>` therefore swallows the remainder of the chunk (and keeps the scanner inside across following chunks), so genuinely rendered text is scored as empty and PRX-B-VISIBLE / PRX-B-SECTION-EMPTY (APPROVED_BLOCKING) gain findings. Verified at checkBody level. |
| 301 | Regex | `/^ {0,3}('{3,}\|~{3,})(.*)/` | Survived | survivor | BLOCKING_PATH | NOT the r1 '$-anchor vacuity' class. The r1 proof (commit-check.mjs:42, TRAILER_LINE_RE) argued the greedy (.*) always consumes to end-of-string so $ is vacuous. That argument is FALSE for any line containing a character `.` cannot cross: CR (U+000D, which survives parseCommitMessage because only \r\n is normalised), U+2028 and U+2029. On such a line the ORIGINAL regex fails to match at all (the (.*) stops at the terminator and $ cannot be satisfied), so the line is classified "text"; the mutant matches with m[2] truncated at the terminator and classifies it "fence-open". Divergence is in the exported classifyCommitBodyLines return value AND in the blocking PRX-C-FENCE rule: a body line "```\rcode" opens an unclosed fence under the mutant, adding PRX-C-FENCE (deterministic, APPROVED_BLOCKING) that the original never emits. It also changes bodyLine.kind/inFence for every subsequent line, which feeds the trailer-block scan and the PRX-C-CONTROL context choice. NOTE FOR THE PARENT: this same CR/U+2028 hole falsifies the r1 EQUIVALENT verdict recorded for commit-check.mjs:42 (`TRAILER_LINE_RE loses trailing $`); probed directly: /^([A-Za-z][A-Za-z0-9-]*):[ \t]*(.*)$/.exec("Evidence: docs/a\rjunk") === null while the $-less mutant matches with value "docs/a", so that carried row needs re-opening in whichever group owns it. |
| 308 | ArithmeticOperator | `col * 4` | Survived | survivor | BLOCKING_PATH | col += 4 - (col * 4) = 4 - 3*col. For col=0 it coincides with the original (4), but for any tab preceded by at least one space/tab the mutant UNDERCOUNTS (col=1 -> 1 instead of 4; col=2 -> -2; col=3 -> -5), so lines the original classifies "indented-code" become "text". That kind is load-bearing for two blocking decisions in commit-check.mjs: (a) CODE_KINDS membership selects the control-character policy — "code" (NUL only) vs "body-text" (every Cc except TAB, plus U+2028/9), so a control character inside a space+tab-indented code line raises a PRX-C-CONTROL (deterministic, APPROVED_BLOCKING) finding that the original suppresses by policy; (b) the out-of-block governed-trailer scan and the formal-block shape test both key off kind, so indented content can be re-read as trailer text. Divergence is also directly visible in the exported classifyCommitBodyLines return value. |
| 334 | MethodExpression | `line` | Survived | survivor | BLOCKING_PATH | Removing .trim() from the blank-line test makes a whitespace-only line (" ", "\t") no longer blank: it falls through to the fence test, then to the indent test, and is emitted as "text" while ALSO setting paragraphOpen = true. Two observable consequences: (1) the exported kinds array changes directly ("blank" -> "text"); (2) because paragraphOpen is now left open, a following >=4-column line is treated as a lazy paragraph continuation ("text") instead of "indented-code", which flips the control-character policy from "code" to "body-text" and raises a PRX-C-CONTROL (APPROVED_BLOCKING) finding the original suppresses. The same kind flip also decides CODE_KINDS exclusion in the trailer-block scan and the `l.kind === "text"` gate of the out-of-block governed-trailer record, so it feeds PRX-C-TRAILER/PRX-C-GOV as well. |
| 342 | BooleanLiteral | `true` | Survived | survivor | BLOCKING_PATH | The assignment sits in the fence-OPEN arm. While the fence is open every line takes the `if (fence)` branch, and the fence-CLOSE arm never touches paragraphOpen — so the mutated value survives the whole fenced block and governs the first line AFTER the closing fence. Original: paragraphOpen=false, so a >=4-column line after the fence is "indented-code". Mutant: paragraphOpen=true, so the same line is a lazy continuation "text". Exported kinds differ, and the kind change flips the control-scan context from "code" to "body-text", producing a PRX-C-CONTROL (APPROVED_BLOCKING) finding on control characters that the code policy deliberately permits; it equally flips CODE_KINDS/text gating for the trailer record. |
| 400 | BooleanLiteral | `true` | Survived | survivor | BLOCKING_PATH | Pre-established live survivor, re-verified here rather than taken on trust. The mutant turns the drive-letter REJECTION into an ACCEPTANCE while leaving the reason string "drive-letter prefix" untouched, so any oracle that asserts only `.reason` cannot see it — the weak-oracle shape introduced between run 1 and run 2. Blocking reach: validateEvidenceRef feeds commit-check.mjs:496 (PRX-C-GOV, deterministic/APPROVED_BLOCKING) and body-check.mjs:565 (PRX-B-CAPSULE), so a governed commit carrying `Evidence: C:evil` LOSES its PRX-C-GOV finding entirely. The fix is already landed: canonical.test.ts's `reject()` helper now asserts `r.valid === false` alongside the reason, and the drive-letter case is exercised there with an exact-reason anchor. |
| 429 | MethodExpression | `segments` | Survived | survivor | BLOCKING_PATH | NOT equivalent, and the divergence is reachable with the SHIPPED adapter config — this is the one I expected to be config-only and empirically was not. `rest` is re-assigned at line 434 only for roots carrying firstSegmentRe (just `run`), so for every other root the mutant additionally runs segments[0] through config.segmentRe. `docs` passes segmentRe, so the interesting case is the root lookup itself: `config.roots[segments[0]]` is a plain property read on a frozen OBJECT LITERAL, so it inherits Object.prototype. `config.roots["__proto__"]` returns Object.prototype (truthy), `root.minSegments` is undefined (so `segments.length < undefined` is false) and `root.firstSegmentRe` is undefined — so "__proto__/x" reaches line 429 as an ACCEPTED reference under the original (a latent hole worth its own fix), while the mutant re-checks "__proto__" against segmentRe (leading underscore) and REJECTS it. That flips the blocking PRX-C-GOV finding on and off. Same for "__defineGetter__", "__defineSetter__", "__lookupGetter__", "__lookupSetter__" (the Object.prototype names that fail segmentRe). |
| 467 | ArithmeticOperator | `i - 4` | Survived | survivor | BLOCKING_PATH | Searching the terminator from i-4 lets the scanner find a "-->" that lies BEFORE the comment it is currently skipping. Two consequences, both empirically confirmed. (1) Termination: if the earlier "-->" ends at or before the comment start, `i = end + 3` moves the cursor back to the same comment start and the loop repeats forever — createHtmlContainerTracker.feed NEVER RETURNS. That is reachable in-process through extractProse (an html node whose value is "<!--a--><!--b" — an HTML block type 2 line that is not a pure comment, so isCommentNode does not filter it out), and therefore through the live gate: .github/workflows/14-prx-communication.yml runs `check-body.mjs pr-body.md --extract-prose pr-prose.md`, and check-body.mjs:54 calls extractProse INSIDE the try BEFORE checkBody — so the whole body gate hangs and NO verdict/exit code is ever produced. That is why this is BLOCKING_PATH rather than ADVISORY_PATH: the divergence is not confined to prose content, it destroys the exit-code contract. (2) There is also a terminating divergence ("<!-->x<table>": the mutant finds the "-->" at i+2 inside the comment opener and resumes mid-string) which changes tracker state without hanging. |
| 468 | ConditionalExpression | `false` | Survived | survivor | BLOCKING_PATH | With the guard disabled, an UNTERMINATED comment (end === -1) no longer causes the early return; the scanner instead executes `i = end + 3` = 2 and resumes scanning at index 2 — INSIDE the comment it failed to close. Two consequences, both confirmed. (1) Terminating divergence: markup inside an unterminated comment is counted ("<!-- unterminated <table>" flips isInside false -> true), changing tracker state and extractProse output. (2) Non-termination: because the cursor can move BACKWARD (2 < the comment start for any later comment), the input "<!--<!--" cycles 2 -> 4 -> 2 forever. That input reaches extractProse in-process (unterminated comments are not filtered by isCommentNode), and the live gate calls extractProse before checkBody inside check-body.mjs's try (workflow 14-prx-communication.yml passes --extract-prose), so the body gate hangs and never produces a verdict or exit code. Exit-code destruction is why this is BLOCKING_PATH rather than prose-only ADVISORY_PATH. |
| 468 | EqualityOperator | `end !== -1` | Survived | survivor | BLOCKING_PATH | The guard is inverted: a WELL-FORMED comment now causes the early return (the rest of the chunk is abandoned), and an UNTERMINATED comment falls through to `i = end + 3` = 2 and rescans from inside itself. So it carries both failure modes at once — mutant 539's early-abandon behaviour on terminated comments AND mutant 540's backward-cursor behaviour on unterminated ones. The backward cursor is again non-terminating on "<!--<!--" (2 -> 4 -> 2 forever), reachable in-process through extractProse and therefore through the live gate (check-body.mjs:54 runs extractProse inside the try, before checkBody, and workflow 14 passes --extract-prose), so the body gate hangs and never emits a verdict or exit code. Classified BLOCKING_PATH on the exit-code contract; the terminating half of its divergence is prose-only. |
| 468 | UnaryOperator | `+1` | Survived | survivor | BLOCKING_PATH | `end` is either -1 or an indexOf result >= i + 4 >= 4, so `end === 1` is unsatisfiable: the guard becomes dead and the mutant is behaviourally IDENTICAL to mutant 540 (`if (false) return`) — the unterminated-comment early return is removed and the scanner resumes at i = end + 3 = 2, inside the comment. Consequences are 540's: markup inside an unterminated comment is counted (terminating, prose-visible), and the backward cursor makes "<!--<!--" loop forever, hanging extractProse and therefore the whole check-body invocation (extractProse runs inside check-body.mjs's try before checkBody; workflow 14 passes --extract-prose), so no verdict or exit code is produced. Not equivalent, and BLOCKING_PATH on the exit-code contract rather than ADVISORY_PATH. |
| 474 | ConditionalExpression | `false` | Survived | survivor | BLOCKING_PATH | The no-match/mismatched-index guard is removed, so execution falls into `const name = m[1].toLowerCase()` even when TAG_SCAN_RE.exec returned null — a TypeError ("Cannot read properties of null (reading '1')") thrown OUT of the exported feed(), out of extractProse (which wraps only parseBody in try/catch), and into check-body.mjs main(). Because check-body.mjs:54 calls extractProse INSIDE the try and BEFORE checkBody, the catch at line 76 converts the whole run to `return 2` — the body gate reports a tool error and prints ZERO findings where the real code prints the full blocking set and exits 0/1. That is an exit-code and verdict change, not a prose change, so BLOCKING_PATH. Reachable input: any raw-HTML node with trailing non-tag text ("<div>x</div> tail"), which mdast keeps as a single html node inside an HTML block. |
| 474 | LogicalOperator | `!m && m.index !== i` | Survived | survivor | BLOCKING_PATH | && forces evaluation of the right operand whenever `!m` is true — i.e. exactly when m is null — so `m.index` throws TypeError ("Cannot read properties of null (reading 'index')") on any chunk whose remaining text contains no matchable tag. When m is non-null the condition is always false, so the mismatched-index case also degrades (the later-found tag is processed as if it began at i), but the throw dominates. Same escalation path as 545: the exception leaves feed() and extractProse uncaught, and check-body.mjs's try/catch turns the entire body gate into `return 2` with no findings — an exit-code and verdict change, hence BLOCKING_PATH. |
| 474 | BlockStatement | `{}` | Survived | survivor | BLOCKING_PATH | Emptying the guard's body keeps the test but removes the recovery, so control falls through to `const name = m[1].toLowerCase()` in exactly the cases the guard was written to protect — including m === null, which throws TypeError ("Cannot read properties of null (reading '1')"). Identical escalation to 545/546: the throw exits the exported feed() and extractProse uncaught, and check-body.mjs's catch (extractProse is called inside the try at line 54, before checkBody) turns the run into `return 2` with no findings emitted. Exit-code and verdict change -> BLOCKING_PATH. |
| 485 | BooleanLiteral | `c` | Survived | survivor | BLOCKING_PATH | The guard is inverted twice over: when the close tag IS found the scanner returns early (abandoning the rest of the chunk — a prose-level divergence), and when it is NOT found execution falls through to `i += c.index + c[0].length` with c === null, throwing TypeError ("Cannot read properties of null (reading 'index')"). The throw is reachable in-process through extractProse with an unterminated raw-text element (`<script>` with no `</script>`, which mdast keeps as one html node to end of block), and check-body.mjs calls extractProse inside its try at line 54 before checkBody, so the catch converts the run to `return 2` with no findings — an exit-code and verdict change. BLOCKING_PATH (its terminating half is merely advisory, but the throwing half is not). |
| 485 | ConditionalExpression | `false` | Survived | survivor | BLOCKING_PATH | The unmatched-close guard is disabled, so when a raw-text element (script/style) is never closed in the chunk, execution reaches `i += c.index + c[0].length` with c === null and throws TypeError ("Cannot read properties of null (reading 'index')"). This is the ONLY divergence class this mutant has (every terminating case is identical), and it is reachable in-process via extractProse on an unterminated `<script>` block. check-body.mjs:54 calls extractProse inside the try before checkBody, so the catch turns the entire body gate into `return 2` with zero findings printed instead of the real verdict — an exit-code and verdict change, hence BLOCKING_PATH rather than ADVISORY_PATH. |
| 456 | StringLiteral | `""` | Survived | survivor | ADVISORY_PATH | RAW_TEXT_ELEMENTS is read at exactly one site (line 482, inside createHtmlContainerTracker.feed) to decide whether an opening tag's content is skipped whole. Blanking "style" means a <style> element's body is now SCANNED, so container tags quoted inside CSS change the depth balance; the "" member is inert because TAG_SCAN_RE's name group requires a leading [A-Za-z], so `name` is never the empty string. createHtmlContainerTracker has exactly one in-repo consumer, extractProse (body-check.mjs:511/537), whose output is written to pr-prose.md and consumed only by the advisory Vale style layer — no finding, level, line, or exit code changes. Not equivalent (the exported tracker state and the prose text both change), so ADVISORY_PATH. Stryker flags this mutant `static: true` (module scope); the independent replay confirms the real suite passes it, so it is a genuine oracle gap, not only an activation artifact — canonical.test.ts's raw-text case uses <script> only. |
| 457 | Regex | `/<\/?([A-Za-z][A-Za-z0-9-]*)(?:"[^"]*"\|'[^']'\|[^>"'])*>/g` | Survived | survivor | ADVISORY_PATH | The single-quoted-attribute alternative loses its * quantifier, so it matches only a one-character quoted value. Any tag carrying a single-quoted attribute of length != 1 no longer matches at its own index: the tracker's `!m \|\| m.index !== i` arm then walks past it one character at a time, and the element never enters the depth balance (and, with a '>' inside the quoted value, the fallback [^>"'] cannot consume the quote either). Only the exported tracker state and, through it, extractProse's prose text change; no finding, level, line, or exit code is affected (createHtmlContainerTracker's sole consumer is extractProse -> pr-prose.md -> advisory Vale). Static/module-scope mutant, but the replay shows the real suite passes it: canonical.test.ts's attribute test uses a DOUBLE-quoted value, which this mutation leaves untouched. |
| 457 | Regex | `/<\/?([A-Za-z][A-Za-z0-9-]*)(?:"[^"]*"\|'[']*'\|[^>"'])*>/g` | Survived | survivor | ADVISORY_PATH | Same alternative, negation dropped: the branch now matches only runs of apostrophes ('', ''', ...), so EVERY single-quoted attribute value (including one-character values, unlike mutant 519) breaks the tag match at its index. Consequence is identical in kind: containers with single-quoted attributes stop being counted, the tracker's exported depth state changes, and extractProse's designated-prose output changes. No blocking or advisory FINDING changes — the prose stream feeds only the Vale style layer. Static/module-scope mutant; the real suite passes it because the only attribute test in canonical.test.ts uses double quotes. |
| 466 | MethodExpression | `value.endsWith("<!--", i)` | Survived | survivor | ADVISORY_PATH | endsWith(s, i) tests value.slice(0, i), so the comment branch fires four characters LATE — at the index just past a "<!--" rather than at its start. For a plain comment that mostly self-corrects (positions i..i+3 contain no '<' that TAG_SCAN_RE can match at its own index), which is why the canonical.test comment case still passes. It does NOT self-correct in the terminator search: the branch computes indexOf("-->", i + 4) from the LATE i, i.e. four characters past the comment's own "-->", so a short comment's terminator is missed, end === -1 and feed() RETURNS EARLY, silently abandoning the rest of the chunk. Divergence is confined to the exported tracker state and extractProse's prose text; no finding/level/line changes, and the function still terminates and never throws (the comment branch only ever advances i). |
| 467 | StringLiteral | `""` | Survived | survivor | ADVISORY_PATH | indexOf("", p) returns min(p, len), so `end` becomes i+4 unconditionally: the terminator is never searched, end === -1 never happens, and the scanner resumes at i+7 — seven characters into the comment instead of past it. Every comment longer than seven characters therefore has its BODY scanned for tags, so container markup quoted inside a comment starts counting toward the depth balance. Divergence is confined to the exported tracker state and to extractProse's prose text (createHtmlContainerTracker has no other consumer); no finding, level, line, or exit code changes, and i strictly advances so the loop still terminates. Reachability note: a PURE comment node is filtered out of `visible` by isCommentNode, so the prose-level divergence needs a raw-HTML node that merely CONTAINS a comment — which is why my first document corpus showed no prose divergence and the second one did. Recording that so the row is not read as tracker-only. |
| 468 | ConditionalExpression | `true` | Survived | survivor | ADVISORY_PATH | The scanner now returns from feed() at the FIRST comment start regardless of whether the comment terminates, so everything after a comment in the same raw-HTML chunk is never scanned and cannot affect the depth balance. Note it also never sets insideComment = true, so it is an unconditional early return, not a state change. Divergence shows in the exported tracker state and in extractProse's prose text only: no finding presence/rule/level/line changes, the function always terminates, and it never throws (the early return is the safest possible exit). ADVISORY_PATH. |
| 469 | ArithmeticOperator | `end - 3` | Survived | survivor | ADVISORY_PATH | After skipping a comment the scanner resumes three characters BEFORE the terminator instead of after it, so the last three characters of the comment body plus the "-->" are re-scanned. Termination is safe (end >= commentStart + 4, so end - 3 > commentStart: the cursor always advances past the comment opener and cannot loop), and nothing throws. The re-scanned window is too narrow to admit a container tag whose NAME is a container (the greedy name class [A-Za-z0-9-]* swallows the terminator's hyphens, e.g. "<td-->" parses as name "td--"), which is why my first corpora showed no divergence — but it IS wide enough to re-detect a comment opener when the source reads "...<!--->", and the mutant then searches for the NEXT "-->", fails, and returns early, abandoning the rest of the chunk. Divergence is confined to the exported tracker state and extractProse's prose text: ADVISORY_PATH. |
| 474 | ConditionalExpression | `false` | Survived | survivor | ADVISORY_PATH | The condition collapses to `!m`, so a tag found LATER in the chunk is processed as though it started at the cursor. No throw (m is non-null on that path) and no hang (i is set to m.index + m[0].length, which strictly advances). The behavioural difference is that the skipped intervening characters are never examined for a comment opener: the original walks them one at a time and would enter the comment branch, the mutant jumps over them, so markup inside a comment can be counted into the depth balance. Divergence is confined to the exported tracker state and extractProse's prose text — no finding, level, line, or exit-code change. ADVISORY_PATH. |
| 483 | StringLiteral | `""` | Survived | survivor | ADVISORY_PATH | `name` is lower-cased at line 478, so without the case-insensitive flag the generated close pattern can never match an upper- or mixed-case closing tag. For `<SCRIPT>x</SCRIPT>` the close is not found, `if (!c) return;` fires and feed() abandons the rest of the chunk — everything after the raw-text element stops contributing to the depth balance. No throw (the !c path still returns cleanly) and no hang. Divergence is confined to the exported tracker state and extractProse's prose text; createHtmlContainerTracker has no other consumer, so no finding or exit code moves. ADVISORY_PATH. The existing canonical.test raw-text case uses lowercase <script>, which is why the real suite passes this mutant. |
| 484 | MethodExpression | `value` | Survived | survivor | ADVISORY_PATH | Removing the slice makes c.index an ABSOLUTE offset that is then added to the cursor (`i += c.index + c[0].length`), double-counting the prefix and overshooting the close tag by c.index characters — typically past the end of the chunk, so the remainder is silently dropped. (It can also match a `</script>` that occurs before the current position.) No throw (c is non-null on the divergent inputs) and no hang (i only increases). Divergence is confined to the exported tracker state and extractProse's prose text: no finding, level, line, or exit code changes. ADVISORY_PATH. |
| 485 | ConditionalExpression | `true` | Survived | survivor | ADVISORY_PATH | feed() now returns unconditionally as soon as a raw-text element (script/style) opens, so the rest of the chunk is never scanned and never contributes to the depth balance. c is computed but unused, so nothing throws, and the early return trivially terminates. Divergence is confined to the exported tracker state and extractProse's prose text — createHtmlContainerTracker's only consumer — so no finding, level, line, or exit code changes: ADVISORY_PATH. The existing canonical.test raw-text assertion (`t.feed('<script>var a = "<ul>";</script>'); expect(t.isInside()).toBe(false)`) cannot kill it because the expected answer is false either way; the killing shape needs markup AFTER the raw-text element. |
| 30 | ConditionalExpression | `false` | Survived | survivor | OFF_PATH | Span is exactly `typeof key !== "string"` -> false, leaving `false \|\| !TRAILER_KEY_RE.test(key)`. For every STRING key the first disjunct is false in the original too, so the two are bit-identical on the whole string domain (probe: 9 string keys incl. "", "9lead", "has space", "kéy", fullwidth Run-Id — 0 diffs). Divergence needs a NON-string whose String() coercion satisfies the ASCII trailer grammar (null, undefined, true, false, NaN, Infinity): the regex test then passes and `key.toLowerCase()` throws TypeError instead of returning {validKey:false}. That input is unreachable in-process: the only two call sites are commit-check.mjs:146 `canonicalGovernedKey(e.key)` and :161 `canonicalGovernedKey(m[1])`, both fed by regex captures (always strings), and body-check never calls it — checkCommit over 12 messages x 2 option sets showed 0 divergence, and a 3000-case checkBody fuzz showed 0 divergence. So no rule id/level/presence/line and no verdict can change; the mutant only removes a defensive type guard on a path no exported entry point can reach (same mechanism class as the r1 check-commit.mjs:43 malformed-record guard, NO-COVERAGE explained). It is nonetheless killable at module level (`canonicalTrailerKey(null, GOV)` returns vs throws) if the lane wants the defensive arm pinned. |
| 73 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `Tab: "\t"` -> `""`. NOT equivalent: decodeHtmlEntities("&Tab;") returns "" instead of "\t", an observable change in the exported API's return value (and in meaningfulVisibleHtml().decoded). But the decision is invariant: "\t" is White_Space, so MEANINGFUL_RE rejects it exactly as it rejects "", and isMeaningfulText is the ONLY in-process consumer of decoded text — body-check uses it at lines 582/624/634/655 and concatenates it at 681 solely to feed isMeaningfulText; heading/section matching uses inlineText, which skips html node values (line 90 `n.type !== "html"`), and extractProse likewise uses inlineText, so neither the section grammar, PRX-B-ORDER/EXT, looksNarrative, nor the Vale prose stream can see it. No finding presence/rule/level/line and no verdict can change on any input. |
| 74 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `NewLine: "\n"` -> `""`. Not equivalent (exported decode return value changes) but decision-invariant: U+000A is White_Space, so isMeaningfulText is false for both expansions. The only consumer of decoded html text is isMeaningfulText; the newline cannot reach any line-oriented predicate because inlineText (used by heading text, UNICODE_BULLET_RE and extractProse) skips html node values, and looksNarrative runs on code-node values only. No blocking or advisory finding can change. |
| 76 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `NonBreakingSpace: " "` -> `""`. Not equivalent (decode return value changes) but decision-invariant: U+00A0 is White_Space, so isMeaningfulText(" ") === isMeaningfulText("") === false. Decoded html text feeds nothing but isMeaningfulText (proof as in id 46), so no finding or verdict can change. (The sibling `nbsp` key is pinned by canonical.test.ts and is recorded Killed; this alias is not pinned.) |
| 78 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `ensp: " "` -> `""`. Exported decode output changes (not equivalent), but U+2002 is White_Space so the isMeaningfulText verdict is identical for expansion and ""; decoded html text has no consumer other than isMeaningfulText (id 46 proof), so no rule presence/level/line and no verdict can change. |
| 79 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `emsp: " "` -> `""`. U+2003 is White_Space; expansion and "" are both non-meaningful, and decoded html text feeds only isMeaningfulText (id 46 proof). Observable on the exported decoder (so not equivalent) but off every rule path. |
| 80 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `emsp13: " "` -> `""`. U+2004 is White_Space; both expansions score non-meaningful, and the decoded text reaches nothing but isMeaningfulText (id 46 proof). No finding or verdict divergence. |
| 81 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `emsp14: " "` -> `""`. U+2005 is White_Space; meaningfulness verdict identical, decoded text consumed only by isMeaningfulText (id 46 proof). |
| 82 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `numsp: " "` -> `""`. U+2007 is White_Space; meaningfulness verdict identical, decoded text consumed only by isMeaningfulText (id 46 proof). |
| 83 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `puncsp: " "` -> `""`. U+2008 is White_Space; meaningfulness verdict identical, decoded text consumed only by isMeaningfulText (id 46 proof). |
| 85 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `ThinSpace: " "` -> `""`. U+2009 is White_Space; identical meaningfulness verdict. (The lowercase `thinsp` alias IS pinned by canonical.test.ts and recorded Killed; this alias is unpinned, which is why it survives.) Decoded text feeds only isMeaningfulText (id 46 proof). |
| 86 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `hairsp: " "` -> `""`. U+200A is White_Space; identical meaningfulness verdict; decoded text consumed only by isMeaningfulText (id 46 proof). |
| 87 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `VeryThinSpace: " "` -> `""`. Alias of hairsp; U+200A is White_Space, so the meaningfulness verdict is identical and the decoded text reaches nothing else (id 46 proof). |
| 89 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `NegativeVeryThinSpace: "​"` -> `""`. U+200B is General_Category=Cf, matched by \p{Cf} in MEANINGFUL_RE, so both the expansion and "" are non-meaningful — the emptiness verdict is unchanged. Decoded text feeds only isMeaningfulText (id 46 proof), so no blocking/advisory finding can move. (The `ZeroWidthSpace` primary spelling is pinned by canonical.test.ts and recorded Killed.) |
| 90 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `NegativeThinSpace: "​"` -> `""`. Same as id 62: U+200B is Cf, non-meaningful either way; decoded text has no consumer beyond isMeaningfulText. |
| 91 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `NegativeMediumSpace: "​"` -> `""`. Same as id 62: U+200B is Cf, non-meaningful either way; no rule can observe the difference. |
| 92 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `NegativeThickSpace: "​"` -> `""`. Same as id 62: U+200B is Cf, non-meaningful either way; no rule can observe the difference. |
| 95 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `lrm: "‎"` -> `""`. U+200E LEFT-TO-RIGHT MARK is Cf, so \p{Cf} rejects it exactly as MEANINGFUL_RE rejects "": the emptiness verdict is invariant. Decoded html text feeds only isMeaningfulText (id 46 proof), never heading matching or prose. |
| 96 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `rlm: "‏"` -> `""`. U+200F is Cf; non-meaningful either way, and the decoded text has no other consumer (id 46 proof). |
| 97 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `MediumSpace: " "` -> `""`. U+205F is White_Space; non-meaningful either way, decoded text feeds only isMeaningfulText (id 46 proof). |
| 98 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `ThickSpace: " "` -> `""`. Both code points are White_Space, so the two-character expansion and "" are equally non-meaningful; decoded text feeds only isMeaningfulText (id 46 proof). Observable on the exported decoder, invisible to every rule. |
| 100 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `ApplyFunction: "⁡"` -> `""`. U+2061 is Cf; the emptiness verdict is identical for expansion and "", and the decoded text reaches nothing but isMeaningfulText (id 46 proof). |
| 101 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `af: "⁡"` -> `""`. Alias of ApplyFunction; U+2061 is Cf, non-meaningful either way; no rule can observe the difference (id 46 proof). |
| 102 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `InvisibleTimes: "⁢"` -> `""`. U+2062 is Cf; identical emptiness verdict; decoded text has no consumer beyond isMeaningfulText (id 46 proof). |
| 103 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `it: "⁢"` -> `""`. Alias of InvisibleTimes; U+2062 is Cf, non-meaningful either way (id 46 proof). |
| 104 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `InvisibleComma: "⁣"` -> `""`. U+2063 is Cf; identical emptiness verdict; decoded text consumed only by isMeaningfulText (id 46 proof). |
| 105 | StringLiteral | `""` | Survived | survivor | OFF_PATH | `ic: "⁣"` -> `""`. Alias of InvisibleComma; U+2063 is Cf, non-meaningful either way (id 46 proof). |
| 117 | EqualityOperator | `cp >= 0x10ffff` | Survived | survivor | OFF_PATH | `cp > 0x10ffff` -> `cp >= 0x10ffff` moves exactly one code point, U+10FFFF, from the fromCodePoint arm to the U+FFFD arm. Not equivalent: decodeHtmlEntities("&#x10FFFF;") / ("&#1114111;") returns "�" instead of "\u{10FFFF}". But U+10FFFF is General_Category=Cn (a noncharacter), neither White_Space nor Cf, and U+FFFD is likewise meaningful, so isMeaningfulText — the sole consumer of decoded text (id 46 proof) — answers true in both. No finding presence/rule/level/line and no verdict can change on any input; the divergence is confined to the decoded string. Note this is the same weak-oracle SHAPE flagged for id 427: canonical.test.ts pins only "&#1114112;" (one past the boundary), never the boundary itself. |
| 117 | EqualityOperator | `cp < 0xdfff` | Survived | survivor | OFF_PATH | `cp <= 0xdfff` -> `cp < 0xdfff` releases exactly one code point, U+DFFF, from the U+FFFD arm: decodeHtmlEntities("&#xDFFF;") returns the lone surrogate "\uDFFF" instead of "�". Not equivalent (exported decoder output changes, and the WHATWG rule the comment cites is violated at one code point). But a lone surrogate is General_Category=Cs — neither White_Space nor Cf — so MEANINGFUL_RE matches it exactly as it matches U+FFFD; isMeaningfulText, the only consumer of decoded text (id 46 proof), returns true either way. No finding or verdict can change. Same weak-oracle shape as id 106: the test pins &#xD800; (the low end) but not &#xDFFF; (the high end). |
| 228 | ConditionalExpression | `false` | Survived | survivor | OFF_PATH | `if (value.length > limitBytes) return true;` -> `if (false)` removes the bounded-cost fast path. For every STRING the result is unchanged: UTF-8 length >= UTF-16 length code-unit-for-code-unit (BMP unit -> 1-3 bytes, surrogate pair (2 units) -> 4 bytes, lone surrogate -> 3 bytes for U+FFFD), so value.length > limitBytes implies byteLength(value) > limitBytes and the fallthrough returns true anyway; NaN and negative limits also agree (both comparisons behave identically). So no PRX-C-SIZE / PRX-B-SIZE / evidenceRef byte-cap decision, and no finding line/level/verdict, can change — confirmed by the commit battery (incl. a 600k multi-byte message and the Evidence 120-byte limit/limit+1 pair) and a 3000-body fuzz. I do NOT call it EQUIVALENT: with a non-string that carries a .length property the original short-circuits to true while the mutant reaches Buffer.byteLength and throws TypeError. That input is unreachable in-process — every call site guards the type first (commit-check.mjs:193 `typeof raw !== "string" \|\|`, body-check.mjs:156 and :490 the same, canonical.mjs:375 evidenceRef's `typeof value !== "string"`) — so the divergence lives only on a path no exported entry point can reach. |
| 372 | Regex | `/[A-Za-z][A-Za-z0-9+.-]*:/` | Survived | survivor | OFF_PATH | Dropping the ^ makes REF_SCHEME_RE match a scheme-shaped run ANYWHERE, so values carrying a colon in a non-initial position are rejected EARLIER, with reason "URI scheme or drive-letter prefix" instead of the reason the later check would have produced ("segment outside the declared grammar", "unknown root segment", ...). The verdict never flips, because a value that survives to the end is colon-free: every segment is validated by one of the three colon-rejecting gates — the roots table lookup (segments[0]), roots.run.firstSegmentRe = RUN_ID_RE (segments[1]), or EVIDENCE_REF_SEGMENT_RE (the rest) — and the sentinel returns before this line. Divergence is therefore confined to the `reason` string, which no caller renders: commit-check.mjs:496 and body-check.mjs:565 consume only `.valid`, and their diagnostic text is a fixed literal. Per the taxonomy this is message-text-only divergence, hence OFF_PATH, not EQUIVALENT. Residual boundary (stated, not hidden): a caller-supplied config whose roots keys contained a colon could flip validity; no such config exists (EVIDENCE_REF_CONFIG in rules.mjs is frozen with roots run/docs, and canonical.test.ts mirrors it). |
| 399 | Regex | `/[A-Za-z]:/` | Survived | survivor | OFF_PATH | Dropping ^ from the drive-letter test widens the branch to any value containing letter+colon anywhere AND no '/'. Every newly captured value is still rejected — by the very next check (REF_SCHEME_RE) or by the root lookup — so only the `reason` string changes ("drive-letter prefix" instead of "URI scheme or drive-letter prefix" / "unknown root segment"). Validity cannot flip: the branch is gated on !value.includes("/"), and every valid reference under the shipped config contains a slash (roots docs minSegments 2, run minSegments 3), while the only slash-free acceptance — the sentinel — returns at line 378 before this line. Reason strings are never rendered by either checker (both read only .valid), so this is message-text-only divergence: OFF_PATH. Related note for the parent: this line is the twin of mutant 427 (same statement, the `valid:false` literal). 427's kill was restored by re-adding the `.valid` assertion to `reject()` in canonical.test.ts; that same strengthened oracle does NOT kill 421 (the verdict is unchanged), so 421 is a genuine reason-granularity survivor and not a second weak-oracle case. Ids 428/429 sit on the same return object (reason literal / neighbouring evidenceRef reason strings) and belong to another group, but by the same argument they can only be reason-text mutants unless they also carry a `valid` literal — the `valid` literal is the only field with blocking reach. |
| 179 | MethodExpression | `tag[1].toUpperCase()` | Survived | survivor | EQUIVALENT | EQUIVALENCE PROOF. Operator: MethodExpression toLowerCase -> toUpperCase on the value assigned to the closure variable `insideRemoved`. insideRemoved has exactly two uses: (a) the truthiness test at line 152 `if (insideRemoved)` and, transitively, `isInside()` at line 192 `insideRemoved !== null` — both blind to case because tag[1] is non-empty and non-null under either casing; (b) interpolation into `new RegExp(`</${insideRemoved}[\t\n\f\r ]*>`, "i")` at line 153, whose "i" flag makes matching case-insensitive. Invariant relied on: tag[1] is captured by /^<\/?([A-Za-z][A-Za-z0-9-]*)/ , so it is pure ASCII letters/digits/hyphen. Why it holds: that regex is the only writer of tag. Consequences: (i) ASCII case conversion is locale-independent for String.prototype.toLowerCase/toUpperCase (only toLocale*Case varies), so no Turkish-I class hazard; (ii) the RegExp "i" flag performs simple case folding, which is exact and symmetric over ASCII letters, so `</SCRIPT[...]*>`/i and `</script[...]*>`/i accept the identical language; (iii) the only non-alphanumeric character the grammar admits is "-", which is unaffected by case conversion and is not a regex metacharacter outside a character class, so the compiled pattern is structurally identical. Note the mutant is NOT equivalent under a companion mutation of the "i" flag (id 144) — but single-mutant analysis is the contract here. Empirically confirmed by 6000 multi-chunk scanner sequences (alphabet including SCRIPT/Script/svg/my-el/self-closing forms), 8000 single-value fuzz cases, 3000 checkBody bodies and the full commit battery: zero divergence anywhere. |
| 180 | ArithmeticOperator | `gt - 1` | Survived | survivor | EQUIVALENT | EQUIVALENCE PROOF. Operator: ArithmeticOperator `i = gt + 1` -> `i = gt - 1` on the line that resumes scanning immediately after an OPENING tag of a removed-content element. Control flow: this statement executes only inside the branch that has just set `insideRemoved`, so the very next loop iteration takes the `if (insideRemoved)` arm at line 152, whose only use of i is `close.exec(value.slice(i))` followed by `i += m.index + m[0].length`. Let idx_k be the match index in value.slice(k). The mutant searches a window that starts exactly 2 characters earlier, at gt-1 and gt. Invariant relied on: a match of /<\/name[\t\n\f\r ]*>/i must begin with '<' followed by '/'. Why it holds for the extra window: value[gt] === '>' by construction (gt = value.indexOf(">", i+1)), so no match can start at gt; and a match starting at gt-1 would require value[gt] === '/', contradicting value[gt] === '>'. Therefore idx_{gt-1} = idx_{gt+1} + 2 whenever a match exists, and the resulting i is (gt-1) + (idx+2) + L = (gt+1) + idx + L — identical. When no match exists both arms `return out` identically. Termination is preserved: the branch is entered only when the tag probe matched a letter at i+1, so the tag is at least `<X>` and gt >= i+2, giving gt-1 >= i+1 > i (strict forward progress, no infinite loop). Empirically zero divergence over 8000 single-value fuzz cases, 6000 multi-chunk sequences, 3000 bodies and the commit battery. |
| 221 | StringLiteral | `""` | Survived | survivor | EQUIVALENT | EQUIVALENCE PROOF. Operator: StringLiteral, the encoding argument of `Buffer.byteLength(value, "utf8")` -> `""`. Node's implementation (lib/buffer.js byteLength) tests `if (!encoding) return byteLengthUtf8(string)` BEFORE any encoding lookup, so a falsy encoding — the empty string included — takes the same UTF-8 path as the explicit "utf8"; for non-string inputs (Buffer/TypedArray/ArrayBuffer) the encoding argument is ignored entirely and .byteLength is returned in both variants, and for anything else both throw the same TypeError. Invariant relied on: the pinned runtime (node v22.22.0 here, and the lockfile-pinned CI image) implements that falsy-encoding fallback; this is the same class of proof as the r1 rows for readFileSync's blanked encoding, but the API is different so I do not carry that row. Since byteLength is the sole implementation behind exceedsByteLimit — used for PRX-C-SIZE, PRX-B-SIZE and the evidenceRef maxBytes check — the multi-byte boundary behavior is what matters, and it is byte-identical: é=2, €=3, emoji=4, and the 120-byte Evidence boundary fixtures at limit and limit+1 are unchanged. |
| 308 | ArithmeticOperator | `4 + col % 4` | Survived | survivor | EQUIVALENT | Operator: the tab-advance term inside indentColumns. indentColumns is a module-private function whose ONLY consumer is the predicate `indentColumns(line) >= 4` at line 346; the numeric value is never returned, printed, or compared to anything else. Logical implication: both versions start at col=0 and are strictly increasing over the leading run of spaces/tabs; a space contributes +1 in both. On a tab the original assigns the next multiple of 4, which is > col and a positive multiple of 4, hence ALWAYS >= 4; the mutant assigns col + 4 + (col % 4) >= col + 4 >= 4. Therefore after the first tab both counters are >= 4 and, being non-decreasing, stay >= 4 for the rest of the prefix; on prefixes with no tab the two counters are literally equal (space-only arithmetic is untouched). So the >= 4 predicate — the only observable — agrees on every input, and kinds/unclosedFence are byte-identical. Invariant relied on: 4 - (col % 4) ∈ [1,4] and col + (4 - col%4) ≡ 0 (mod 4), both properties of the modulus, no configuration involved. |
| 378 | ConditionalExpression | `true` | Survived | survivor | EQUIVALENT | The mutation replaces only the LEFT conjunct (columns 7..31 = `config.sentinel !== null`); the guard becomes `true && value === config.sentinel`, i.e. `value === config.sentinel`. Logical implication: line 375 has already returned for every non-string value, so at line 378 `value` is a string. If config.sentinel === null the original short-circuits to false, and the mutant evaluates `<string> === null`, which is false by strict equality (no coercion) — same result. If config.sentinel !== null both expressions are the identical right operand. There is no third case (undefined, objects, numbers all behave as the second case since a string is never strictly equal to a non-string). Invariant relied on: the typeof guard at line 375 dominates line 378 on every path, so `value` cannot be null. Nothing else in the function reads the conjunct. |
| 434 | MethodExpression | `segments` | Survived | survivor | EQUIVALENT | Line 434 executes only inside `if (root.firstSegmentRe)`. Under the frozen adapter config (rules.mjs EVIDENCE_REF_CONFIG) exactly one root carries firstSegmentRe: `run`. So every value reaching this line has segments[0] === "run" and segments[1] matching RUN_ID_RE, and the mutant's only effect is to submit those two extra segments to config.segmentRe. Both pass, necessarily: "run" matches /^[A-Za-z0-9][A-Za-z0-9._-]*$/, and RUN_ID_RE = /^ONE-[0-9]{8}-[A-Z0-9]+$/ ⊂ segmentRe (every such string starts with 'O' ∈ [A-Za-z0-9] and is composed only of [A-Z0-9-] ⊆ [A-Za-z0-9._-]). Hence the grammar loop's verdict, the reason string, and the return value are identical on every input. Note that the `__proto__` prototype-chain route that makes the sibling mutant 487 live cannot reach this line: Object.prototype/Object carry no firstSegmentRe property, so those values take the non-firstSegmentRe path. Boundary stated explicitly rather than hidden: the invariant is a property of the CONFIG, not of the code. I probed the boundary — with a hypothetical config `roots: { run: { firstSegmentRe: /^_id$/, minSegments: 3 } }`, evidenceRef("run/_id/x") is valid under the original and invalid under the mutant. No such config exists in the repo (the adapter config is Object.frozen and canonical.test.ts mirrors it), so under every reachable configuration the mutant is behaviourally identical; if a future adapter root's firstSegmentRe ever admits a first segment outside segmentRe, this row must be re-opened. |
| 465 | EqualityOperator | `i <= value.length` | Survived | survivor | EQUIVALENT | The mutant grants exactly one extra loop iteration, at i === value.length, and that iteration is provably inert. Step by step at i = len: (1) `value.startsWith("<!--", len)` is false for every string (no four characters remain), so the comment branch is not taken and value.indexOf is never called; (2) TAG_SCAN_RE.lastIndex is set to len and exec searches forward from len — the pattern requires a literal '<', so no match is possible and m is null; (3) `!m` is true, so the body executes `i += 1; continue;`, giving i = len + 1 and terminating the loop under both `<` and `<=`. No `depth` update, no early return, no state mutation. The only shared state is TAG_SCAN_RE.lastIndex, and it is unobservable: line 472 assigns lastIndex before every exec, so no later read can see the difference (a failed exec resets it to 0 in both versions). Empty input is covered by the same argument (one inert iteration instead of none). |

#### `scripts/prx/modes.mjs`

| Line | Mutator | Replacement | Run-2 | Replay | Class | Disposition / proof |
| --- | --- | --- | --- | --- | --- | --- |
| 17 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Blanking the PRX-C-SIZE literal inside APPROVED_BLOCKING has two blocking-path effects. (1) At module load the filter calls ruleClass(""), which is not a RULES key, so rules.mjs:150 throws `unknown PRX rule id: ` — importing modes.mjs fails outright, so resolveVerdict/parseModeState/APPROVED_BLOCKING are unavailable and both CLI wrappers die (exit 2 crash path) for every input. (2) Even absent the guard, the approved set would lose PRX-C-SIZE, so an enforcing-mode PRX-C-SIZE error would return exitCode 0 instead of 1. Both are verdict/exit-code changes on an APPROVED_BLOCKING rule. Ground truth: the current suite already kills this (modes.test.ts imports modes.mjs; the throw fails the whole file, and the exact-set pin + per-rule blocking test would fail anyway) — the Stryker survival is the static/module-scope activation artifact (the vitest runner does not reliably re-execute module scope under in-worker activation), exactly the r1 mechanism. [carried r1: modes.mjs r1 line 17 \| StringLiteral \| `""` \| `"PRX-C-SIZE",` — ARTIFACT (replay-killed, static module-scope)] |
| 18 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-SUBJECT literal: ruleClass("") throws at module load (`unknown PRX rule id: `), killing every exported entry point; and the approved set would otherwise lose PRX-C-SUBJECT so an enforcing PRX-C-SUBJECT error would exit 0 instead of 1. Verdict/exit-code divergence on an APPROVED_BLOCKING rule. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 18 \| StringLiteral \| `""` \| `"PRX-C-SUBJECT",` — ARTIFACT (replay-killed, static module-scope)] |
| 19 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-PREFIX literal: module-load throw via ruleClass(""), and otherwise loss of PRX-C-PREFIX from the approved set (enforcing exitCode 1 → 0). Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 19 \| StringLiteral \| `""` \| `"PRX-C-PREFIX",` — ARTIFACT (replay-killed, static module-scope)] |
| 20 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-SEPARATOR literal: ruleClass("") throws at load; otherwise PRX-C-SEPARATOR stops blocking in enforcing mode (exitCode 1 → 0). Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 20 \| StringLiteral \| `""` \| `"PRX-C-SEPARATOR",` — ARTIFACT (replay-killed, static module-scope)] |
| 21 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-FENCE literal: ruleClass("") throws at load; otherwise PRX-C-FENCE stops blocking in enforcing mode. Blocking-rule verdict/exit-code divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 21 \| StringLiteral \| `""` \| `"PRX-C-FENCE",` — ARTIFACT (replay-killed, static module-scope)] |
| 22 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-TRAILER literal: ruleClass("") throws at load; otherwise PRX-C-TRAILER stops blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 22 \| StringLiteral \| `""` \| `"PRX-C-TRAILER",` — ARTIFACT (replay-killed, static module-scope)] |
| 23 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-GOV literal: ruleClass("") throws at load; otherwise the governed-trailer blocking rule silently stops blocking in enforcing mode (exitCode 1 → 0). Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 23 \| StringLiteral \| `""` \| `"PRX-C-GOV",` — ARTIFACT (replay-killed, static module-scope)] |
| 24 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-C-FIXUP literal: ruleClass("") throws at load; otherwise PRX-C-FIXUP stops blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 24 \| StringLiteral \| `""` \| `"PRX-C-FIXUP",` — ARTIFACT (replay-killed, static module-scope)] |
| 25 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | PRX-C-CONTROL is NEW in r2 (it does not exist in the r1 APPROVED_BLOCKING list at d98793c21 — verified: r1 lines 17-30 hold 14 ids and PRX-C-CONTROL is not among them), so there is no carried r1 row; the MECHANISM is nevertheless the id-6 family. Blanking the literal makes the load-time filter call ruleClass(""), which throws `unknown PRX rule id: `, so importing modes.mjs fails and every exported entry point (resolveVerdict, parseModeState, APPROVED_BLOCKING) plus both CLI wrappers die; absent the guard, PRX-C-CONTROL would drop out of the approved set and an enforcing-mode control-character error would exit 0 instead of 1. Blocking-rule verdict/exit-code divergence. Already killed by the real suite (ground truth ids 6-20); Stryker survival is the static module-scope activation artifact. |
| 26 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-B-SIZE literal (r1 line 25, shifted to 26 by the r2 insertion of PRX-C-CONTROL): ruleClass("") throws at load; otherwise the body size cap stops blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 25 \| StringLiteral \| `""` \| `"PRX-B-SIZE",` — ARTIFACT (replay-killed, static module-scope)] |
| 27 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-B-VISIBLE literal (r1 line 26 → r2 line 27): ruleClass("") throws at load; otherwise PRX-B-VISIBLE stops blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 26 \| StringLiteral \| `""` \| `"PRX-B-VISIBLE",` — ARTIFACT (replay-killed, static module-scope)] |
| 28 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-B-SECTION-MISSING literal (r1 line 27 → r2 line 28): ruleClass("") throws at load; otherwise the missing-section blocking rule stops blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 27 \| StringLiteral \| `""` \| `"PRX-B-SECTION-MISSING",` — ARTIFACT (replay-killed, static module-scope)] |
| 29 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-B-SECTION-DUP literal (r1 line 28 → r2 line 29): ruleClass("") throws at load; otherwise duplicate-section findings stop blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 28 \| StringLiteral \| `""` \| `"PRX-B-SECTION-DUP",` — ARTIFACT (replay-killed, static module-scope)] |
| 30 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-B-SECTION-EMPTY literal (r1 line 29 → r2 line 30): ruleClass("") throws at load; otherwise empty-section findings stop blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 29 \| StringLiteral \| `""` \| `"PRX-B-SECTION-EMPTY",` — ARTIFACT (replay-killed, static module-scope)] |
| 31 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Same mechanism as id 6 at the PRX-B-CAPSULE literal (r1 line 30 → r2 line 31): ruleClass("") throws at load; otherwise capsule findings stop blocking in enforcing mode. Blocking-rule verdict divergence. Already killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 30 \| StringLiteral \| `""` \| `"PRX-B-CAPSULE",` — ARTIFACT (replay-killed, static module-scope)] |
| 33 | ConditionalExpression | `true` | Survived | killed | BLOCKING_PATH | Forcing the registry-drift guard to true makes the filter throw on the FIRST element, so modes.mjs fails at module load with `APPROVED_BLOCKING may only contain deterministic rules; PRX-C-SIZE is deterministic`. APPROVED_BLOCKING, resolveVerdict and parseModeState never come into existence: every commit/body verdict and both CLI wrappers crash instead of returning an exit code. That is a maximal change to resolveVerdict/exit-code behavior, hence BLOCKING_PATH, not equivalent. Ground truth confirms the real suite kills it; the Stryker survival is the static module-scope activation artifact (in-worker activation does not re-execute module scope). [carried r1: modes.mjs r1 line 32 \| ConditionalExpression \| `true` \| `if (ruleClass(id) !== "deterministic") {` — ARTIFACT (replay-killed, static module-scope)] |
| 33 | EqualityOperator | `ruleClass(id) === "deterministic"` | Survived | killed | BLOCKING_PATH | Inverting the guard makes it true for exactly the ids that are deterministic — i.e. all 15 — so the filter throws on the first element and modes.mjs fails at module load (`... PRX-C-SIZE is deterministic`). No exported binding is created; every verdict path and both CLI wrappers crash rather than returning an exit code. Blocking-path (resolveVerdict/exit-code behavior destroyed), not equivalent. Ground truth: killed by the real suite; the Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 32 \| EqualityOperator \| `ruleClass(id) === "deterministic"` — ARTIFACT (replay-killed, static module-scope)] |
| 33 | StringLiteral | `""` | Survived | killed | BLOCKING_PATH | Blanking the comparison literal turns the guard into `ruleClass(id) !== ""`, which is true for every id (every class string is non-empty), so the filter throws on the first element and modes.mjs fails at module load. Same consequence as ids 22/24: no APPROVED_BLOCKING, no resolveVerdict, both CLI wrappers crash — a change to verdict/exit-code behavior on every input, so BLOCKING_PATH. Ground truth: killed by the real suite; Stryker survival is the static module-scope activation artifact. [carried r1: modes.mjs r1 line 32 \| StringLiteral \| `""` \| `if (ruleClass(id) !== "deterministic") {` — ARTIFACT (replay-killed, static module-scope)] |
| 33 | BlockStatement | `{}` | NoCoverage | survivor | OFF_PATH | Reachability argument: the emptied block is the body of `if (ruleClass(id) !== "deterministic")` inside the module-scope filter. The guard's operands are two input-independent frozen module literals — the 15 ids in modes.mjs 17-31 and the RULES table in rules.mjs 24-51, where each of those ids exists with class "deterministic" — so the condition is false for every element on every execution, and the block body is unreachable from ANY exported entry point (resolveVerdict, parseModeState, APPROVED_BLOCKING) on any input. That is exactly why Stryker records NoCoverage. Emptying unreachable code therefore produces no divergence in findings, verdicts, exit codes or thrown errors (probed identical). Classified OFF_PATH per the unreachable-code rule rather than EQUIVALENT because the mutant deletes a registry-drift fail-closed guard: it is behaviour-preserving only while that module-scope invariant holds, and the invariant is a property of the current sources, not of the code under mutation. [carried r1: modes.mjs r1 line 32 \| BlockStatement \| `{}` \| NoCoverage — NO-COVERAGE (explained): load-guard throw block, intentionally dead defensive code] |
| 35 | StringLiteral | `''` | NoCoverage | survivor | OFF_PATH | Doubly off-path. (1) Reachability: the literal is the first half of the Error MESSAGE constructed inside the load-guard throw block, which executes only when `ruleClass(id) !== "deterministic"` for a listed id — impossible while the two module-scope frozen literals (APPROVED_BLOCKING ids and the RULES class table) hold, so it is unreachable in-process from every exported entry point; hence NoCoverage. (2) Even if it were reachable, the mutation changes only the text of a thrown Error message (an internal drift diagnostic, not a makeFinding message): no rule id, level, line, presence, or exit code changes. Nothing in the decision path parses that string. [carried r1: modes.mjs r1 line 34 \| StringLiteral \| `''` \| NoCoverage — NO-COVERAGE (explained): message literal of that same unreachable defensive throw] |
| 16 | MethodExpression | `["PRX-C-SIZE", "PRX-C-SUBJECT", "PRX-C-PREFIX", "PRX-C-SEPA…` | Survived | survivor | EQUIVALENT | Operator: MethodExpression removes the `.filter(id => {...})` call from the APPROVED_BLOCKING array literal, leaving Object.freeze(<literal>). Logical implication: the predicate's ONLY effects are (a) a conditional throw when ruleClass(id) !== "deterministic" and (b) an unconditional `return true`. Since it returns true for every element, filter's output array is element-for-element identical to the receiver literal; the only way the mutant could diverge is if the guard fired, i.e. if some listed id were absent from RULES (ruleClass throws) or classed non-deterministic. Invariant relied on: both operands are module-scope frozen literals in the same repo — the 15 ids in modes.mjs lines 17-31 are all keys of the frozen RULES object in rules.mjs with class "deterministic" (rules.mjs lines 24-51), and neither literal depends on any input, so no reachable input can change the predicate's verdict. The invariant is additionally pinned by modes.test.ts "contains only deterministic-class rules". Array identity (fresh array vs literal) is unobservable: both are frozen arrays with equal contents and the exported API exposes only membership/iteration. NOT a message-text or presence change of any kind — probed byte-identical. [carried r1: modes.mjs r1 line 16 \| MethodExpression \| `["PRX-C-SIZE", "PRX-C-SUBJECT", "PRX-C-P` — "APPROVED_BLOCKING .filter(...) removal" (EQUIVALENT)] |
| 33 | ConditionalExpression | `false` | Survived | survivor | EQUIVALENT | Operator: ConditionalExpression forces the drift guard's condition to false, so the throw block is skipped and the predicate falls through to `return true` for every element. Logical implication: in the unmutated module the condition `ruleClass(id) !== "deterministic"` is ALREADY false for every element, so the throw never fires and the predicate already returns true for every element — forcing false replaces an always-false expression with false. Invariant relied on: all 15 ids in the module-scope literal are keys of the frozen RULES object with class "deterministic" (rules.mjs 24-51); both literals are input-independent module constants, so no reachable input can make the guard true. Consequence: the filtered array, and therefore APPROVED_BLOCKING, resolveVerdict's blocking set and exit codes, and parseModeState, are all unchanged. Caveat recorded honestly: the mutant does disable a future-drift safety net, but that is not an observable-behavior difference on the current sources, which is what equivalence is defined over. [carried r1: modes.mjs r1 line 32 \| ConditionalExpression \| `false` \| `if (ruleClass(id) !== "deterministic") {` — EQUIVALENT (load-guard condition -> false)] |
| 50 | OptionalChaining | `state.mode` | Survived | survivor | EQUIVALENT | Operator: OptionalChaining removes the `?.` from `state?.mode` in `if (state?.version !== 1 \|\| !MODES.includes(state?.mode))`. Logical implication: `\|\|` short-circuits, so the mutated member access is evaluated ONLY when the left operand `state?.version !== 1` is false, i.e. when `state?.version === 1`. Optional chaining returns undefined for null/undefined receivers and undefined !== 1, so that outcome requires state to be neither null nor undefined; for every non-nullish receiver `state.mode` and `state?.mode` are the identical operation. Invariant relied on: state is the value of JSON.parse (any JSON value — object, array, string, number, boolean, null — or the parse throws first and is caught into the 'not valid JSON' error), and only a non-nullish JSON value can carry version === 1. Therefore the mutant can never turn a thrown shape Error into a TypeError, and both the returned state object and the exact error messages are unchanged for every input. (The genuinely load-bearing `state?.version` chain on the same line has its own killing test, modes.test.ts `parseModeState("null")` → /prx-mode\.json must be/, and is a different mutant.) [carried r1: modes.mjs r1 line 49 \| OptionalChaining \| `state.mode` — EQUIVALENT (state?.mode -> state.mode; the second \|\| operand only evaluates when state?.version ==…] |

#### `scripts/prx/rules.mjs`

| Line | Mutator | Replacement | Run-2 | Replay | Class | Disposition / proof |
| --- | --- | --- | --- | --- | --- | --- |
| 142 | StringLiteral | `""` | Survived | survivor | ADVISORY_PATH | NOT equivalent: blanking the `"pre"` entry of HTML_CONTAINER_ELEMENTS removes `pre` from the raw-HTML container tracker built by createHtmlContainerTracker (canonical.mjs 459-499, exact lowercase name match against a Set — the empty string can never match a scanned tag name because TAG_SCAN_RE requires a name character, so the mutation is a pure removal). Divergence is real but strictly confined to extractProse: HTML_CONTAINER_ELEMENTS is referenced only at body-check.mjs:511 and :537, both inside extractProse's frame construction (grep-verified), and extractProse feeds only check-body.mjs:54 `writeFileSync(prosePath, extractProse(text))` — the Vale advisory style stream. checkBody's finding multiset (rule/level/line/presence, including every APPROVED_BLOCKING body rule) is untouched, probed identical. Why it survives the existing per-element test (body-check.test.ts:699): for `pre` the test's blank-line-split fixture is vacuous — CommonMark HTML block type 1 (`<pre`, `<script`, `<style`, `<textarea`) runs to a line containing `</pre>`, so the 'leaked' text is already swallowed into one html node and never becomes a paragraph. The tracker's `pre` entry only matters when the `<pre>` opener rides inside a type-6 html block, e.g. a line starting `<div>x</div> <pre>`. |

## The two label-gated reruns: which is authoritative, and what differed (r3 Gap B)

The r2 record named runs `31859979909` and `31860519370` and reported
only the second one's aggregate. Both are stated exactly here.

| | Run 1 | Run 2 |
| --- | --- | --- |
| Workflow run id | `31859979909` | `31860519370` |
| Head | `a436f28abe85f6874b599fe1ba747c3a4edff5c5` | `93bda26ed02ceeed1e61b394dff801e8faf120a0` |
| Created (UTC) | 2026-08-15T02:44:35Z | 2026-08-15T02:56:58Z |
| Jobs (native conclusions) | 6 of 6 `success` | 6 of 6 `success` |
| Valid mutants | 2038 | 2038 |
| Killed | 1720 | 1778 |
| Timeout (detected) | 34 | 34 |
| Survived | 254 | 206 |
| No coverage | 30 | 20 |
| Score (detected / valid) | 86.06% | 88.91% |

**Run 2 is authoritative.** It measured the FINAL r2 test suite against
the final r2 sources, and its head is the r2 candidate head. Run 1 is
the pre-hardening BASELINE: it is the measurement that identified which
survivors the post-run-1 targeted kills then went after, so it is
retained as the "before" figure, not discarded as wrong. Neither run is
superseded in the sense of being invalid; they are the two ends of one
hardening step, and quoting only run 2's aggregate without run 1's
concealed the size and the direction of that step.

**What differed: the test suite only.** The mutated sources are
byte-identical across the two heads —
`git diff a436f28a..93bda26e -- 'scripts/prx/*.mjs' 'scripts/prx/lib/*.mjs' scripts/prx/stryker.blocking.prx.json scripts/prx/vitest.prx.mutation.config.ts .github/workflows/15-prx-mutation.yml pnpm-lock.yaml`
is empty. The mutant POPULATION is therefore identical, and that is
verified rather than assumed: matching every mutant across the two
report sets on (file, start line, start column, end line, end column,
mutator, replacement) yields two sets of the same 2038 valid members
with no unmatched entry on either side. The only inputs that changed
are five test files (+241 / −6 lines): `commit-check.test.ts`
(+96/−1), `body-check.test.ts` (+42/−2), `rules.test.ts` (+37/−1),
`modes.test.ts` (+36/−0), `canonical.test.ts` (+30/−2).

**Exact effect of that difference: 62 mutants changed status**, in
`commit-check.mjs` (20), `rules.mjs` (24), and `lib/canonical.mjs`
(18); the other three modules are unchanged mutant-for-mutant.

| Transition | Count | Reading |
| --- | --- | --- |
| Survived → Killed | 51 | the targeted kills landed |
| NoCoverage → Killed | 8 | new tests both reached and killed |
| NoCoverage → Survived | 2 | new tests reached the line but do not kill (`canonical.mjs:147`, ids 135/137 — the comment-resume arm of the sanitized-text scanner) |
| Killed → Survived | 1 | a REGRESSION — see below |

**The one regression, stated plainly.** `lib/canonical.mjs:400`
(BooleanLiteral, `valid: false` → `true` on the drive-letter
rejection) was KILLED in run 1 and SURVIVED in run 2. The cause is in
the hardening itself: the run-1 suite asserted this case through the
`reject()` helper, which checks the verdict; the post-run-1 rewrite
replaced that call with a reason-only assertion to pin the exact
reason strings, and dropped the verdict check. The mutant leaves
the reason string untouched, so nothing failed. This is the MUT-01
weak-oracle class reappearing inside the fix for MUT-01's own class,
and the aggregate improvement (+58 net detected) hid it — which is
precisely why the per-transition delta is recorded here and not just
the two totals.

It is not cosmetic: with the mutant applied, a governed commit carrying
`Evidence: C:evil` loses its `PRX-C-GOV` finding entirely, because
`validateEvidenceRef` reports the reference valid. It is classified
BLOCKING_PATH in the r3 register below, and it is fixed — both cases
now route through `reject()` with anchored regexes, keeping the exact
reason pins while restoring the verdict assertion, negative-tested in
both directions.
