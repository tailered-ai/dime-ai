# DEC-003 — DEF-002 resolution path

| Field | Value |
| --- | --- |
| Decision | **DOCUMENT_LIVE_STATE** |
| Required by | DEF-002 |
| Authority | Owner instruction, this session: "Resolve DEF-002 by selecting the documentation-only path" |
| Rejected alternative | RESTORE_CLASSIC_PROTECTION |

## What was done
`docs/verification/RULESETS.md` was corrected to match the live measurement:
ruleset 18701573 is the sole authoritative enforcement surface; classic branch
protection is absent; nine contexts are enforced today; five remain graduating;
the merge queue is not enabled and the ten `merge_group:` triggers are inert.
The 2026-08-05 description is retained verbatim under a "Historical record —
superseded" heading so the audit trail is not erased.

## What was explicitly NOT done
No repository protection, ruleset, required check, approval setting, merge
queue, permission, or organization setting was created, restored, or altered.
The `gh api` snippet that would create classic protection is retained in the
document but annotated NOT APPLICABLE, precisely so it is not run by reflex.

## Verification
`docs/verification/evidence/remediation/def-002-validate.mjs` re-derives the
documented enforced/graduating sets from the markdown and compares them against
live GitHub, asserts classic protection returns 404, and asserts `mergeQueue`
is null. Exit 0 == documentation and live evidence agree exactly.
